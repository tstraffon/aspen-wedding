---
phase: 02-registry-page
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - components/Navbar.tsx
autonomous: false
requirements:
  - D-13
user_setup: []

must_haves:
  truths:
    - "D-13: The Registry nav link is uncommented at `components/Navbar.tsx:13` with `href` updated from `/#registry` to `/registry`"
    - "Visible nav order on desktop (left-to-right after the logo): Home, Travel & Stay, Itinerary, Things To Do, FAQ, Registry, RSVP"
    - "Active-state highlighting works: navigating to `/registry` turns the 'Registry' nav text warm-gold (`text-primary`) via the existing `pathname.startsWith(href)` + `isActive && href === pathname` logic — no code change to the active-state branching"
    - "Mobile menu (collapsed nav) also shows the Registry link because the same `links` array drives both desktop and mobile renders"
    - "End-to-end smoke checklist passes on `localhost:3000` covering every UI-SPEC §Acceptance Criteria item"
  artifacts:
    - path: "components/Navbar.tsx"
      provides: "Nav with Registry link wired to /registry"
      contains: '{ label: "Registry", href: "/registry" },'
  key_links:
    - from: "Navbar Registry link"
      to: "/registry route (Plan 01 + 02 output)"
      via: "Next.js `<Link>` client navigation"
      pattern: 'href="/registry"'
    - from: "Navbar active-state logic (lines 42-48 and 83-89)"
      to: "/registry pathname match"
      via: "`pathname.startsWith(href)` plus `isActive && href === pathname`"
      pattern: "pathname.startsWith"
---

<objective>
Wire the new `/registry` route into the global Navbar with a single one-line edit, then run the full lint + build + manual smoke checklist that closes the phase.

Purpose: The page exists after Plan 02 but is undiscoverable without a nav link. This plan is the final wire-up plus the end-to-end verification gate. It carries one human checkpoint because the UI-SPEC §Acceptance Criteria has 16 manual-only items (hover timing, reduced-motion behavior, breakpoint resize, a11y tree, etc.) that cannot be automated without introducing a Playwright/Vitest framework — explicitly out of scope per VALIDATION.md Wave 0.

Output: Modified `components/Navbar.tsx` (one line) plus a green smoke checklist documented in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry-page/02-CONTEXT.md
@.planning/phases/02-registry-page/02-UI-SPEC.md
@.planning/phases/02-registry-page/02-PATTERNS.md
@.planning/phases/02-registry-page/02-VALIDATION.md
@.planning/phases/02-registry-page/02-01-SUMMARY.md
@.planning/phases/02-registry-page/02-02-SUMMARY.md
@AGENTS.md
@components/Navbar.tsx
@app/(main)/registry/page.tsx

<interfaces>
<!-- Navbar's `links` array is the only surface this plan touches. Active-state logic is read-only. -->

Current `components/Navbar.tsx:7-16`:
```typescript
const links = [
  { label: "Home", href: "/" },
  // { label: "Our Story", href: "/#our-story" },
  { label: "Travel & Stay", href: "/travel" },
  { label: "Itinerary", href: "/itinerary" },
  { label: "Things To Do", href: "/things-to-do" },
  // { label: "Registry", href: "/#registry" },
  { label: "FAQ", href: "/faq" },
  { label: "RSVP", href: "/rsvp" },
];
```

Active-state logic (lines 42–48 desktop, 83–89 mobile — DO NOT touch per RESEARCH Pitfall 5):
```typescript
const isActive =
  href === "/"
    ? pathname === "/"
    : href.startsWith("/#")
      ? pathname === "/"
      : pathname.startsWith(href);
```

Because `/registry` does not start with `/#` and is not `/`, the third branch (`pathname.startsWith("/registry")`) handles it. The subsequent `isActive && href === pathname` check at line 54 then applies `text-primary` only on exact `/registry` match.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Uncomment Registry nav link and update href</name>
  <files>components/Navbar.tsx</files>
  <read_first>
    - .planning/phases/02-registry-page/02-UI-SPEC.md §"Navbar Integration" (lines 209–220)
    - .planning/phases/02-registry-page/02-CONTEXT.md D-13
    - .planning/phases/02-registry-page/02-PATTERNS.md §"components/Navbar.tsx (client nav component, MODIFY)" (lines 253–293) — and the "Active-state logic — DO NOT TOUCH" warning
    - .planning/phases/02-registry-page/02-RESEARCH.md §"Common Pitfalls" Pitfall 5
    - components/Navbar.tsx (current state — line 13 is the target)
  </read_first>
  <action>
    Replace the commented-out registry entry at `components/Navbar.tsx` line 13 (`// { label: "Registry", href: "/#registry" },`) with the active entry `{ label: "Registry", href: "/registry" },`.

    Two changes happen in the same edit:
    1. Remove the `// ` comment prefix.
    2. Change `href: "/#registry"` to `href: "/registry"` (drop the `#`).

    Position is preserved — the entry stays between `Things To Do` and `FAQ`. After the edit, the `links` array order matches UI-SPEC §Navbar Integration: Home, Travel & Stay, Itinerary, Things To Do, Registry, FAQ, RSVP. Wait — UI-SPEC line 213 specifies: `Home · Travel & Stay · Itinerary · Things To Do · FAQ · Registry · RSVP`. The CURRENT comment position has Registry BEFORE FAQ. UI-SPEC wants Registry AFTER FAQ but BEFORE RSVP. Resolve this by uncommenting at the existing line 13 position AND moving the entry below the FAQ line so the final array is:

    ```typescript
    const links = [
      { label: "Home", href: "/" },
      // { label: "Our Story", href: "/#our-story" },
      { label: "Travel & Stay", href: "/travel" },
      { label: "Itinerary", href: "/itinerary" },
      { label: "Things To Do", href: "/things-to-do" },
      { label: "FAQ", href: "/faq" },
      { label: "Registry", href: "/registry" },
      { label: "RSVP", href: "/rsvp" },
    ];
    ```

    Net effect: delete the commented stub at line 13, insert the active `{ label: "Registry", href: "/registry" },` between FAQ and RSVP. The mobile menu re-uses the same `links` array, so this single edit covers both desktop and mobile renders.

    Do NOT touch the active-state branching (lines 42–48 and 83–89). Do NOT add icons. Do NOT change the styling. Do NOT reorder any other entries.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `components/Navbar.tsx` contains the EXACT line `{ label: "Registry", href: "/registry" },` (no `//` prefix, no `/#`)
    - File contains NO occurrence of `/#registry` (the commented stub is gone)
    - File contains NO occurrence of `// { label: "Registry"` (the commented entry is removed)
    - Registry entry appears AFTER the FAQ entry and BEFORE the RSVP entry in the `links` array source order (verify with: `grep -n '"Registry"\|"FAQ"\|"RSVP"' components/Navbar.tsx` shows FAQ then Registry then RSVP)
    - The active-state branching at lines 42-48 (and the mobile equivalent) is UNCHANGED — the file `git diff` should show only the single `links` array edit
    - `npm run lint` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The Navbar `links` array exports an entry pointing to `/registry`. Lint and build both pass. Mobile and desktop nav both render the Registry link because they share the same array.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke checklist for the complete phase</name>
  <what-built>
    Plans 01–03 of Phase 02 ship the entire `/registry` page (hero + framing + 3-card grid) and wire the Navbar Registry link. The full UI-SPEC §Acceptance Criteria block (lines 270–313, 16 manual-only items per VALIDATION.md §"Manual-Only Verifications") needs human verification because no e2e test framework is installed and VALIDATION.md Wave 0 explicitly forbids introducing one in this phase.
  </what-built>
  <how-to-verify>
    Start the dev server in one terminal:
    ```
    npm run dev
    ```

    Then open `http://localhost:3000` in the browser. Walk every item below; each must pass.

    **A. Navbar (D-13):**
    1. On the homepage, look at the desktop nav (>=768px viewport). Verify left-to-right order: `Home  Travel & Stay  Itinerary  Things To Do  FAQ  Registry  RSVP`. Logo on the left is unchanged.
    2. Click `Registry`. URL changes to `http://localhost:3000/registry`. The page loads (no 404).
    3. While on `/registry`, the nav `Registry` link is rendered in warm gold (`text-primary`, hex `#d4a373`). Other nav items remain muted (`text-on-surface-variant`).
    4. Resize the browser below 768px. The hamburger menu appears. Tap it open — `Registry` is in the mobile menu list. Tap it — navigates to `/registry`, mobile menu closes.

    **B. Hero section (D-05, D-11):**
    5. Scroll to top of `/registry`. Hero fills the viewport-width area at `h-[614px]`. Background image is visible (Unsplash placeholder, scene per Plan 01's image choice).
    6. The hero eyebrow reads `For Our Guests` in uppercase warm-gold with wide tracking.
    7. The hero headline reads `Our Registries` with `Registries` rendered in italic warm-gold (the rest in cream `text-on-surface`).
    8. The hero subtitle reads exactly: `A few places we've put together — but truly, just being there is enough.`
    9. The three hero text elements fade-up in sequence on initial paint (label first ~200ms delay, title ~400ms, subtitle ~600ms).
    10. Scroll up and down a bit — the background image parallaxes relative to the viewport.

    **C. Framing block (D-09, D-10):**
    11. Below the hero, the framing block renders a single centered paragraph (max-w-2xl) reading exactly: `Your presence is the greatest gift. If you'd like to celebrate with something more, here are a few places we've registered.`
    12. Paragraph fades in via `reveal-on-scroll` as it enters the viewport.
    13. There is NO eyebrow label above the framing prose. NO headline. NO exclamation marks anywhere in the framing copy.

    **D. Card grid (D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-12):**
    14. Below the framing block, the grid section renders. Eyebrow reads `Gift Registries`. Section heading reads `A Few of Our Favorites` with `Favorites` italic warm-gold.
    15. Three cards render in this left-to-right order at >=1024px viewport: Honeyfund, Amazon, Crate & Barrel. At 768–1023px, 2-column layout. At <768px, 1-column.
    16. Each card image is `aspect-[4/5]` (taller than wide). Images are scenic photos (mountains/passport, wrapped gift, tabletop) — NOT brand logos.
    17. Hover over each card image — image scales slightly larger over ~1 second; the dark overlay fades to transparent over ~0.5 second; the card title turns warm-gold; the "Visit Registry" CTA gap widens slightly.
    18. Card titles are bare brand names in Noto Serif: `Honeyfund`, `Amazon`, `Crate & Barrel`. No taglines.
    19. Each card has a `Visit Registry` CTA in italic warm-gold with the editorial-underline.

    **E. Security + a11y (T-02-01, UI-SPEC §Accessibility Contract):**
    20. Right-click any card → "Inspect Element". Confirm the `<a>` tag has `target="_blank"`, `rel="noopener noreferrer"`, and `aria-label="Visit <Title> registry (opens in new tab)"`.
    21. Confirm the parallax background div, the gradient scrim div, the card image overlay divs, and the radial gradient div all have `aria-hidden="true"`.
    22. Tab through the page with keyboard. Each card receives a visible focus ring (native browser default). The aria-label is announced if a screen reader is active.
    23. Click a card — opens in a NEW tab. Currently the new tab is just the placeholder `#` URL (resolves to `/registry#`, scrolls to top of new tab). This is expected per D-12 — Tyler swaps real URLs at handoff.
    24. View source on the page. Verify exactly THREE occurrences of `// TODO: replace with real registry URL` (the placeholders for Honeyfund, Amazon, Crate & Barrel are all flagged).

    **F. Reduced motion + cross-page parity:**
    25. Enable OS-level reduce-motion (macOS: System Settings → Accessibility → Display → Reduce motion). Reload `/registry`. Hero reveal animations no longer animate; card hover scale is instant or disabled.
    26. Disable reduce-motion. Visit `/things-to-do` — it still looks the same as before (regression check: this phase did NOT refactor Things-To-Do).
    27. Visit `/`, `/travel`, `/itinerary`, `/faq`, `/rsvp` — they all render normally with the new Registry link visible in the nav.

    **G. Build gate:**
    28. Stop the dev server. Run `npm run lint && npm run build`. Both exit 0 with no warnings about `@next/next/no-img-element` (the eslint-disable comments handle it) and no errors about `images.remotePatterns` (plain `<img>` requires no allowlist).

    If any item above fails, capture the specific failure (screenshot or DOM snippet) and report it instead of approving.
  </how-to-verify>
  <resume-signal>Type "approved — phase 02 ready to close" or describe failing items by letter+number (e.g., "C13 failed: framing block contains an exclamation mark").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | This plan modifies only `components/Navbar.tsx` (one line) and runs a smoke checklist. No new outbound surface, no new input handling. The tabnabbing threat (T-02-01) was mitigated in Plan 02; Task 2 of this plan VERIFIES the mitigation as part of the smoke checklist (item E20). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 (verify) | Tampering (reverse-tabnabbing) | Card `<a>` tags from Plan 02 | mitigate (verify) | Smoke checklist item E20 inspects each card `<a>` in the browser devtools to confirm `target="_blank"` AND `rel="noopener noreferrer"` are present on the rendered DOM. If absent, fail the checkpoint and return to Plan 02. |
| T-02-NAV | Spoofing (open redirect via nav) | Navbar Registry link | accept | The Registry link targets `/registry` — a same-origin route. `next/link` does not interpolate user input. No open-redirect surface. |
</threat_model>

<verification>
- `npm run lint` exits 0 after Task 1
- `npm run build` exits 0 after Task 1
- Smoke checklist (Task 2) every item passes — checker explicitly approves
- `git diff components/Navbar.tsx` shows ONLY the `links` array edit (active-state logic untouched)
- `git diff app/(main)/things-to-do/page.tsx` is empty (Things-To-Do untouched throughout the phase)
- Final phase output: `/registry` is discoverable from every page via the nav, renders with full hero + framing + grid, and ships all 16 UI-SPEC acceptance criteria green
</verification>

<success_criteria>
- `components/Navbar.tsx` `links` array contains `{ label: "Registry", href: "/registry" },` in the position between FAQ and RSVP
- The previously-commented stub `// { label: "Registry", href: "/#registry" },` is removed
- Active-state branching at lines 42-48 and 83-89 is unchanged
- Manual smoke checklist (28 items across 7 sections) passes — checker approves
- Phase 02 ready to close: `/gsd:verify-work 2` will pass
</success_criteria>

<output>
Create `.planning/phases/02-registry-page/02-03-SUMMARY.md` when done. Include:
- D-IDs satisfied (D-13)
- Smoke checklist result (which items, if any, were flagged and how they were resolved before approval)
- Confirmation of T-02-01 verification via item E20
- Confirmation that `git diff` on Things-To-Do is empty (no regression)
- Final nav order observed in the running app
- Phase 02 close-out note: ready for `/gsd:verify-work 2` and `/gsd:close-phase 2`
</output>
</content>
</invoke>