---
phase: 02-registry-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(main)/registry/page.tsx
autonomous: true
requirements:
  - D-05
  - D-09
  - D-10
  - D-11
user_setup: []

must_haves:
  truths:
    - "D-05: /registry route renders the full-bleed cinematic hero (h-[614px], parallax background, gradient scrim, hero-reveal-* entrance animations) matching Things-To-Do"
    - "D-09: A personal framing block renders between the hero and where the grid will sit, centered, max-w-2xl, with the approved gracious-tone copy"
    - "D-10: Framing copy is warm and gracious, no exclamation marks"
    - "D-11: Hero subtitle carries the complementary line locked in UI-SPEC §Copywriting Contract"
  artifacts:
    - path: "app/(main)/registry/page.tsx"
      provides: "Server Component page export with metadata, hero section, framing block section. Card grid section absent (Plan 02 adds it)."
      contains: "export const metadata, export default function RegistryPage, <section relative h-[614px]>, hero-parallax-bg, hero-reveal-label, hero-reveal-title, hero-reveal-subtitle, max-w-2xl framing block"
  key_links:
    - from: "app/(main)/registry/page.tsx"
      to: "app/(main)/layout.tsx"
      via: "(main) route group inheritance — Navbar + Footer + MusicButton wrap this page automatically"
      pattern: "no pt-20 on <main>"
    - from: "app/(main)/registry/page.tsx hero-parallax-bg div"
      to: "app/globals.css .hero-parallax-bg, .hero-reveal-*"
      via: "Tailwind class names referencing existing CSS animation tokens"
      pattern: "hero-(parallax-bg|reveal-(label|title|subtitle))"
---

<objective>
Scaffold the new `/registry` route as a Next.js 16 Server Component with the cinematic hero section and the personal framing block. This is the renderable foundation — visiting `localhost:3000/registry` after this plan ships returns 200, displays the hero and framing copy, but has no card grid yet (Plan 02 adds it).

Purpose: Ship a route that exists and renders before adding the card grid avoids a "half-page" merge if Plan 02 stalls. Splitting hero+framing from the data-bearing grid keeps each plan under ~30% context and keeps the file's structural choices reviewable in isolation.

Output: `app/(main)/registry/page.tsx` (new file) containing `import type { Metadata }`, `export const metadata`, and a Server Component default export with `<main>` -> `<section>` hero -> `<section>` framing block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-registry-page/02-CONTEXT.md
@.planning/phases/02-registry-page/02-RESEARCH.md
@.planning/phases/02-registry-page/02-UI-SPEC.md
@.planning/phases/02-registry-page/02-PATTERNS.md
@.planning/phases/02-registry-page/02-VALIDATION.md
@AGENTS.md
@app/(main)/things-to-do/page.tsx
@app/(main)/travel/page.tsx
@app/(main)/layout.tsx
@app/globals.css

<interfaces>
<!-- Existing exports/utilities the executor consumes. No new types to introduce — this is a static Server Component with no props. -->

From `next` (Next.js 16.2.6):
```typescript
export type Metadata = { title?: string; description?: string; /* ... */ };
```
Used via `import type { Metadata } from "next"` and `export const metadata: Metadata = { ... }`.

From `app/globals.css` (CSS utility classes consumed by this plan — already shipped, no new tokens):
- `.hero-parallax-bg` — CSS `animation-timeline: view()` parallax (lines 364–368)
- `.hero-reveal-label` — 800ms fade-up, 200ms delay (lines 346–356)
- `.hero-reveal-title` — 800ms fade-up, 400ms delay
- `.hero-reveal-subtitle` — 800ms fade-up, 600ms delay
- `.reveal-on-scroll` — scroll-driven fade-slide-up (lines 432–446)
- `.glass-nav` (consumed indirectly via fixed Navbar — hero floats under it)
- Tokens: `bg-background`, `text-on-surface`, `text-on-surface-variant`, `text-primary`, `font-headline`, `font-label`, `font-body`

Next.js 16 page-export shape (from `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md`):
- Static routes take NO props. `function RegistryPage() { ... }` — do not write `function RegistryPage({ params })`.
- `params` / `searchParams` are `Promise<...>` in v15+ if a route ever becomes dynamic. Not applicable here.

(main) layout chrome (from `app/(main)/layout.tsx`):
- `<Navbar />` (fixed, glass-nav, ~h-20) + skip-link + `<Footer />` + `<MusicButton />` wrap every `(main)` route automatically.
- `<main>` on this page should NOT add `pt-20`. The hero is intentionally underneath the fixed glass nav.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create /registry page scaffold with metadata and hero section</name>
  <files>app/(main)/registry/page.tsx</files>
  <read_first>
    - .planning/phases/02-registry-page/02-UI-SPEC.md §"Hero Spec" and §"Copywriting Contract" (lines 120–137, 183–205)
    - .planning/phases/02-registry-page/02-PATTERNS.md §"Imports + Metadata Export" and §"Page Shell + Hero" (sections 1 and 3)
    - .planning/phases/02-registry-page/02-RESEARCH.md §"Pattern 2: Full-Bleed Cinematic Hero" + §"Common Pitfalls" Pitfall 1 (no 'use client') and Pitfall 2 (no next/image)
    - app/(main)/things-to-do/page.tsx (lines 1, 60–92 — hero structural source)
    - app/(main)/travel/page.tsx (lines 1–9 — metadata export precedent)
    - app/globals.css (search for `.hero-parallax-bg`, `.hero-reveal-`)
    - AGENTS.md (heed Next.js 16 deprecation notices)
  </read_first>
  <action>
    Create `app/(main)/registry/page.tsx` as a Server Component (no `'use client'` directive). Imports: `import type { Metadata } from "next";` only — do NOT import `next/link`, `useState`, or any client hooks. Export `metadata` as `{ title: "Registry — Emily & Tyler", description: "A few places we've registered for our Aspen wedding." }` per RESEARCH §Open Questions Q1 and the Travel page precedent.

    Default export is `function RegistryPage()` returning `<main>` (NO `pt-20` per Pitfall 6 / Pattern 2). Inside `<main>`, render a single `<section className="relative h-[614px] w-full overflow-hidden bg-background">` containing:
    - A `<div className="absolute inset-0 z-0">` wrapping two children:
      - The parallax bg div: `<div className="w-full h-full bg-cover bg-center hero-parallax-bg" style={{ backgroundImage: "url('https://images.unsplash.com/photo-1519225421980-715cb0215aed?w=1600&q=80')" }} aria-hidden="true" />` — placeholder URL during execution. Add an inline comment immediately above: `{/* TODO: replace with /public local hero image */}` per RESEARCH §Open Questions Q2.
      - The scrim div: `<div className="absolute inset-0" aria-hidden="true" style={{ background: "linear-gradient(to bottom, rgba(13,27,30,0.05), rgba(13,27,30,0.3))" }} />`
    - A `<div className="relative z-10 h-full max-w-[1440px] mx-auto px-6 md:px-12 flex flex-col justify-end pb-20">` wrapping `<div className="max-w-4xl">` containing the three hero copy elements (exact strings, verbatim from UI-SPEC §Copywriting Contract):
      - Eyebrow `<p className="font-label text-xs uppercase tracking-[0.4em] text-primary mb-6 hero-reveal-label">For Our Guests</p>`
      - Headline `<h1 className="font-headline text-5xl md:text-8xl text-on-surface leading-[0.85] tracking-tighter mb-6 hero-reveal-title">Our <span className="italic font-light text-primary/80">Registries</span></h1>`
      - Subtitle `<p className="text-on-surface-variant text-lg max-w-2xl font-light leading-relaxed hero-reveal-subtitle">A few places we&apos;ve put together — but truly, just being there is enough.</p>` (use `&apos;` for the apostrophe to match the project convention seen in Things-To-Do line 124).

    Do NOT add the card grid section in this task — Plan 02 will append it. Do NOT add `'use client'`. Do NOT add `next/image`. Do NOT add `pt-20` on `<main>`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File `app/(main)/registry/page.tsx` exists
    - First non-comment line is `import type { Metadata } from "next";` (NOT `"use client";`)
    - Source contains `export const metadata` with `title: "Registry — Emily & Tyler"`
    - Source contains `export default function RegistryPage()`
    - Source contains `h-[614px]` exactly once
    - Source contains `hero-parallax-bg`, `hero-reveal-label`, `hero-reveal-title`, `hero-reveal-subtitle`
    - Source contains the exact strings `For Our Guests`, `Our `, `Registries`, and `just being there is enough`
    - Source contains TWO `aria-hidden="true"` attributes (parallax-bg div + scrim div)
    - Source does NOT contain `'use client'`, `useState`, `useEffect`, `pt-20`, or `next/image`
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Hero section renders at `localhost:3000/registry` with parallax background, gradient scrim, and the three locked hero copy strings revealing in sequence. Page is a Server Component (no `'use client'`).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append framing block section between hero and (future) grid</name>
  <files>app/(main)/registry/page.tsx</files>
  <read_first>
    - .planning/phases/02-registry-page/02-UI-SPEC.md §"Framing Block Spec" (lines 141–151) and §"Copywriting Contract" framing block row
    - .planning/phases/02-registry-page/02-PATTERNS.md §"Framing Block" (section 4) — note: no eyebrow per UI-SPEC
    - .planning/phases/02-registry-page/02-RESEARCH.md §"Pattern 4: Framing Block"
    - app/(main)/registry/page.tsx (the file from Task 1 — append, do not rewrite)
    - app/globals.css (search for `.reveal-on-scroll`)
  </read_first>
  <action>
    Append a second `<section>` directly after the hero `<section>` closing tag, before the closing `</main>`. Section markup per UI-SPEC §Framing Block Spec (D-09, D-10):

    `<section className="py-16 bg-background">` containing `<div className="max-w-[1440px] mx-auto px-6 md:px-12">` containing `<div className="max-w-2xl mx-auto text-center reveal-on-scroll">` containing a single `<p className="text-on-surface-variant text-lg font-light leading-relaxed">` with the EXACT framing copy from UI-SPEC §Copywriting Contract:

    `Your presence is the greatest gift. If you&apos;d like to celebrate with something more, here are a few places we&apos;ve registered.`

    Use `&apos;` for both apostrophes. NO eyebrow label (UI-SPEC explicitly forbids — "would over-formalize the warm-and-gracious tone"). NO exclamation marks (UI-SPEC §Copywriting Contract tone reminder). NO `<h1>` / `<h2>` — this is body prose only.

    Do NOT add the card grid section yet — Plan 02 will append it after this section.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source contains TWO `<section` tags inside `<main>` (hero + framing)
    - Source contains the exact string `Your presence is the greatest gift`
    - Source contains the exact string `here are a few places we&apos;ve registered`
    - Source contains `max-w-2xl mx-auto text-center reveal-on-scroll` exactly once
    - Framing block section uses `py-16 bg-background` (NOT `py-32`)
    - Framing block contains NO `font-label`, NO `text-primary` eyebrow, NO `<h1>`, NO `<h2>`, NO `<h3>`
    - Source contains NO `!` characters inside any string literal in the framing block (zero exclamation marks)
    - `npm run lint` exits 0
    - `npm run build` exits 0 (catches type errors, metadata shape errors, Next.js 16 page-prop violations)
  </acceptance_criteria>
  <done>Visiting `localhost:3000/registry` renders the hero followed by the framing block. Framing prose centers in a `max-w-2xl` container, fades in on scroll-into-view, contains zero exclamation marks, and matches UI-SPEC copy verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Plan 01 ships no outbound links — only the hero and framing block. The tabnabbing threat (T-02-01) applies to Plan 02's card anchors, not this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (reverse-tabnabbing, CWE-1022) | Card anchor tags in `/registry` | mitigate (in Plan 02) | Plan 02 task adds `target="_blank" rel="noopener noreferrer"` on every card `<a>`. Not in scope for this plan — flagged for traceability. |
| T-02-XSS | Tampering / XSS via React props | Hero copy + framing prose | accept | Copy is hard-coded string literals from UI-SPEC, no user input, no `dangerouslySetInnerHTML`. React's default text escaping covers ASVS L1 §5.3 output-encoding. |
</threat_model>

<verification>
- `npm run lint` exits 0 after each task
- `npm run build` exits 0 after Task 2 (production build catches Next.js 16 page-prop shape errors and metadata-export shape errors)
- Manual smoke: `npm run dev` then visit `http://localhost:3000/registry` → expect:
  - Hero with parallax background image, gradient scrim, eyebrow "For Our Guests", h1 "Our *Registries*" (italic warm-gold accent), subtitle line
  - Framing block: centered prose, max-w-2xl, fades in on scroll
  - Footer present below framing block (no card grid yet — that's Plan 02)
  - No console errors, no 404 on `/registry`
- DOM inspect: parallax-bg div and scrim div both carry `aria-hidden="true"`
- `app/(main)/things-to-do/page.tsx` is UNTOUCHED (UI-SPEC §Typography "Merges applied" only applies to Registry)
</verification>

<success_criteria>
- `app/(main)/registry/page.tsx` exists as a Server Component (no `'use client'`)
- `export const metadata` declared with title "Registry — Emily & Tyler"
- Hero section renders the three locked copy strings (eyebrow, h1 with italic accent, subtitle)
- Framing block renders the locked gracious-tone prose between hero and future grid location
- Lint + build pass clean
- File does NOT contain `pt-20`, `'use client'`, `next/image`, or any exclamation marks in the framing block
</success_criteria>

<output>
Create `.planning/phases/02-registry-page/02-01-SUMMARY.md` when done. Include:
- D-IDs satisfied (D-05, D-09, D-10, D-11)
- D-IDs deferred to later plans in this phase (D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-12, D-13)
- Hero placeholder image URL used (so Plan 03 / Tyler can swap)
- Confirmation: no exclamation marks introduced, no `'use client'` introduced, Things-To-Do not refactored
</output>
</content>
</invoke>