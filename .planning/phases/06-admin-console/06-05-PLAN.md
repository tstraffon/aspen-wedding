---
phase: 06-admin-console
plan: 05
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - .planning/phases/06-admin-console/RUNBOOK.md
autonomous: false
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, GUEST-01]

must_haves:
  truths:
    - "A runbook documents env setup, CSV import (test list -> real list), regroup-on-request, meal-count reading, and export"
    - "An end-to-end smoke proves lookup -> submit -> admin view -> export against real-list data"
    - "GUEST-01 CSV import is operationalized end-to-end (Studio import of .planning/guests-import.csv)"
  artifacts:
    - path: ".planning/phases/06-admin-console/RUNBOOK.md"
      provides: "Operator runbook + end-to-end smoke checklist (absorbs former Phase 7 handoff)"
      min_lines: 40
  key_links:
    - from: "RUNBOOK.md"
      to: ".planning/guests-import.csv"
      via: "Supabase Studio CSV import procedure"
      pattern: "guests-import.csv"
---

<objective>
Write the operator runbook and execute the end-to-end smoke that proves the whole admin console works against real-list data. Absorbs the former Phase 7 "Tyler-Facing Handoff": how to swap the test list for the real one, how to fix a wrong household on request, how to read the meal-count report at the catering deadline, and a full lookup -> submit -> admin view -> export pass.

Purpose: Operationalize GUEST-01's CSV import pathway end-to-end and provide the standing operational reference. Final verification that ADMIN-01..05 hold together.
Output: `.planning/phases/06-admin-console/RUNBOOK.md` + a passed end-to-end smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-console/06-CONTEXT.md
@.planning/phases/06-admin-console/06-RESEARCH.md
@.planning/phases/06-admin-console/06-VALIDATION.md
@.planning/ROADMAP.md

<interfaces>
Built in Plans 01-04 — the surfaces the runbook documents and the smoke exercises:
  Admin login: POST /api/admin/auth/login (passphrase ADMIN_ACCESS_CODE -> admin_session cookie); gate at proxy.ts /admin/* branch.
  Households view: /admin (grouped households, inline add/rename/remove/move).
  Guest mutations: POST /api/admin/guests, PATCH/DELETE /api/admin/guests/[id] (delete cascades rsvps).
  RSVP view: /admin/rsvps (grouped + meal-count summary keyed from MEAL_OPTIONS).
  Exports: GET /api/admin/export/guests, GET /api/admin/export/rsvps (text/csv).
Env vars (server-only): ADMIN_ACCESS_CODE, SUPABASE_SERVICE_ROLE_KEY (Plan 01).
Data: .planning/guests-import.csv (cleaned 138 guests / 75 households, format household_id,full_name) imported via Supabase Studio (GUEST-01).
Guest-facing flow (Phase 4/5, already shipped): /rsvp lookup -> form scaffold; submit endpoint /api/rsvp/submit. Note the guest group form is Phase 7 (not yet built) — the smoke can submit via the existing /api/rsvp/submit endpoint directly (curl) to populate rsvps for the admin-view + export legs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the operator runbook</name>
  <files>.planning/phases/06-admin-console/RUNBOOK.md</files>
  <read_first>
    - .planning/phases/06-admin-console/06-VALIDATION.md (Manual-Only Verifications + per-task map — the smoke source)
    - .planning/phases/06-admin-console/06-CONTEXT.md (D-07 env vars; D-12 export format; specifics: import round-trip)
    - .planning/STATE.md (2026-06-30 entries — guest list provenance, ambiguous-row resolutions)
    - .planning/ROADMAP.md (Phase 6 success criteria 1-6)
  </read_first>
  <action>
    Write `.planning/phases/06-admin-console/RUNBOOK.md` covering, as numbered operator procedures: (1) Env setup — set `ADMIN_ACCESS_CODE` and `SUPABASE_SERVICE_ROLE_KEY` in `.env.local` AND Vercel before deploy (D-07), where to get the service_role key, and the RLS-disabled prerequisite. (2) CSV import (GUEST-01) — import `.planning/guests-import.csv` (138 guests / 75 households) via Supabase Studio Table editor CSV import into `public.guests`, including how to swap the test list for the real list and re-import. (3) Validate groupings — log in at `/admin`, confirm the 75/138 counts, spot-check the known edge rows (Veeck Sr./Jr. split, the two "And Guest" plus-ones). (4) Fix a wrong household on request — use inline rename / move / split-merge in the households view; note that edits reflect immediately in guest name lookup. (5) Read the meal-count report at the catering deadline — open `/admin/rsvps`, read the summary card (counts by meal among attending; dietary-notes list); note labels come from `lib/rsvp/meal-options.ts` and must be set to the real menu there. (6) Export — download both CSVs from `/admin/rsvps`; guest-list CSV matches the import format for clean round-trip. (7) Troubleshooting — guest cookie does not grant admin; redirect-to-/admin/login means no admin_session; sanitized 5xx means check server logs. Append the end-to-end smoke checklist (from Task 2) as the final section.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-admin-console/RUNBOOK.md && grep -q "guests-import.csv" .planning/phases/06-admin-console/RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-admin-console/RUNBOOK.md` exists.
    - Contains sections for env setup, CSV import (referencing `.planning/guests-import.csv`), regroup-on-request, meal-count reading, export, and troubleshooting.
    - References both `ADMIN_ACCESS_CODE` and `SUPABASE_SERVICE_ROLE_KEY` and the `lib/rsvp/meal-options.ts` menu-swap step.
  </acceptance_criteria>
  <done>Standing operator runbook exists; GUEST-01 import pathway documented end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute end-to-end smoke</name>
  <read_first>
    - .planning/phases/06-admin-console/RUNBOOK.md (the procedures + smoke checklist written in Task 1)
    - .planning/phases/06-admin-console/06-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <what-built>The full admin console (Plans 01-04): admin gate, households view + guest CRUD/regroup, RSVP view + meal-count, two CSV exports — plus the runbook (Task 1).</what-built>
  <how-to-verify>
    Run `npm run build && npm run lint` (must be green), then in a fresh browser:
    1. GATE (ADMIN-01): visit `/admin` -> redirected to `/admin/login`. Log in with the guest `SITE_ACCESS_CODE` only -> still blocked. Log in with `ADMIN_ACCESS_CODE` -> reach `/admin`.
    2. IMPORT + HOUSEHOLDS (GUEST-01 / ADMIN-02): import `.planning/guests-import.csv` via Supabase Studio; reload `/admin` -> confirm 75 households / 138 members render.
    3. REGROUP (ADMIN-03): rename one test person; then look that new name up via `/rsvp` -> found (reflection). Move a person to another household; reload -> persisted. Delete a guest who has an RSVP (seed one first via step 4) -> confirm prompt fires and Supabase Studio shows BOTH the guests and rsvps rows gone.
    4. RSVP VIEW (ADMIN-04): with zero rsvps, open `/admin/rsvps` -> clean empty state, summary all-zero, no error. Seed 1-2 rsvps by POSTing to `/api/rsvp/submit` (the guest group form is Phase 7; use curl/REST with a real household_id + guest_id from the import). Reload `/admin/rsvps` -> grouped view + correct meal counts.
    5. EXPORT (ADMIN-05): download both CSVs from `/admin/rsvps`. Open the guest-list CSV -> `household_id,full_name`, re-importable. Open the rsvp CSV -> name/household/attending/meal_choice/dietary_restrictions columns. Confirm any field starting with `=`/`+`/`-`/`@` renders as text, not a formula.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run build && npm run lint` green.
    - All five legs pass (gate, import+households, regroup+reflection, rsvp view empty+populated, exports correct+injection-safe) — matching ROADMAP Phase 6 success criteria 1-6.
  </acceptance_criteria>
  <resume-signal>Type "smoke passed" or list which leg failed and the observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Supabase Studio import | Real guest PII enters the system via CSV |
| full guest->admin path | End-to-end exercise crosses every gate built in 01-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Repudiation / Operator error | CSV import + regroup procedures | mitigate | Runbook documents the exact import target table + format and the reflect-in-lookup behavior, reducing mis-import and wrong-household risk. |
| T-06-17 | Elevation of Privilege | end-to-end smoke | mitigate | Smoke explicitly re-tests that the guest `session` cookie does NOT grant `/admin` (ADMIN-01 leg 1). |
| T-06-18 | Tampering | export injection re-check | mitigate | Smoke leg 5 confirms formula-injection neutralization on real exported data. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `npm run build && npm run lint` green.
- RUNBOOK.md present with all required sections.
- End-to-end smoke passes all five legs (ROADMAP Phase 6 success criteria 1-6).
</verification>

<success_criteria>
Phase 6 success criterion 6 satisfied: a documented runbook + end-to-end smoke passes (swap-in list, full lookup -> submit, admin view, export). GUEST-01 import operationalized end-to-end.
</success_criteria>

<output>
Create `.planning/phases/06-admin-console/06-05-SUMMARY.md` when done.
</output>
