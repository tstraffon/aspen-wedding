---
phase: 06-admin-console
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/(admin)/admin/rsvps/page.tsx
  - app/api/admin/export/guests/route.ts
  - app/api/admin/export/rsvps/route.ts
autonomous: true
requirements: [ADMIN-04, ADMIN-05]

must_haves:
  truths:
    - "The /admin/rsvps page shows submissions grouped by household with a meal-count summary card"
    - "The meal-count summary is keyed from MEAL_OPTIONS (no drift from the form)"
    - "The RSVP view renders cleanly with zero submissions (empty state, no error)"
    - "Two CSV exports download with text/csv + Content-Disposition; every field is RFC-4180 quoted (formula-injection safe)"
  artifacts:
    - path: "app/(admin)/admin/rsvps/page.tsx"
      provides: "RSVP view grouped by household + meal-count summary + dietary list + empty state"
      contains: "MEAL_OPTIONS"
    - path: "app/api/admin/export/guests/route.ts"
      provides: "guest-list CSV (household_id,full_name) matching import format"
      exports: ["GET"]
    - path: "app/api/admin/export/rsvps/route.ts"
      provides: "RSVP CSV (name, household, attending, meal_choice, dietary_restrictions)"
      exports: ["GET"]
  key_links:
    - from: "app/(admin)/admin/rsvps/page.tsx"
      to: "lib/rsvp/meal-options.ts"
      via: "MEAL_OPTIONS import for summary keys"
      pattern: "MEAL_OPTIONS"
    - from: "app/api/admin/export/*/route.ts"
      to: "text/csv response"
      via: "new Response with Content-Disposition"
      pattern: "Content-Disposition"
---

<objective>
Build the reporting layer: an RSVP view that groups submissions by household with a meal-count summary card keyed from the single source of truth `MEAL_OPTIONS`, plus two CSV export Route Handlers (guest list + RSVPs) that stream `text/csv` with RFC-4180 quoting to neutralize spreadsheet formula injection.

Purpose: Satisfy ADMIN-04 (RSVP view + meal-count summary, renders clean with zero submissions — D-11) and ADMIN-05 (two CSV exports — D-12, guest-list format matches the import for clean round-trip). D-13: meal labels come only from `lib/rsvp/meal-options.ts`.
Output: `/admin/rsvps` view + `/api/admin/export/guests` + `/api/admin/export/rsvps`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-console/06-CONTEXT.md
@.planning/phases/06-admin-console/06-RESEARCH.md
@.planning/phases/06-admin-console/06-PATTERNS.md
@AGENTS.md

<project_constraint>
CUSTOM Next.js 16 (AGENTS.md) — read `node_modules/next/dist/docs/` before coding. The RSVP page is a Server Component reading `supabaseAdmin` directly (manual reload is the agreed refresh model — CONTEXT Claude's Discretion). CSV routes return `new Response(csv, { headers })` — the "Non-UI Responses" pattern from route.md, NOT `NextResponse.json`. Each `/api/admin/export/*` route re-verifies `admin_session` (PRIMARY authz — proxy.ts does NOT cover `/api/admin/*`).
</project_constraint>

<interfaces>
From Plan 06-01 — lib/supabase/admin.ts: export supabaseAdmin (service-role; direct SELECT on rsvps which anon cannot read).
From lib/rsvp/meal-options.ts (existing, single source of truth — D-13):
  export const MEAL_OPTIONS = ["Option A","Option B","Option C"] as const;  // placeholder labels
  export type MealChoice; export function isMealChoice(value): value is MealChoice.
Schema: rsvps { guest_id, household_id, attending (bool), meal_choice (text|null), dietary_restrictions (text|null) }; guests { id, household_id, full_name }. No FK rsvps->guests.
RFC-4180 field helper (RESEARCH Pattern 5): wrap every field in double-quotes, escape internal `"` as `""`; null/empty -> `""`. This neutralizes leading = + - @ formula injection.
Stitch tokens (app/globals.css): bg-background, bg-surface-container-lowest, text-on-surface, text-on-surface-variant, text-primary, text-error, border-white/10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RSVP view page + meal-count summary</name>
  <files>app/(admin)/admin/rsvps/page.tsx</files>
  <read_first>
    - .planning/phases/06-admin-console/06-PATTERNS.md (app/(admin)/admin/rsvps/page.tsx section — Server Component, meal-count from MEAL_OPTIONS, empty state)
    - lib/rsvp/meal-options.ts (MEAL_OPTIONS — the only meal-label source, D-13)
    - lib/supabase/admin.ts (supabaseAdmin)
    - .planning/phases/06-admin-console/06-CONTEXT.md (D-11 grouped by household + summary card + dietary list + clean empty state)
  </read_first>
  <action>
    Create `app/(admin)/admin/rsvps/page.tsx` as an async Server Component. Read rsvps via `supabaseAdmin.from("rsvps").select("guest_id, household_id, attending, meal_choice, dietary_restrictions").order("household_id")` and read `guests` (`id, household_id, full_name`) to resolve per-guest display names (join in-JS by guest_id; no DB FK). On error return a `text-error` div. Build the meal-count summary card at the top: initialize a counts object from `MEAL_OPTIONS` (D-13 — never hardcode meal labels), then increment per rsvp where `attending === true` and `meal_choice` is one of the options; also collect a dietary-notes list from attending members with a non-empty `dietary_restrictions`. Below the summary, render submissions grouped by household. Handle zero submissions cleanly (empty-state copy, summary shows all-zero counts, no error — D-11). Add a small toolbar with download links to `/api/admin/export/guests` and `/api/admin/export/rsvps` (plain anchor hrefs trigger the attachment download).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build + type check pass.
    - File does NOT contain `"use client"`; imports `MEAL_OPTIONS` from `@/lib/rsvp/meal-options` and uses it to key the summary (grep `MEAL_OPTIONS`).
    - Reads rsvps via `supabaseAdmin` (grep `supabaseAdmin` + `rsvps`).
    - Summary counts only `attending === true` rows; renders an explicit zero-submissions empty state.
    - Contains anchor hrefs to `/api/admin/export/guests` and `/api/admin/export/rsvps`.
  </acceptance_criteria>
  <done>RSVP view renders grouped submissions + a MEAL_OPTIONS-keyed meal-count summary, clean at zero submissions, with export links.</done>
</task>

<task type="auto">
  <name>Task 2: Guest-list CSV export route</name>
  <files>app/api/admin/export/guests/route.ts</files>
  <read_first>
    - .planning/phases/06-admin-console/06-PATTERNS.md (app/api/admin/export/guests/route.ts section — toRfc4180Field + new Response)
    - .planning/phases/06-admin-console/06-RESEARCH.md (Pattern 5 CSV route; Pitfall 7 formula injection)
    - lib/supabase/admin.ts (supabaseAdmin)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md (Non-UI Responses section)
  </read_first>
  <action>
    Create `app/api/admin/export/guests/route.ts` exporting `GET(request: NextRequest)`. Verify `request.cookies.get("admin_session")?.value` -> 401 if absent (PRIMARY authz). Define a `toRfc4180Field` helper that wraps every value in double-quotes and escapes internal `"` as `""` (null/empty -> `""`) — this neutralizes leading `=`/`+`/`-`/`@` formula injection (Pitfall 7). Read `guests` (`household_id, full_name`) via supabaseAdmin ordered by household_id then full_name. On error: console.error + sanitized 500. Emit a `household_id,full_name` header line followed by quoted rows (matching the import format so a re-import round-trips — CONTEXT specifics). Return `new Response(csv, { headers: { "Content-Type": "text/csv; charset=utf-8", "Content-Disposition": 'attachment; filename="guests.csv"' } })`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build + type check pass.
    - Exports `GET`; checks `admin_session` -> 401 when absent.
    - Returns `new Response(...)` (not NextResponse.json) with `Content-Type: text/csv` and a `Content-Disposition: attachment` filename (grep both header strings).
    - Header row is exactly `household_id,full_name`; every data field passes through the quote-wrapping helper.
  </acceptance_criteria>
  <done>Guest-list CSV downloads in import-compatible, formula-injection-safe form, admin-gated.</done>
</task>

<task type="auto">
  <name>Task 3: RSVP CSV export route</name>
  <files>app/api/admin/export/rsvps/route.ts</files>
  <read_first>
    - app/api/admin/export/guests/route.ts (sibling — same helper + Response shape, created in Task 2)
    - .planning/phases/06-admin-console/06-PATTERNS.md (app/api/admin/export/rsvps/route.ts section)
    - lib/supabase/admin.ts (supabaseAdmin)
  </read_first>
  <action>
    Create `app/api/admin/export/rsvps/route.ts` exporting `GET(request: NextRequest)`, mirroring the guest export: `admin_session` 401 check, the same `toRfc4180Field` quote-wrapping helper, sanitized 5xx, and `new Response(csv, { headers })` with `text/csv` + `Content-Disposition: attachment; filename="rsvps.csv"`. Read rsvps (`guest_id, household_id, attending, meal_choice, dietary_restrictions`) and guests (`id, full_name`) via supabaseAdmin; resolve the guest name by joining on guest_id in-JS. Columns per D-12: `name, household_id, attending, meal_choice, dietary_restrictions`. Every field quote-wrapped.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint pass.
    - Exports `GET`; checks `admin_session` -> 401 when absent.
    - Header row contains `name`, `household_id`, `attending`, `meal_choice`, `dietary_restrictions`.
    - Returns `text/csv` with `Content-Disposition: attachment` filename; all fields quote-wrapped via the helper.
    - Manual smoke (ADMIN-05, RESEARCH test map): download both CSVs; guest-list opens as `household_id,full_name`; a field starting with `=`/`+`/`-`/`@` is rendered as text (quoted), not a formula.
  </acceptance_criteria>
  <done>RSVP CSV downloads with correct columns, admin-gated and injection-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/admin/export/* | Untrusted GET; proxy.ts does NOT cover /api/admin/* |
| exported CSV -> spreadsheet app | Field values may be interpreted as formulas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Tampering | CSV export fields | mitigate | RFC-4180 `toRfc4180Field` wraps every field in double-quotes (escaping internal quotes), neutralizing `=`/`+`/`-`/`@` formula injection in Excel/Sheets (Pitfall 7). |
| T-06-13 | Elevation of Privilege | /api/admin/export/*, /admin/rsvps | mitigate | Export routes re-verify `admin_session` (PRIMARY authz); the page sits behind the proxy admin branch. rsvps (anon cannot SELECT) read only via service-role server-side. |
| T-06-14 | Information Disclosure | DB error responses | mitigate | Sanitized 5xx vocabulary; no PostgREST fragments in responses (Phase 4 D-13). |
| T-06-15 | Tampering | meal-count summary | mitigate | Counts keyed from `MEAL_OPTIONS` (D-13) and only over `attending === true`; orphan-resistant since Plan 02 cascades deletes. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `npm run build && npm run lint` green.
- Source: MEAL_OPTIONS-keyed summary; service-role reads server-side only; both exports text/csv + Content-Disposition + quote-wrapping; admin_session 401 on each export route.
- Manual ADMIN-04/05 smoke per RESEARCH test map (empty state, populated counts, CSV contents + injection guard).
</verification>

<success_criteria>
ADMIN-04 + ADMIN-05 satisfied: RSVP view with correct meal-count summary that renders clean at zero submissions; two CSV exports with correct, import-compatible, formula-injection-safe contents.
</success_criteria>

<output>
Create `.planning/phases/06-admin-console/06-04-SUMMARY.md` when done.
</output>
