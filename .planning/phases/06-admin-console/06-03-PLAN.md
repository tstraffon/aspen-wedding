---
phase: 06-admin-console
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - app/(admin)/admin/page.tsx
  - app/(admin)/admin/HouseholdsTable.tsx
autonomous: true
requirements: [ADMIN-02, ADMIN-03]

must_haves:
  truths:
    - "The /admin page lists every household with its members, grouped by household_id"
    - "The view renders cleanly with zero guests (empty state, no error)"
    - "Inline rename, add, remove, and move persist via the Plan 02 routes and survive reload"
    - "Removing a guest who has an RSVP prompts a confirmation before deleting"
  artifacts:
    - path: "app/(admin)/admin/page.tsx"
      provides: "Server Component: service-role read of all guests, grouped, passed to island"
      min_lines: 15
    - path: "app/(admin)/admin/HouseholdsTable.tsx"
      provides: "Client island: inline edit + regroup + confirm-delete"
      contains: "use client"
  key_links:
    - from: "app/(admin)/admin/page.tsx"
      to: "supabaseAdmin"
      via: "service-role select from guests"
      pattern: "supabaseAdmin.*guests"
    - from: "app/(admin)/admin/HouseholdsTable.tsx"
      to: "/api/admin/guests"
      via: "fetch POST/PATCH/DELETE"
      pattern: "fetch.*api/admin/guests"
---

<objective>
Build the households management view: a Server Component page that reads every guest via the service-role client and groups them by `household_id`, plus a client island that renders the grouped list and provides inline rename, add-person, remove-person (with confirmation), and move-person (split/merge) by calling the Plan 02 routes.

Purpose: Satisfy ADMIN-02 (view all households + members to validate the imported 75 households / 138 guests) and the UI half of ADMIN-03 (inline edit + regroup per D-08, confirm-on-delete per D-09, UUIDs client-generated per D-10).
Output: `/admin` households view with full inline editing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-console/06-CONTEXT.md
@.planning/phases/06-admin-console/06-RESEARCH.md
@.planning/phases/06-admin-console/06-PATTERNS.md
@AGENTS.md

<project_constraint>
CUSTOM Next.js 16 (AGENTS.md) — read `node_modules/next/dist/docs/` before coding. The page is a Server Component (reads `supabaseAdmin` directly, server-side); the editing UI is the single Client Component island. CRITICAL: `lib/supabase/admin.ts` (service-role) must NEVER be imported in the `"use client"` island — only the Server Component reads it (PATTERNS Critical Finding 5). Plain `<img>` + eslint-disable for any imagery (site convention).
</project_constraint>

<interfaces>
From Plan 06-01 — lib/supabase/admin.ts: export supabaseAdmin (service-role).
From Plan 06-02 — guest mutation API contracts the island calls:
  POST /api/admin/guests        body { household_id, full_name }            -> { success, guest }
  PATCH /api/admin/guests/[id]  body { full_name? , household_id? }          -> { success }
  DELETE /api/admin/guests/[id]                                             -> { success } (cascades rsvps)
guests row shape: { id: uuid, household_id: uuid, full_name: string }.
New household_id for a split: crypto.randomUUID() generated client-side (D-10).
fetch error convention (mirror app/(main)/rsvp/page.tsx): check res.status >= 500 first, then !res.ok, then parse JSON.
Stitch tokens (app/globals.css): bg-background, bg-surface-container-lowest, bg-surface-container-low, text-on-surface, text-on-surface-variant, text-primary, text-error, border-white/10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Households Server Component page</name>
  <files>app/(admin)/admin/page.tsx</files>
  <read_first>
    - .planning/phases/06-admin-console/06-PATTERNS.md (app/(admin)/admin/page.tsx section — Server Component reads supabaseAdmin, groups, passes to island)
    - lib/supabase/admin.ts (supabaseAdmin)
    - app/(main)/rsvp/page.tsx (Server-Component-wraps-client-island structure reference)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route-groups.md
  </read_first>
  <action>
    Create `app/(admin)/admin/page.tsx` as an async Server Component (NO `"use client"`). Read all guests via `supabaseAdmin.from("guests").select("id, household_id, full_name").order("household_id").order("full_name")`. On error, return a simple error UI div using `text-error` (Server Components have no default error boundary — render, do not throw). Group the flat guest rows by `household_id` into an array of `{ household_id, members[] }` (in-JS grouping per RESEARCH Open Question 3). Render `HouseholdsTable` (the Plan-03 island) with the grouped data and the flat guests as props. Show a household/member count header so the couple can eyeball the expected 75 households / 138 guests. Handle zero guests gracefully (empty-state copy, no crash).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build + type check pass.
    - File does NOT contain `"use client"`.
    - Contains `supabaseAdmin` select from `guests` (grep both).
    - Renders an empty-state branch for zero guests and an error branch using `text-error`.
    - Imports and renders `HouseholdsTable`.
  </acceptance_criteria>
  <done>/admin server-renders all households grouped by household_id with counts; empty + error states handled.</done>
</task>

<task type="auto">
  <name>Task 2: HouseholdsTable client island — inline edit + regroup</name>
  <files>app/(admin)/admin/HouseholdsTable.tsx</files>
  <read_first>
    - .planning/phases/06-admin-console/06-PATTERNS.md (HouseholdsTable client island notes; Shared Patterns: fetch() to Route Handler; Stitch token table)
    - app/(main)/rsvp/page.tsx (useState + handleSubmit fetch pattern, error-state ordering, button disabled pattern)
    - .planning/phases/06-admin-console/06-CONTEXT.md (D-08 inline edit + household picker; D-09 confirm-before-delete; D-10 client-generated UUIDs)
  </read_first>
  <action>
    Create `app/(admin)/admin/HouseholdsTable.tsx` beginning with `"use client"`. Receive grouped households + flat guests as props; hold them in local state so mutations update the view without a full reload (a router refresh after a successful call is also acceptable). Provide, per member row: inline RENAME (editable name -> PATCH `/api/admin/guests/[id]` with `{ full_name }`); REMOVE (DELETE `/api/admin/guests/[id]`) gated behind a confirmation prompt per D-09 (the confirm copy notes the guest's RSVP row is deleted too); and MOVE via a household picker (PATCH with `{ household_id }`) — choosing an existing household merges, choosing "new household" generates `crypto.randomUUID()` client-side and assigns it (split, per D-10). Provide an ADD-person control per household (POST `/api/admin/guests` with that `household_id` + entered `full_name`). Use the rsvp page's fetch error ordering (>=500 server error, then !res.ok, then JSON) and a shared error/disabled-while-submitting pattern. Do NOT import `lib/supabase/admin.ts` here — all data changes go through the Plan 02 fetch endpoints. Use Stitch token classes for styling.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint pass.
    - File starts with `"use client"` and does NOT import `@/lib/supabase/admin`.
    - Contains fetch calls to `/api/admin/guests` (POST) and `/api/admin/guests/` + id (PATCH and DELETE) — grep `fetch("/api/admin/guests`.
    - Delete path is guarded by a confirmation step (grep: `confirm` or an explicit confirm-state branch).
    - Split path generates a UUID client-side (grep: `crypto.randomUUID`).
    - Manual smoke (ADMIN-02/03, RESEARCH test map): load /admin -> households render; rename a person and look the new name up via the guest RSVP form -> found; move a person -> household membership updates; delete a guest with an RSVP -> confirm Supabase Studio shows both rows gone.
  </acceptance_criteria>
  <done>Couple can validate groupings and add/rename/remove/move people; changes persist and reflect in name lookup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component -> guests | Service-role read renders to admin-gated page only |
| client island -> /api/admin/guests* | Mutations cross to admin-gated API (re-verified server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | HouseholdsTable client island | mitigate | Island never imports `lib/supabase/admin.ts`; it only calls fetch endpoints. Service-role key stays server-side (build assertion: no `@/lib/supabase/admin` import in the `"use client"` file). |
| T-06-10 | Tampering / Data loss | inline delete | mitigate | Confirmation prompt before DELETE (D-09); server route cascades the rsvps row. Prevents accidental destructive ops + orphans. |
| T-06-11 | Elevation of Privilege | /admin page route | mitigate | Page sits behind the proxy.ts admin branch (Plan 01); the mutation routes re-verify admin_session (Plan 02). |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase. |
</threat_model>

<verification>
- `npm run build && npm run lint` green.
- Source: page is a Server Component reading supabaseAdmin; island is `"use client"` with no admin client import; confirm-before-delete; crypto.randomUUID for splits.
- Manual ADMIN-02/03 smoke per RESEARCH test map.
</verification>

<success_criteria>
ADMIN-02 + UI half of ADMIN-03 satisfied: households visible and validatable; inline add/rename/remove/move persist and reflect in name lookup; deletes confirmed and cascade.
</success_criteria>

<output>
Create `.planning/phases/06-admin-console/06-03-SUMMARY.md` when done.
</output>
