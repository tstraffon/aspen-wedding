---
phase: 06-admin-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - lib/supabase/admin.ts
  - app/api/admin/auth/login/route.ts
  - proxy.ts
  - app/(admin)/admin/layout.tsx
  - app/(admin)/admin/login/page.tsx
autonomous: false
requirements: [ADMIN-01]
user_setup:
  - service: supabase
    why: "Admin server routes use the Supabase service-role key to bypass RLS and read rsvps / write guests (D-05)."
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role secret (NOT the anon key). Server-only, no NEXT_PUBLIC_ prefix."
      - name: ADMIN_ACCESS_CODE
        source: "Choose a shared passphrase for Tyler + Emily; set in .env.local and Vercel (D-01, D-04)."
    dashboard_config:
      - task: "Confirm Row Level Security is DISABLED on public.guests and public.rsvps (service-role bypass relies on this — see RESEARCH open question A2)."
        location: "Supabase Dashboard -> Authentication -> Policies (or Table editor -> RLS toggle)"

must_haves:
  truths:
    - "Visiting /admin without an admin_session cookie redirects to /admin/login"
    - "A valid guest session cookie does NOT grant access to /admin (separate cookie)"
    - "Submitting the correct ADMIN_ACCESS_CODE sets admin_session and lands on /admin"
    - "An incorrect access code returns 401 and sets no cookie"
  artifacts:
    - path: "proxy.ts"
      provides: "Admin gate branch checking admin_session + matcher excluding admin/login and api/admin/auth"
      contains: "admin_session"
    - path: "app/api/admin/auth/login/route.ts"
      provides: "Admin passphrase -> admin_session cookie issuer"
      exports: ["POST"]
    - path: "lib/supabase/admin.ts"
      provides: "Server-only service-role Supabase client"
      contains: "supabaseAdmin"
    - path: "app/(admin)/admin/layout.tsx"
      provides: "Minimal admin chrome (no guest Navbar/Footer) with cross-view nav"
    - path: "app/(admin)/admin/login/page.tsx"
      provides: "Admin login form client island"
  key_links:
    - from: "app/(admin)/admin/login/page.tsx"
      to: "/api/admin/auth/login"
      via: "fetch POST"
      pattern: "fetch.*api/admin/auth/login"
    - from: "proxy.ts"
      to: "admin_session cookie"
      via: "request.cookies.get on /admin/* paths"
      pattern: "admin_session"
---

<objective>
Build the admin authentication gate and the privileged data path that every other Phase 6 plan depends on: a separate `admin_session` cookie distinct from the guest `session` cookie, a `proxy.ts` branch that protects `/admin/*`, a server-only service-role Supabase client, and the admin login entry (layout + page + route).

Purpose: Satisfy ADMIN-01 — a private admin area gated separately from the public guest `SITE_ACCESS_CODE`, where the guest code grants NO admin access. Establishes the service-role read/write path (D-05..D-07) the households, RSVP, and export plans consume.
Output: Working admin login -> cookie -> `/admin` gate, plus `lib/supabase/admin.ts` for downstream plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-console/06-CONTEXT.md
@.planning/phases/06-admin-console/06-RESEARCH.md
@.planning/phases/06-admin-console/06-PATTERNS.md
@AGENTS.md

<project_constraint>
This is a CUSTOM build of Next.js 16 (AGENTS.md). Before writing code, read the relevant guide under `node_modules/next/dist/docs/`. Do NOT assume training-data Next.js APIs. Middleware here is root `proxy.ts` exporting `proxy()` + `config.matcher` (NOT middleware.ts). `cookies()` from `next/headers` is ASYNC. For proxy and Route Handlers, prefer the synchronous `request.cookies.get()` on `NextRequest`.
</project_constraint>

<interfaces>
Existing guest login route to mirror — app/api/auth/login/route.ts:
  POST(request: Request): reads { password }, compares process.env.SITE_ACCESS_CODE, sets cookie "session"="authenticated" with { httpOnly, secure: NODE_ENV==="production", sameSite:"lax", path:"/", maxAge: 60*60*24*90 }, returns NextResponse.json.

Existing anon client to mirror (do NOT reuse) — lib/supabase/client.ts:
  createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!) exported as `supabase`.

Existing proxy to extend — proxy.ts:
  proxy(request) checks `session` cookie, redirects to /login if absent; matcher excludes login|api/auth|_next/static|_next/image|favicon|images.

Root app/layout.tsx already provides <html>/<body>; the (admin) layout is a NESTED layout and must NOT redeclare them.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision admin env vars + confirm RLS disabled</name>
  <read_first>
    - .planning/phases/06-admin-console/06-RESEARCH.md (Environment Availability; Open Question 1 RLS status; Assumption A2)
    - .planning/phases/06-admin-console/06-CONTEXT.md (D-05, D-07)
  </read_first>
  <what-built>Nothing yet — this is the manual prerequisite the rest of the plan and phase depend on.</what-built>
  <how-to-verify>
    1. Add `ADMIN_ACCESS_CODE=<chosen shared passphrase>` to `.env.local` (server-only, no NEXT_PUBLIC_ prefix).
    2. Retrieve the service_role secret from Supabase Dashboard -> Project Settings -> API -> `service_role` (NOT anon). Add `SUPABASE_SERVICE_ROLE_KEY=<that key>` to `.env.local`.
    3. In Supabase Dashboard, confirm Row Level Security is DISABLED on `public.guests` and `public.rsvps` (service-role bypass depends on this — RESEARCH A2). If RLS is enabled with restrictive policies, note it in the resume signal.
    4. Restart the dev server so the new env vars load.
  </how-to-verify>
  <acceptance_criteria>
    - `.env.local` contains `ADMIN_ACCESS_CODE` and `SUPABASE_SERVICE_ROLE_KEY`, neither prefixed `NEXT_PUBLIC_`.
    - RLS status on `guests` and `rsvps` confirmed (disabled, or noted otherwise).
  </acceptance_criteria>
  <resume-signal>Type "env set, RLS disabled" or describe the RLS state found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Service-role client + admin login route</name>
  <files>lib/supabase/admin.ts, app/api/admin/auth/login/route.ts</files>
  <read_first>
    - lib/supabase/client.ts (the anon analog — same createClient call, different key/export)
    - app/api/auth/login/route.ts (the guest login analog to clone)
    - .planning/phases/06-admin-console/06-PATTERNS.md (lib/supabase/admin.ts and app/api/admin/auth/login/route.ts sections)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md (Cookies section)
  </read_first>
  <action>
    Create `lib/supabase/admin.ts`: call `createClient` with `process.env.NEXT_PUBLIC_SUPABASE_URL!` and `process.env.SUPABASE_SERVICE_ROLE_KEY!`, export as `supabaseAdmin`. Add a header comment that this module is server-only and must NEVER be imported in a Client Component (the `server-only` package is not installed; convention enforces the boundary). Per D-05 do NOT reuse or re-export the anon `supabase` client.
    Create `app/api/admin/auth/login/route.ts` by cloning `app/api/auth/login/route.ts` with exactly two changes: read `process.env.ADMIN_ACCESS_CODE` instead of `SITE_ACCESS_CODE`, and set the cookie named `admin_session` instead of `session`. Keep the identical cookie flags (httpOnly, secure in production, sameSite "lax", path "/", maxAge 60*60*24*90) per D-04, the same 500-if-unconfigured and 401-on-mismatch responses, and the same `{ success: true }` success body.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build + type check pass.
    - `lib/supabase/admin.ts` exports `supabaseAdmin` and references `SUPABASE_SERVICE_ROLE_KEY` (grep: `SUPABASE_SERVICE_ROLE_KEY`), with NO `NEXT_PUBLIC_` on that var.
    - `app/api/admin/auth/login/route.ts` exports `POST`, references `process.env.ADMIN_ACCESS_CODE`, and sets a cookie named `admin_session` (grep both strings present).
    - The string `session` cookie name is NOT set by the admin route (only `admin_session`).
  </acceptance_criteria>
  <done>Admin credential issuer and service-role client exist; guest login route is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: proxy.ts admin branch + admin layout + login page</name>
  <files>proxy.ts, app/(admin)/admin/layout.tsx, app/(admin)/admin/login/page.tsx</files>
  <read_first>
    - proxy.ts (extend, do NOT rewrite the guest gate)
    - .planning/phases/06-admin-console/06-PATTERNS.md (proxy.ts MODIFY section; admin layout section; admin login page section)
    - app/(main)/layout.tsx (layout analog — admin layout OMITS Navbar/Footer/MusicButton)
    - app/(main)/rsvp/page.tsx (client island fetch + useState + button pattern to mirror for the login form)
    - app/globals.css (Stitch token class names: bg-background, text-on-surface, text-primary, text-error, border-white/10)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md (Conditional Statements, Using Cookies, matcher negative lookahead)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route-groups.md
  </read_first>
  <action>
    Extend `proxy.ts`: read `pathname` from `request.nextUrl`. Add an EARLY-return admin branch before the existing guest-gate block — when `pathname.startsWith("/admin")`, read `request.cookies.get("admin_session")?.value` (synchronous, no await); if absent redirect to a `/admin/login` URL, else `NextResponse.next()`. Leave the existing guest `session` block unchanged below it (D-03). Update `config.matcher` to add `admin/login` and `api/admin/auth` to the existing negative-lookahead exclusion list so the admin login page and admin auth API are reachable without a cookie (RESEARCH Pitfalls 2 and 3).
    Create `app/(admin)/admin/layout.tsx`: a minimal NESTED layout (root app/layout.tsx already supplies html/body — do NOT redeclare them). Wrap children in a `bg-background text-on-surface min-h-screen` container. Include a small admin nav with links to `/admin` (Households) and `/admin/rsvps` (RSVPs) and a logout affordance; omit Navbar, Footer, and MusicButton (internal tool, function over polish).
    Create `app/(admin)/admin/login/page.tsx`: a `"use client"` form island mirroring the rsvp page's fetch pattern — a single password input, submit button with disabled/`isSubmitting` state, error banner. On submit POST `/api/admin/auth/login` with `{ password }` and Content-Type application/json; on `res.status >= 500` show a server-error message, on `!res.ok` show "Incorrect access code", on success set `window.location.href = "/admin"`. Use Stitch token classes for styling. Plain `<img>` + eslint-disable for any imagery (site convention).
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Build + lint pass.
    - `proxy.ts` contains an admin branch referencing `admin_session` and a redirect to `/admin/login`; the guest `session` block is still present (grep both `admin_session` and `session`).
    - `proxy.ts` matcher string contains both `admin/login` and `api/admin/auth`.
    - `app/(admin)/admin/layout.tsx` does NOT import Navbar/Footer/MusicButton and does NOT render `<html>` or `<body>`.
    - `app/(admin)/admin/login/page.tsx` begins with `"use client"` and contains `fetch("/api/admin/auth/login"`.
    - Manual smoke (RESEARCH test map ADMIN-01): in a fresh browser, visiting `/admin` redirects to `/admin/login`; logging in with only the guest code does NOT reach `/admin`; logging in with `ADMIN_ACCESS_CODE` reaches `/admin`.
  </acceptance_criteria>
  <done>Admin gate enforced at the proxy layer; admin can authenticate and reach /admin; guests cannot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> proxy.ts | Unauthenticated visitor crosses the page-route gate here |
| browser -> /api/admin/auth/login | Untrusted passphrase submission |
| server module -> client bundle | Service-role key must never cross into client JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | proxy.ts admin branch | mitigate | Admin branch checks ONLY `admin_session`; guest `session` cookie can never satisfy it (D-02). Distinct cookie names. |
| T-06-02 | Spoofing | /api/admin/auth/login | mitigate | Constant passphrase compare against `ADMIN_ACCESS_CODE`; 401 on mismatch, no cookie set. Low surface (two-person tool); no rate limiting at this scale (accepted). |
| T-06-03 | Information Disclosure | lib/supabase/admin.ts | mitigate | `SUPABASE_SERVICE_ROLE_KEY` has no `NEXT_PUBLIC_` prefix (excluded from client bundle); module documented server-only and imported only in Route Handlers / Server Components. Build assertion confirms no client import. |
| T-06-04 | Spoofing | proxy matcher | mitigate | `/admin/login` and `/api/admin/auth` excluded from matcher so the login path is reachable, but every OTHER `/admin/*` page requires `admin_session`. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit: none). No install task; no [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `npm run build && npm run lint` green.
- proxy.ts: admin branch + matcher exclusions present; guest gate intact.
- Manual ADMIN-01 smoke: /admin gate blocks non-admins, guest cookie ≠ admin, admin code grants access.
</verification>

<success_criteria>
ADMIN-01 satisfied: admin area gated separately; guest `SITE_ACCESS_CODE`/`session` does not grant admin access; service-role client available server-side for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/06-admin-console/06-01-SUMMARY.md` when done.
</output>
