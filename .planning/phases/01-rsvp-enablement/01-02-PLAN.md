---
phase: 01-rsvp-enablement
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/(main)/rsvp/page.tsx
autonomous: true
requirements:
  - "Polish submission UX per UI-SPEC: per-field validation, focus management, accept/decline success variants, error-type variants, decorative icon aria-hidden, label htmlFor/id pairing, conditional reveal announcement"

must_haves:
  truths:
    - "Submitting an empty form shows three inline errors (Name, Email, Attending) with custom warm copy, no native browser popup"
    - "Submitting with invalid email shows 'That email doesn't look right' inline below the email field"
    - "On accept success, body copy reads 'We can't wait to celebrate with you in Aspen...'"
    - "On decline success, body copy reads 'We'll miss you in Aspen, but thank you for letting us know...'"
    - "On network failure, error banner heading reads 'We couldn't send your RSVP' and focus lands on the heading"
    - "On 5xx response, error banner heading reads 'Something went wrong on our end'"
    - "On 4xx response, error banner heading reads 'One of your answers needs a tweak'"
    - "Every <label> has htmlFor pointing to an existing input id"
    - "Decorative icons (favorite, error, east, format_quote) have aria-hidden='true'"
    - "Conditional Guest Details reveal is wrapped in aria-live='polite'"
    - "Focus moves to success <h1> on success transition; focus moves to error banner heading on error transition"
  artifacts:
    - path: "app/(main)/rsvp/page.tsx"
      provides: "Polished client form per UI-SPEC"
      contains: "noValidate, errorKind, validate(), errorBannerRef, successHeadingRef, useEffect on [status]"
  key_links:
    - from: "<form>"
      to: "validate()"
      via: "handleSubmit calls validate() before fetch"
      pattern: "noValidate"
    - from: "useEffect [status]"
      to: "errorBannerRef.current, successHeadingRef.current"
      via: ".focus()"
      pattern: "successHeadingRef.current\\?\\.focus|errorBannerRef.current\\?\\.focus"
    - from: "errorKind state"
      to: "errorCopy variant"
      via: "object lookup"
      pattern: "errorCopy\\[errorKind"
---

<objective>
Polish `app/(main)/rsvp/page.tsx` to satisfy every line item in UI-SPEC §Acceptance Criteria. This is one large vertical slice of work on a single file — eight micro-changes that share the same JSX baseline. Preserve every shipped class string except where adding accessibility attributes; add per-field validation, focus management, success/error variant copy, decorative icon hiding, and label pairing.

Purpose: The form is the only feature in this phase. UI-SPEC is approved and binding. Without these changes, screen readers cannot navigate the form correctly, error states are ambiguous, and accept-vs-decline guests receive identical copy.

Output: A polished `rsvp/page.tsx` ready for the nav link to be enabled in Plan 03 and the smoke checklist to pass in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rsvp-enablement/01-RESEARCH.md
@.planning/phases/01-rsvp-enablement/01-PATTERNS.md
@.planning/phases/01-rsvp-enablement/01-UI-SPEC.md

@CLAUDE.md
@AGENTS.md

# Code being modified
@app/(main)/rsvp/page.tsx

# Canonical client-form analog per PATTERNS.md
@app/login/page.tsx

<interfaces>
<!-- Existing FormState type (lines 5-12) — DO NOT change shape -->
```ts
type FormState = {
  fullName: string;
  email: string;
  attending: "accept" | "decline" | null;
  guestCount: string;
  dietaryRestrictions: string;
  note: string;
};
```

<!-- New types to add -->
```ts
type FormErrors = Partial<Record<"fullName" | "email" | "attending", string>>;
type ErrorKind = "network" | "server" | "validation";
```

<!-- Existing status state (lines 23-25) — DO NOT change -->
```ts
const [status, setStatus] = useState<"idle" | "submitting" | "success" | "error">("idle");
```

<!-- Existing label/input lines that need htmlFor/id pairs (from PATTERNS.md) -->
<!-- Use these exact kebab-case IDs: -->
<!-- rsvp-full-name (line 99 label / line 102 input) -->
<!-- rsvp-email (line 114 label / line 117 input) -->
<!-- rsvp-attending-accept (line 137 input — radio) -->
<!-- rsvp-attending-decline (line 152 input — radio) -->
<!-- rsvp-guest-count (line 172 label / line 175 select) -->
<!-- rsvp-dietary-restrictions (line 189 label / line 192 input) -->
<!-- rsvp-note (line 211 label / line 214 textarea) -->
<!-- Group label for attending (line 132) — htmlFor="rsvp-attending" pointing at the first radio is acceptable, OR omit htmlFor and use aria-labelledby on the fieldset -->

<!-- Error-variant copy contract (from UI-SPEC §Error State) -->
```tsx
const errorCopy = {
  network: {
    heading: "We couldn't send your RSVP",
    body: <>Check your connection and try again. Still stuck? Email us at <a href="mailto:hello@emilyandtyler.com" className="underline underline-offset-2">hello@emilyandtyler.com</a>.</>,
  },
  server: {
    heading: "Something went wrong on our end",
    body: <>Try again in a minute. If it keeps happening, email us at <a href="mailto:hello@emilyandtyler.com" className="underline underline-offset-2">hello@emilyandtyler.com</a> and we'll add you manually.</>,
  },
  validation: {
    heading: "One of your answers needs a tweak",
    body: "Scroll up and check the highlighted field, then try again.",
  },
}[errorKind ?? "network"];
```

<!-- Success-variant copy (from UI-SPEC §Success State) -->
```tsx
const successBody = form.attending === "decline"
  ? "We'll miss you in Aspen, but thank you for letting us know. We're holding good thoughts for you."
  : "We can't wait to celebrate with you in Aspen. We'll send venue and timing details closer to the wedding.";
```

<!-- Validation messages (from UI-SPEC §Validation Messaging) -->
<!-- fullName empty: "We need your name to find your invitation." -->
<!-- email empty: "Where should we reach you with details?" -->
<!-- email invalid format: "That email doesn't look right — double-check the spelling." -->
<!-- attending null: "Let us know if you can make it." -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add validation, error-kind state, refs, focus effect, label pairing, aria-hidden icons, conditional-reveal aria-live</name>
  <files>app/(main)/rsvp/page.tsx</files>
  <read_first>
    - app/(main)/rsvp/page.tsx (current state — entire file)
    - app/login/page.tsx (canonical client-form analog: try/catch + finally + error banner + htmlFor pattern)
    - .planning/phases/01-rsvp-enablement/01-UI-SPEC.md (§Validation Messaging, §Success State, §Error State, §Accessibility Contract, §Interaction Patterns, §Button States — ALL six are binding)
    - .planning/phases/01-rsvp-enablement/01-PATTERNS.md (§app/(main)/rsvp/page.tsx pattern assignments — every code example mirrors login analog or UI-SPEC verbatim)
    - .planning/phases/01-rsvp-enablement/01-RESEARCH.md (§Validation Architecture, §Code Examples, §Risks & Landmines §Accessibility / iOS landmines)
    - CLAUDE.md + AGENTS.md (Next.js 16 — client components and useState/useEffect/useRef are unchanged in v16, but verify by skimming node_modules/next/dist/docs/01-app/02-guides/forms.md before starting)
  </read_first>
  <action>
    This is a single coordinated edit to one file. Make the eight changes below in one pass — they all touch the same JSX and share state. Preserve every Tailwind class string verbatim except where explicitly noted.

    Change 1 (imports, line 3): Add `useEffect, useRef` to the existing `useState` import. Final: `import { useEffect, useRef, useState } from "react";`

    Change 2 (state declarations, after line 25): Add three new state declarations:
    - `const [errors, setErrors] = useState<FormErrors>({});`
    - `const [errorKind, setErrorKind] = useState<ErrorKind | null>(null);`
    - `const errorBannerRef = useRef<HTMLParagraphElement>(null);`
    - `const successHeadingRef = useRef<HTMLHeadingElement>(null);`
    Add the type aliases `FormErrors` and `ErrorKind` at module scope above the component (after FormState at line 12).

    Change 3 (validate function): Add a `validate()` function inside the component returning `FormErrors`. Implements the four checks from UI-SPEC §Validation Messaging:
    - `!form.fullName.trim()` -> "We need your name to find your invitation."
    - `!form.email.trim()` -> "Where should we reach you with details?"
    - `form.email.trim() && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email)` -> "That email doesn't look right — double-check the spelling."
    - `!form.attending` -> "Let us know if you can make it."
    Returns the populated FormErrors object.

    Change 4 (handleSubmit rewrite, lines 27-41): Replace with:
    - `e.preventDefault();`
    - `setErrorKind(null);`
    - `const validationErrors = validate(); if (Object.keys(validationErrors).length > 0) { setErrors(validationErrors); /* focus first invalid field by id — see Change 4a */ return; }`
    - `setErrors({});`
    - `setStatus("submitting");`
    - try block: fetch as today; if (!res.ok) { setErrorKind(res.status >= 500 ? "server" : "validation"); throw new Error("Submission failed"); } setStatus("success");
    - catch block: setErrorKind((prev) => prev ?? "network"); setStatus("error");

    Change 4a (focus first invalid field): After `setErrors(validationErrors)` in the early-return branch, queue a microtask to focus the first invalid field:
    `queueMicrotask(() => { const firstInvalid = (Object.keys(validationErrors) as Array<keyof FormErrors>)[0]; const fieldId = firstInvalid === "attending" ? "rsvp-attending-accept" : firstInvalid === "fullName" ? "rsvp-full-name" : "rsvp-email"; document.getElementById(fieldId)?.focus(); });`

    Change 5 (focus useEffect, after handleSubmit): Add
    `useEffect(() => { if (status === "success") successHeadingRef.current?.focus(); if (status === "error") errorBannerRef.current?.focus(); }, [status]);`

    Change 6 (success view, lines 43-62):
    - Add `ref={successHeadingRef} tabIndex={-1}` to the `<h1>` at line 53.
    - Add `aria-hidden="true"` to the `favorite` icon span at line 47-52.
    - Replace the static body at lines 56-59 with `{successBody}` where `successBody` is computed via:
      `const successBody = form.attending === "decline" ? "We'll miss you in Aspen, but thank you for letting us know. We're holding good thoughts for you." : "We can't wait to celebrate with you in Aspen. We'll send venue and timing details closer to the wedding.";`
      (Declare `successBody` inside the `if (status === "success")` block before the return.)

    Change 7 (form attributes, line 95):
    - Add `noValidate` to the `<form>` element.
    - Add `aria-labelledby="rsvp-heading"` to the `<form>`.
    - Add `aria-busy={status === "submitting"}` to the `<form>`.
    - Add `id="rsvp-heading"` to the `<h1>` at line 73 (the "Kindly Respond" headline) so aria-labelledby resolves.

    Change 8 (htmlFor/id pairing — six labels and inline error rendering):

    For each pair below, add `htmlFor="ID"` to the label and `id="ID"` to the input. Add `aria-required="true"` to the three required inputs (full_name, email, both radios). Add `aria-invalid={errors.X ? "true" : undefined}` and `aria-describedby={errors.X ? "ID-error" : undefined}` to fields that can produce errors. Render an inline error `<p id="ID-error" role="alert" className="mt-2 text-error text-xs font-body">{errors.X}</p>` directly after each input (only when `errors.X` truthy).

    - Line 99 label / 102 input -> id `rsvp-full-name`, error id `rsvp-full-name-error`, aria-required, aria-invalid, aria-describedby
    - Line 114 label / 117 input -> id `rsvp-email`, error id `rsvp-email-error`, aria-required, aria-invalid, aria-describedby
    - Line 132 group label -> add `id="rsvp-attending-label"`. Wrap the radio group div (lines 135-165) in a `<fieldset>` with `aria-labelledby="rsvp-attending-label"` (no visible legend — use the existing label as the labelledby target). Inputs at lines 137 and 152 get `id="rsvp-attending-accept"` and `id="rsvp-attending-decline"`, both get `aria-required="true"`. After the fieldset (before closing the attendance section div), render inline `<p id="rsvp-attending-error" role="alert" ...>{errors.attending}</p>` when truthy. The fieldset itself gets `aria-describedby={errors.attending ? "rsvp-attending-error" : undefined}` and `aria-invalid={errors.attending ? "true" : undefined}`.
    - Line 172 label / 175 select -> id `rsvp-guest-count` (no aria-required — optional field)
    - Line 189 label / 192 input -> id `rsvp-dietary-restrictions`
    - Line 211 label / 214 textarea -> id `rsvp-note`

    Change 9 (decorative icons, four spots):
    - Line 47-52 `favorite` icon: add `aria-hidden="true"` (already covered in Change 6)
    - Line 230 `error` icon span: add `aria-hidden="true"`
    - Line 250-252 `east` icon span: add `aria-hidden="true"`
    - Line 262-266 `format_quote` icon span: add `aria-hidden="true"`

    Change 10 (conditional reveal, lines 169-206): Wrap the `{form.attending !== "decline" && ( ... )}` block in an always-mounted container `<div aria-live="polite">` so screen readers announce the show/hide. Final shape:
    ```
    <div aria-live="polite">
      {form.attending !== "decline" && (
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 items-end">
          ...
        </div>
      )}
    </div>
    ```
    The outer aria-live div has no Tailwind classes — it is structural only.

    Change 11 (error banner, lines 228-241):
    - Add `aria-live="assertive"` to the wrapper div.
    - Add `aria-hidden="true"` to the `error` icon span at line 230.
    - Replace the static heading at lines 232-234 with `{errorCopy.heading}` AND add `ref={errorBannerRef} tabIndex={-1}` to that `<p>`.
    - Replace the static body at lines 235-238 with `{errorCopy.body}`.
    - Declare `errorCopy` inside the component body before the return (using the lookup shape from the interfaces section above).

    Change 12 (submit button, line 248): no class changes. Confirm `disabled={status === "submitting"}` is present (it is). The `aria-busy` already lives on the `<form>` from Change 7. No further changes needed.

    Do NOT change: outer grid classes, padding, the hero image block, the quote section, any color tokens, the favorite icon size style, the existing space-y-12 rhythm, or the existing `parseInt(guestCount)` mapping (that's server-side in the route handler).

    After all changes, run `npm run lint && npx tsc --noEmit && npm run build` and confirm all three pass.

    Then run `npm run dev` and manually verify (executor performs these — they are part of the autonomous task, not human checkpoints):
    a. Click into the page, click Submit with empty form. Inline errors appear under Name, Email, and the radio group with the four warm-copy strings. Focus lands on the Full Name input (first invalid field).
    b. Type "notanemail" in Email, fill Name, pick Accept, click Submit. Inline error under Email reads "That email doesn't look right — double-check the spelling."
    c. Click each label — the matching input receives focus (proves htmlFor/id pairs work).
    d. Open devtools Network -> Offline. Fill valid form, submit. Error banner appears with heading "We couldn't send your RSVP" and focus lands on the heading.
    e. Re-enable network, pick Decline, fill Name + Email, submit. Success view renders with the decline body copy.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run build && grep -c 'noValidate' 'app/(main)/rsvp/page.tsx' | grep -v '^#' | grep -q 1 && grep -c 'aria-hidden=\"true\"' 'app/(main)/rsvp/page.tsx' | grep -v '^#' | (read n; [ "$n" -ge 4 ]) && grep -cE 'htmlFor="rsvp-' 'app/(main)/rsvp/page.tsx' | grep -v '^#' | (read n; [ "$n" -ge 6 ]) && grep -cE 'id="rsvp-' 'app/(main)/rsvp/page.tsx' | grep -v '^#' | (read n; [ "$n" -ge 7 ]) && grep -q 'errorBannerRef' 'app/(main)/rsvp/page.tsx' && grep -q 'successHeadingRef' 'app/(main)/rsvp/page.tsx' && grep -q 'aria-live="polite"' 'app/(main)/rsvp/page.tsx' && grep -q 'aria-live="assertive"' 'app/(main)/rsvp/page.tsx'</automated>
  </verify>
  <done>
    `rsvp/page.tsx` ships every UI-SPEC §Acceptance criterion in the Interaction and Copy sections. Build, lint, and typecheck all green. Manual devtools verification (a-e above) all pass.
  </done>
  <acceptance_criteria>
    - `grep -c noValidate 'app/(main)/rsvp/page.tsx'` returns 1 (on the form element)
    - `grep -c 'aria-hidden="true"' 'app/(main)/rsvp/page.tsx'` returns 4 or more (favorite + error + east + format_quote, plus any future decorative icons)
    - `grep -cE 'htmlFor="rsvp-' 'app/(main)/rsvp/page.tsx'` returns 6 or more (six labels paired)
    - `grep -cE 'id="rsvp-' 'app/(main)/rsvp/page.tsx'` returns 7 or more (six field IDs + heading id + error ids — total 10+)
    - `grep -c 'errorBannerRef' 'app/(main)/rsvp/page.tsx'` returns 2 or more (declaration + ref assignment)
    - `grep -c 'successHeadingRef' 'app/(main)/rsvp/page.tsx'` returns 2 or more
    - `grep -c 'aria-live="polite"' 'app/(main)/rsvp/page.tsx'` returns 1 (conditional reveal wrapper)
    - `grep -c 'aria-live="assertive"' 'app/(main)/rsvp/page.tsx'` returns 1 (error banner)
    - `grep -c 'aria-busy' 'app/(main)/rsvp/page.tsx'` returns 1 (on form)
    - `grep -c 'aria-labelledby="rsvp-heading"' 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "We need your name to find your invitation" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "Where should we reach you with details" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "That email doesn't look right" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "Let us know if you can make it" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "We can't wait to celebrate with you in Aspen" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "We'll miss you in Aspen" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "We couldn't send your RSVP" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "Something went wrong on our end" 'app/(main)/rsvp/page.tsx'` returns 1
    - `grep -c "One of your answers needs a tweak" 'app/(main)/rsvp/page.tsx'` returns 1
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - Manual verification a-e (empty submit, invalid email, label click, network failure, decline success) all pass when run against `npm run dev`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. All grep counts above pass.
2. `npm run lint`, `npx tsc --noEmit`, `npm run build` all exit 0.
3. Manual devtools walkthrough (steps a-e in Change 12 action) all behave as specified.
4. Visual diff against shipped page: NO change to Tailwind class strings on the hero, the form card outer styling, the radio circles, the submit button styling, the quote section. Only additions are accessibility/state attributes and inline error `<p>` elements.
</verification>

<success_criteria>
- Every UI-SPEC §Acceptance Criteria checkbox in Interaction, Copy, and Visual sections is checkable
- Form submits successfully (proven by Plan 04 smoke checklist) and writes to Supabase (Plan 01 infra)
- Screen-reader-friendly: per-field errors announced, conditional reveal announced, focus management works
- iOS Safari does NOT zoom on input focus (text-base preserved)
</success_criteria>

<output>
Create `.planning/phases/01-rsvp-enablement/01-02-SUMMARY.md` when done.
</output>
