---
phase: 01-rsvp-enablement
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - .planning/phases/01-rsvp-enablement/01-SMOKE.md
autonomous: false
requirements:
  - "Run a five-step manual smoke checklist verifying the full RSVP flow end-to-end"
  - "Confirm production env vars are set in the deploy target (per RESEARCH Open Questions Q2)"

must_haves:
  truths:
    - "Happy-path accept submission writes a real row in Supabase with attending=true, guest_count=N"
    - "Happy-path decline submission writes a row with attending=false"
    - "Submitting an empty form shows three inline errors, no native browser popup"
    - "Network failure (devtools Offline) shows the network-variant error banner with focus on the heading"
    - "At 375px viewport the form has no horizontal scroll and fields stack vertically"
    - "Production deploy target has both NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY set"
  artifacts:
    - path: ".planning/phases/01-rsvp-enablement/01-SMOKE.md"
      provides: "Documented smoke-test results for the phase"
      contains: "Executed:, Environment:, Result:"
  key_links:
    - from: "01-SMOKE.md"
      to: "Supabase rsvps table"
      via: "POST /api/rsvp"
      pattern: "Verify in Supabase Studio"
---

<objective>
Prove the entire RSVP flow works end-to-end with a five-step manual smoke checklist, then confirm production environment is configured. Wave 4 gate — nothing ships to production until this passes.

Purpose: ROADMAP §Scope explicitly allows a manual smoke checklist instead of a test framework. RESEARCH §Testing Approach codifies the five checks. UI-SPEC §Acceptance Criteria §Testing lists the same five. This plan executes them and records results.

Output: A populated `01-SMOKE.md` in the phase folder with date, environment, per-step results, and a final pass/fail verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rsvp-enablement/01-RESEARCH.md
@.planning/phases/01-rsvp-enablement/01-UI-SPEC.md
@.planning/phases/01-rsvp-enablement/01-PATTERNS.md

@CLAUDE.md
@AGENTS.md

# Already-shipped polish (Plans 01-03 must be complete before this plan runs)
@app/(main)/rsvp/page.tsx
@app/(main)/api/rsvp/route.ts
@components/Navbar.tsx

<interfaces>
<!-- 01-SMOKE.md template (from PATTERNS.md §01-SMOKE.md, derived from RESEARCH §Testing Approach lines 253-261) -->

# Phase 1 — RSVP Smoke Checklist

**Executed:** YYYY-MM-DD
**Environment:** local | production
**Result:** pending

## 1. Happy-path accept
## 2. Happy-path decline
## 3. Validation error path
## 4. Network failure path
## 5. Mobile viewport (375px)

## Production env var check
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the smoke checklist scaffold</name>
  <files>.planning/phases/01-rsvp-enablement/01-SMOKE.md</files>
  <read_first>
    - .planning/phases/01-rsvp-enablement/01-RESEARCH.md (§Testing Approach lines 253-261 — the five checks verbatim)
    - .planning/phases/01-rsvp-enablement/01-PATTERNS.md (§01-SMOKE.md skeleton)
  </read_first>
  <action>
    Create `.planning/phases/01-rsvp-enablement/01-SMOKE.md` with the following structure. Use markdown checkboxes for each substep so the executor can tick them as they run. The "Result:" field at the top starts as "pending" and is updated to "pass" or "fail" after Task 2 completes.

    File content:

    # Phase 1 — RSVP Smoke Checklist

    **Executed:** YYYY-MM-DD (fill in when Task 2 runs)
    **Environment:** local
    **Tester:** Tyler
    **Result:** pending

    ---

    ## 1. Happy-path accept

    - [ ] Run `npm run dev`. Visit http://localhost:3000/rsvp.
    - [ ] Fill: Full Name = "Smoke Accept", Email = "smoke-accept@example.com".
    - [ ] Pick "Delightfully Accept".
    - [ ] Select "2 Guests".
    - [ ] Dietary = "Gluten-free".
    - [ ] Note = "Excited to celebrate!"
    - [ ] Click Submit Response.
    - [ ] **Expect:** "Thank You" view with body copy "We can't wait to celebrate with you in Aspen. We'll send venue and timing details closer to the wedding."
    - [ ] **Verify in Supabase Studio:** open Table Editor -> rsvps. New row exists with `full_name = "Smoke Accept"`, `attending = true`, `guest_count = 2`, `dietary_restrictions = "Gluten-free"`, `note = "Excited to celebrate!"`, `created_at` within the last minute.
    - [ ] **Cleanup:** delete the row in Studio.

    **Result:** pending

    ---

    ## 2. Happy-path decline

    - [ ] Visit http://localhost:3000/rsvp.
    - [ ] Fill: Full Name = "Smoke Decline", Email = "smoke-decline@example.com".
    - [ ] Pick "Regretfully Decline".
    - [ ] Confirm Guest Details (number + dietary) is hidden — the conditional reveal collapses.
    - [ ] Note placeholder changes to "We'll miss you! Leave a note if you'd like..."
    - [ ] Add note = "Wishing you both the best."
    - [ ] Click Submit Response.
    - [ ] **Expect:** "Thank You" view with body copy "We'll miss you in Aspen, but thank you for letting us know. We're holding good thoughts for you."
    - [ ] **Verify in Studio:** row with `attending = false`, `guest_count = 1` (form default — not reset when hidden), `dietary_restrictions = null`, `note = "Wishing you both the best."`
    - [ ] **Cleanup:** delete the row.

    **Result:** pending

    ---

    ## 3. Validation error path

    - [ ] Visit http://localhost:3000/rsvp.
    - [ ] Click Submit Response with empty form.
    - [ ] **Expect:** NO native browser popup (proves `noValidate` is on the form).
    - [ ] Inline error under Full Name: "We need your name to find your invitation."
    - [ ] Inline error under Email: "Where should we reach you with details?"
    - [ ] Inline error under the radio group: "Let us know if you can make it."
    - [ ] Focus is on the Full Name input.
    - [ ] Type "notanemail" into Email, fill Name = "x", pick Accept, click Submit.
    - [ ] Inline error under Email changes to: "That email doesn't look right — double-check the spelling."

    **Result:** pending

    ---

    ## 4. Network failure path

    - [ ] Open devtools -> Network -> set throttling to "Offline".
    - [ ] Fill a valid form (Name + Email + Accept).
    - [ ] Click Submit Response.
    - [ ] **Expect:** Error banner appears above the submit button. Heading reads "We couldn't send your RSVP". Body references `hello@emilyandtyler.com` as a mailto link.
    - [ ] **Expect:** Focus lands on the error banner heading (verify by tabbing — next tab moves to the submit button).
    - [ ] Re-enable network (set throttling to "No throttling"). Click Submit Response again.
    - [ ] **Expect:** Form transitions to success view (already validated in Step 1, no need to clean up the Supabase row this time — go ahead and delete).
    - [ ] **Cleanup:** delete the row in Studio.

    **Result:** pending

    ---

    ## 5. Mobile viewport (375px)

    - [ ] Open devtools -> Device toolbar (Cmd+Shift+M in Chrome).
    - [ ] Set viewport to 375 x 812 (iPhone 13 mini).
    - [ ] Visit http://localhost:3000/rsvp.
    - [ ] **Expect:** No horizontal scroll. Form fields are full-width. Section gaps preserved (~48px between sections).
    - [ ] **Expect:** Accept/Decline radios stack vertically (one above the other).
    - [ ] Tap into the Full Name input.
    - [ ] **Expect:** No iOS-style zoom on focus (font-size is 16px — proves the `text-base` rule was preserved).
    - [ ] Click the hamburger menu icon top-right.
    - [ ] **Expect:** RSVP appears as the last item in the dropdown.
    - [ ] Fill the form, submit.
    - [ ] **Expect:** Success view fits the viewport with no horizontal scroll.
    - [ ] **Cleanup:** delete the row in Studio.

    **Result:** pending

    ---

    ## 6. Production env var check

    - [ ] Confirm the production deploy target (from Plan 01 Task 1 — should be on record).
    - [ ] If deployed (Vercel / Netlify / self-hosted): open the host dashboard.
      - Vercel: Project Settings -> Environment Variables.
      - Netlify: Site configuration -> Environment variables.
    - [ ] Confirm both `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` are set with the same values as `.env.local`.
    - [ ] **Expect:** Both vars present, both `NEXT_PUBLIC_*` prefixed, no `SUPABASE_SERVICE_ROLE_KEY` set.
    - [ ] If NOT deployed yet: note this in the result and skip the host check.

    **Result:** pending

    ---

    ## Final Verdict

    - [ ] All 6 sections pass.
    - [ ] Final Result: ___ (pass / fail / partial — explain if not pass)

    Confirm Plans 01-04 are complete and `01-rsvp-enablement` phase can close.

    After creating this file, do NOT fill in results yet — Task 2 runs the checklist.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-rsvp-enablement/01-SMOKE.md && grep -c '^## [0-9]' .planning/phases/01-rsvp-enablement/01-SMOKE.md | grep -v '^#' | (read n; [ "$n" -ge 6 ]) && grep -q 'Happy-path accept' .planning/phases/01-rsvp-enablement/01-SMOKE.md && grep -q 'Happy-path decline' .planning/phases/01-rsvp-enablement/01-SMOKE.md && grep -q 'Validation error path' .planning/phases/01-rsvp-enablement/01-SMOKE.md && grep -q 'Network failure path' .planning/phases/01-rsvp-enablement/01-SMOKE.md && grep -q 'Mobile viewport' .planning/phases/01-rsvp-enablement/01-SMOKE.md && grep -q 'Production env var' .planning/phases/01-rsvp-enablement/01-SMOKE.md</automated>
  </verify>
  <done>`.planning/phases/01-rsvp-enablement/01-SMOKE.md` exists with all 6 sections, all sub-steps as unchecked markdown checkboxes, and "Result: pending" on each section.</done>
  <acceptance_criteria>
    - File exists at `.planning/phases/01-rsvp-enablement/01-SMOKE.md`
    - 6 numbered sections present (5 smoke tests + production env check)
    - Each section has at least one `- [ ]` checkbox
    - "**Result:** pending" appears on each section
    - The file matches the structure in the `<action>` block (sections in order, expected behaviors documented)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the smoke checklist and record results</name>
  <files>.planning/phases/01-rsvp-enablement/01-SMOKE.md</files>
  <read_first>
    - .planning/phases/01-rsvp-enablement/01-SMOKE.md (just created in Task 1)
    - .planning/phases/01-rsvp-enablement/01-UI-SPEC.md (§Acceptance Criteria — the binding spec the smoke test confirms)
  </read_first>
  <what-built>
    Plans 01-03 have shipped: Supabase backend is configured (Plan 01), form is polished (Plan 02), nav link is enabled (Plan 03). This task verifies the full flow works.

    Surface this exact instruction to the user:

    "The RSVP feature is ready for smoke testing. Open `.planning/phases/01-rsvp-enablement/01-SMOKE.md` in your editor and follow each section in order. After each step:
    1. Check the box (`[x]`) when the step behaves as expected.
    2. If the step fails, leave the box unchecked and add a one-line note describing what went wrong.
    3. Update the `**Result:** pending` line at the bottom of each section to `pass` or `fail`.
    4. Run `npm run dev` in a terminal before starting Step 1.

    Each step references the success criteria from UI-SPEC. If anything is unclear, the UI-SPEC §Acceptance Criteria section is the source of truth.

    For Section 6 (production env var check): if the site is not yet deployed, mark Section 6 as 'skipped' and note 'not deployed yet — re-run before production launch'. This is acceptable for closing the phase.

    Reply 'smoke passed' when all sections are green, or describe any failures."
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/01-rsvp-enablement/01-SMOKE.md` in your editor.
    2. Run `npm run dev` in a terminal.
    3. Open http://localhost:3000/rsvp in Chrome (or Safari for iOS verification).
    4. Walk through each numbered section in order. Tick boxes. Note failures inline.
    5. Update the per-section `**Result:**` lines and the final verdict.
    6. Save the file.
    7. Report back in chat: "smoke passed" OR a list of which sections failed and what you observed.
  </how-to-verify>
  <resume-signal>Reply "smoke passed" or describe failures step-by-step</resume-signal>
  <acceptance_criteria>
    - All 6 sections in `01-SMOKE.md` are marked `**Result:** pass` (or Section 6 marked `skipped` with deploy note)
    - The top-of-file `**Result:** pending` is updated to `pass`
    - `Executed:` field holds today's date (YYYY-MM-DD)
    - Final verdict checkbox is checked: `[x] All 6 sections pass.`
    - User has confirmed in chat with "smoke passed" or equivalent
    - Any Supabase test rows created during smoke testing are deleted from the Table Editor
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `01-SMOKE.md` exists, all 6 sections complete with `pass` results (or Section 6 explicitly skipped + deploy note).
2. User has confirmed smoke passed in chat.
3. No leftover test rows in the Supabase `rsvps` table.
4. Phase 1 is ready to close — `STATE.md` updates and `git commit` happen in the orchestrator phase-close flow.
</verification>

<success_criteria>
- Smoke checklist file exists in the phase folder
- All 5 smoke tests pass against the local dev server
- Production env vars verified (or skipped with explicit deploy-not-yet note)
- The RSVP feature is production-ready: schema + RLS + env + form + nav + verification
</success_criteria>

<output>
Create `.planning/phases/01-rsvp-enablement/01-04-SUMMARY.md` when done. Phase 1 closes after this plan.
</output>
