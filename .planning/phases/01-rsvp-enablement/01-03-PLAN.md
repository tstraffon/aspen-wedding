---
phase: 01-rsvp-enablement
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - components/Navbar.tsx
autonomous: true
requirements:
  - "Enable the commented-out RSVP nav link on both desktop and mobile menus"

must_haves:
  truths:
    - "Visiting / shows an RSVP link in the desktop nav as the last item"
    - "Opening the mobile menu shows RSVP as the last item"
    - "Clicking RSVP navigates to /rsvp"
    - "On /rsvp, the RSVP nav link renders with text-primary (active state)"
  artifacts:
    - path: "components/Navbar.tsx"
      provides: "Navbar with RSVP link enabled"
      contains: '{ label: "RSVP", href: "/rsvp" }'
  key_links:
    - from: "links array (top of file)"
      to: "desktop nav .map() and mobile nav .map()"
      via: "shared array iteration"
      pattern: "label: \"RSVP\""
---

<objective>
Enable the RSVP link in the navbar so guests can reach the form from any page. Wave 3 — after form polish is verified — keeps a half-broken form from being discoverable mid-deploy.

Purpose: ROADMAP Phase 1 §Scope explicitly calls out "Enable the commented-out RSVP nav link in `components/Navbar.tsx`." Without this, the polished form is invisible.

Output: A single-line uncomment + a dead-comment deletion. The shipped desktop and mobile loops both iterate the same `links` array, so one change covers both surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rsvp-enablement/01-UI-SPEC.md
@.planning/phases/01-rsvp-enablement/01-PATTERNS.md
@.planning/phases/01-rsvp-enablement/01-RESEARCH.md

@CLAUDE.md
@AGENTS.md

# File being modified
@components/Navbar.tsx

<interfaces>
<!-- Current links array (lines 7-16) -->
```tsx
const links = [
  { label: "Home", href: "/" },
  // { label: "Our Story", href: "/#our-story" },
  { label: "Travel & Stay", href: "/travel" },
  { label: "Itinerary", href: "/itinerary" },
  { label: "Things To Do", href: "/things-to-do" },
  // { label: "Registry", href: "/#registry" },
  { label: "FAQ", href: "/faq" },
  // { label: "RSVP", href: "/rsvp" },
];
```

<!-- Final desktop order per UI-SPEC §Navbar Enablement: Home, Travel & Stay, Itinerary, Things To Do, FAQ, RSVP (last) -->
<!-- The commented entry at line 15 is already in the correct last position. Just uncomment. -->

<!-- Dead comment at line 75 to remove -->
```tsx
{/* RSVP button — re-enable when ready to collect responses */}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Uncomment RSVP link and delete dead comment slot</name>
  <files>components/Navbar.tsx</files>
  <read_first>
    - components/Navbar.tsx (current state — entire file)
    - .planning/phases/01-rsvp-enablement/01-UI-SPEC.md (§Navbar Enablement — confirms link order, mobile parity, no special button styling)
    - .planning/phases/01-rsvp-enablement/01-PATTERNS.md (§components/Navbar.tsx — confirms one-line change)
    - CLAUDE.md + AGENTS.md (Next.js 16 reminder — `usePathname` and `Link` are unchanged in v16, no migration concern)
  </read_first>
  <action>
    Two edits to `components/Navbar.tsx`:

    Edit 1 (line 15): Remove the leading `// ` from `// { label: "RSVP", href: "/rsvp" },` so the entry becomes a live array element. The trailing comma stays. Final line: `  { label: "RSVP", href: "/rsvp" },`.

    Edit 2 (line 75): Delete the entire line `        {/* RSVP button — re-enable when ready to collect responses */}` including any leading whitespace. The blank line above and the closing `</div>` below stay as-is.

    Do NOT touch:
    - The commented Our Story entry at line 9 (out of phase scope — deferred)
    - The commented Registry entry at line 13 (Phase 2 will uncomment)
    - The desktop .map() at lines 41-63 (no change needed — iterates the links array)
    - The mobile .map() at lines 84-105 (no change needed — same array)
    - The active-state class logic (`pathname.startsWith(href)` at lines 43-48 and 85-90 — already handles `/rsvp` correctly)

    After editing, run `npm run lint && npx tsc --noEmit && npm run build` to confirm no regressions. Then `npm run dev` and manually verify:
    a. Visit `http://localhost:3000/`. Desktop viewport: RSVP appears as the last item in the nav (after FAQ), styled like every other link (no special accent).
    b. Visit `http://localhost:3000/rsvp`. The RSVP link renders with `text-primary` (active state).
    c. Resize to <768px viewport. Click the hamburger. RSVP is the last item in the dropdown.
    d. Click RSVP in the mobile menu. Navigates to /rsvp, dropdown closes (setMenuOpen(false) already fires on click — preserved).
  </action>
  <verify>
    <automated>grep -cE '^\s*//\s*\{\s*label:\s*"RSVP"' components/Navbar.tsx | grep -v '^#' | grep -q 0 && grep -cE '\{\s*label:\s*"RSVP",\s*href:\s*"/rsvp"\s*\}' components/Navbar.tsx | grep -v '^#' | grep -q 1 && grep -c 'RSVP button — re-enable' components/Navbar.tsx | grep -v '^#' | grep -q 0 && npm run lint && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    The RSVP entry in the `links` array is live (not commented). The dead `{/* RSVP button — re-enable... */}` comment is gone. Build, lint, typecheck all green. Visiting `/` shows RSVP in the desktop and mobile nav. Visiting `/rsvp` highlights it with `text-primary`.
  </done>
  <acceptance_criteria>
    - `grep -E '^\s*//\s*\{\s*label:\s*"RSVP"' components/Navbar.tsx` returns no match (no commented RSVP entry left)
    - `grep -E '\{\s*label:\s*"RSVP",\s*href:\s*"/rsvp"\s*\}' components/Navbar.tsx` returns 1 match (live entry exists)
    - `grep -c 'RSVP button — re-enable' components/Navbar.tsx` returns 0 (dead comment removed)
    - The RSVP entry is in position 6 of the links array (after FAQ) — confirmed by reading lines 7-15
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - Manual verification a-d all pass against `npm run dev`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep` counts above pass.
2. `npm run lint`, `npx tsc --noEmit`, `npm run build` all exit 0.
3. Visual confirmation in browser: RSVP visible last on desktop nav (>=1024px) and last in mobile dropdown (<768px), active state highlights when on /rsvp.
4. Click-through: clicking RSVP from any page navigates to /rsvp without a full reload (Next.js Link behavior preserved).
</verification>

<success_criteria>
- RSVP link appears in both desktop and mobile navs as the last item
- Active state highlighting works on /rsvp
- No commented-out RSVP code or dead "re-enable when ready" comment remains
</success_criteria>

<output>
Create `.planning/phases/01-rsvp-enablement/01-03-SUMMARY.md` when done.
</output>
