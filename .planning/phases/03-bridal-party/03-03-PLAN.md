---
phase: 03-bridal-party
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - components/Navbar.tsx
autonomous: false
requirements:
  - D-12
tags:
  - bridal-party
  - navbar
  - smoke-checklist

must_haves:
  truths:
    - "D-12: The links array in components/Navbar.tsx has a new entry { label: 'Bridal Party', href: '/bridal-party' } inserted between Things To Do (index 3) and FAQ (index 4). After the edit, the array order is exactly: Home, Travel & Stay, Itinerary, Things To Do, Bridal Party, FAQ, Registry, RSVP."
    - "Both the desktop nav (>=768px) and the mobile menu list Bridal Party in that position (they share the same links array)"
    - "Active-state highlighting works: navigating to /bridal-party turns the Bridal Party nav text warm-gold (text-primary) via the existing pathname.startsWith(href) + isActive && href === pathname branching — no code change to the active-state logic (lines 43-48 desktop, 84-89 mobile)"
    - "End-to-end smoke checklist passes on localhost:3000 covering every UI-SPEC §Acceptance Criteria A1-A8, B1-B6, C1-C7, D1-D11, E1-E4, F1-F4, G1-G4, H1-H5, I1-I7, J1-J3 (53 items total)"
    - "No regressions on the previously-shipped routes: /, /travel, /itinerary, /things-to-do, /faq, /registry, /rsvp all still render and the Phase 2 Registry nav highlight still works"
  artifacts:
    - path: "components/Navbar.tsx"
      provides: "Nav with Bridal Party link wired to /bridal-party at index 4"
      contains: 'Bridal Party'
  key_links:
    - from: "Navbar Bridal Party link"
      to: "/bridal-party route (Plans 01 + 02 output)"
      via: "Next.js Link client navigation"
      pattern: "/bridal-party"
    - from: "Navbar active-state logic (lines 43-48 and 84-89)"
      to: "/bridal-party pathname match"
      via: "pathname.startsWith(href) plus isActive && href === pathname"
      pattern: "pathname.startsWith"
---

<objective>
Wire the new `/bridal-party` route into the global Navbar with a single one-line insertion, then run the full lint + build + manual smoke checklist that closes Phase 3.

Purpose: Plans 01 and 02 shipped the page itself but it is undiscoverable without a nav link. This plan is the final wire-up plus the end-to-end verification gate. It carries one human checkpoint because UI-SPEC §Acceptance Criteria has 53 verifiable items spanning visual rendering, motion timing, breakpoint behavior, a11y tree, reduced-motion handling, and cross-page regression — none of which can be automated without introducing a Playwright/Vitest framework (out of scope per Phase 2 precedent).

Output: One-line addition to `components/Navbar.tsx` plus a green smoke checklist documented in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bridal-party/03-CONTEXT.md
@.planning/phases/03-bridal-party/03-UI-SPEC.md
@.planning/phases/03-bridal-party/03-PATTERNS.md
@.planning/phases/03-bridal-party/03-01-SUMMARY.md
@.planning/phases/03-bridal-party/03-02-SUMMARY.md
@.planning/phases/02-registry-page/02-03-SUMMARY.md
@AGENTS.md
@components/Navbar.tsx
@app/(main)/bridal-party/page.tsx

<interfaces>
Current `components/Navbar.tsx` lines 7-16 (post-Phase-2 shape):

```
const links = [
  { label: "Home", href: "/" },
  // { label: "Our Story", href: "/#our-story" },
  { label: "Travel & Stay", href: "/travel" },
  { label: "Itinerary", href: "/itinerary" },
  { label: "Things To Do", href: "/things-to-do" },
  { label: "FAQ", href: "/faq" },
  { label: "Registry", href: "/registry" },
  { label: "RSVP", href: "/rsvp" },
];
```

Target `components/Navbar.tsx` lines 7-17 (post-Phase-3 shape):

```
const links = [
  { label: "Home", href: "/" },
  // { label: "Our Story", href: "/#our-story" },
  { label: "Travel & Stay", href: "/travel" },
  { label: "Itinerary", href: "/itinerary" },
  { label: "Things To Do", href: "/things-to-do" },
  { label: "Bridal Party", href: "/bridal-party" },
  { label: "FAQ", href: "/faq" },
  { label: "Registry", href: "/registry" },
  { label: "RSVP", href: "/rsvp" },
];
```

Active-state logic at lines 43-48 desktop and 84-89 mobile (DO NOT touch, per CONTEXT D-12: "Active-state branching at Navbar.tsx:43-48 (desktop) / :84-89 (mobile) is NOT touched — pathname.startsWith('/bridal-party') will work out of the box"):

```
const isActive =
  href === "/"
    ? pathname === "/"
    : href.startsWith("/#")
      ? pathname === "/"
      : pathname.startsWith(href);
```

Because `/bridal-party` does not start with `/#` and is not `/`, the third branch (`pathname.startsWith("/bridal-party")`) handles it. The subsequent `isActive && href === pathname` check at line 54 then applies `text-primary` only on exact `/bridal-party` match.

Deviation from Phase 2 pattern: Phase 2 (Registry) had a commented-out stub at the target position which was simply uncommented and the `href` updated. Bridal Party has NO prior stub — this is a brand-new entry, inserted at index 4, NOT an uncomment. (See PATTERNS §components/Navbar.tsx Pattern 1.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert Bridal Party nav entry between Things To Do and FAQ</name>
  <files>components/Navbar.tsx</files>
  <read_first>
    - .planning/phases/03-bridal-party/03-UI-SPEC.md §"Navbar Integration" (lines 414-431) — verbatim entry, position, styling rationale
    - .planning/phases/03-bridal-party/03-CONTEXT.md D-12 — locked position (between Things To Do and FAQ), confirmation that active-state branching is NOT touched
    - .planning/phases/03-bridal-party/03-PATTERNS.md §"components/Navbar.tsx (component, static-config)" (lines 322-372) — both Pattern 1 (insert) and Pattern 2 (active-state read-only reference)
    - components/Navbar.tsx (current state — the links array at lines 7-16; confirm post-Phase-2 shape has Registry at index 5 as an active entry, NOT a commented stub)
    - .planning/phases/02-registry-page/02-03-SUMMARY.md (verify the Phase 2 nav edit landed and what the final post-Phase-2 array looks like, so this plan inserts at the correct position relative to the new Registry entry)
  </read_first>
  <action>
    Edit `components/Navbar.tsx`. Insert exactly one new line in the `links` array (currently lines 7-16): the entry for Bridal Party goes between the existing Things To Do entry and the existing FAQ entry.

    Specifically, after the existing line that reads `{ label: "Things To Do", href: "/things-to-do" },` and before the existing line that reads `{ label: "FAQ", href: "/faq" },`, add a new line containing the entry `{ label: "Bridal Party", href: "/bridal-party" },`.

    Match the surrounding entries' indentation exactly — do not introduce tab vs. space inconsistencies. The existing entries inside the array use two-space indentation relative to the `const links = [` line (so four leading spaces from column 1); mirror that.

    Resulting `links` array order (top-to-bottom, after the edit):
    1. Home — `/`
    2. (commented-out Our Story — leave it)
    3. Travel & Stay — `/travel`
    4. Itinerary — `/itinerary`
    5. Things To Do — `/things-to-do`
    6. Bridal Party — `/bridal-party` (the new entry)
    7. FAQ — `/faq`
    8. Registry — `/registry`
    9. RSVP — `/rsvp`

    Final desktop nav order rendered left-to-right after the logo: Home, Travel & Stay, Itinerary, Things To Do, Bridal Party, FAQ, Registry, RSVP. Matches UI-SPEC §Navbar Integration final order exactly.

    Do NOT in this task:
    - Touch the active-state branching at lines 43-48 (desktop) or lines 84-89 (mobile). The existing pathname.startsWith(href) third branch handles `/bridal-party` correctly out of the box (CONTEXT D-12 confirms).
    - Add icons, styles, or any new className to the entry. Styling is inherited from the shared `font-label text-[10px] uppercase tracking-[0.2em]` desktop className applied in the .map at line 53 (and the equivalent at line 95 for mobile).
    - Reorder any other entries. Only one insertion.
    - Remove the commented-out `// { label: "Our Story", ... }` line. That comment is unrelated dead code from earlier phases; leaving it preserves git blame on the surrounding lines.
    - Touch any other file. No `app/(main)/bridal-party/page.tsx` edits (it is content-complete after Plan 02). No layout file edits. No global CSS edits.

    Post-edit verification commands the executor should run:
    1. `grep -c "Bridal Party" components/Navbar.tsx` returns 1 (the label appears once in source — the .map renders it in two places).
    2. `grep -c "/bridal-party" components/Navbar.tsx` returns 1.
    3. `grep -n '"Things To Do"\|"Bridal Party"\|"FAQ"\|"Registry"\|"RSVP"' components/Navbar.tsx` returns the five matches in source line-number order: Things To Do, then Bridal Party, then FAQ, then Registry, then RSVP.
    4. `git diff components/Navbar.tsx` shows exactly one added line and zero deleted lines (a pure insertion).
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; grep -q "Bridal Party" components/Navbar.tsx &amp;&amp; grep -q "/bridal-party" components/Navbar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `components/Navbar.tsx` contains exactly one new line declaring the Bridal Party entry; the entry text matches the exact form used by the surrounding entries (label key, href key, trailing comma)
    - The label string `Bridal Party` appears exactly once in the file (single source entry; .map renders it to DOM twice)
    - The href value `/bridal-party` appears exactly once in the file
    - Source order check: running `grep -n '"Things To Do"\|"Bridal Party"\|"FAQ"\|"Registry"\|"RSVP"' components/Navbar.tsx` returns lines in the order Things To Do, Bridal Party, FAQ, Registry, RSVP
    - The active-state branching at lines 43-48 (desktop) and 84-89 (mobile) is UNCHANGED — git diff scope is restricted to the single insertion inside the links array
    - The commented-out `// { label: "Our Story", ... }` line is still present and unchanged
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - On dev server (`npm run dev`), visiting `/` shows the desktop nav with the Bridal Party entry visible between Things To Do and FAQ
  </acceptance_criteria>
  <done>The Navbar `links` array exports an entry pointing to `/bridal-party` in position 4 (zero-indexed: index 4, between Things To Do and FAQ). Lint and build both pass. Mobile and desktop nav both render the Bridal Party link because they share the same array.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke checklist for the complete Bridal Party phase</name>
  <what-built>
    Plans 01-03 of Phase 03 ship the entire `/bridal-party` page (hero + Bride's Side section with 8 magazine rows + Groom's Side section with 8 magazine rows + monogram fallback for missing photos) and wire the Navbar Bridal Party link at index 4. The full UI-SPEC §Acceptance Criteria block (53 items across sections A-J) needs human verification because no e2e test framework is installed and the Phase 2 precedent explicitly kept e2e tooling out of scope for these UI phases. The smoke checklist below walks every UI-SPEC item.
  </what-built>
  <how-to-verify>
    Start the dev server in one terminal:
    ```
    npm run dev
    ```

    Then open `http://localhost:3000` in the browser. Walk every item below; each must pass. If any item fails, capture the specific failure (screenshot or DOM snippet) and report it instead of approving.

    **A. Functional (UI-SPEC A1-A8):**
    1. (A1) URL `http://localhost:3000/bridal-party` returns 200 and renders the page; no 404, no runtime error.
    2. (A2) The page shows the shared Navbar at the top, the Footer at the bottom, and the floating MusicButton — confirming the `(main)` route group layout auto-wrapped the new page.
    3. (A3) Count member rows on the page: exactly 16 total. Bride's Side section has 8, Groom's Side section has 8. Use the heading-tree count or DOM inspection.
    4. (A4) Inside Bride's Side, the row order top-to-bottom is exactly: Sarah Else, Emily Asinger, Lindsay Carr, Sarah Horan, Sam Jones, Shannon Robins, Michelle Spencer, Ryan Hindle. Inside Groom's Side: Dylan Straffon, Aaron Sorge, Jack Cardello, Ken Kinoshita, Jon Metz, Ian Adams, Collin DeMatt, Josh Tallman. No alphabetization, no shuffle.
    5. (A5) Right-click any row and Inspect Element. The rendered DOM is templated from a Member object with `name`, `role`, `bio` text content; the photo path matches `/bridal-party/<slug>.jpg`. Verify the source file `app/(main)/bridal-party/page.tsx` declares `photo: string | null` in the Member type.
    6. (A6) View source on `app/(main)/bridal-party/page.tsx`. Verify exactly 16 occurrences of `// TODO: replace with real bio` and that every bio reads the verbatim placeholder line `A dear friend to both of us — we're so glad they're standing with us.`
    7. (A7) Open `components/Navbar.tsx`. Verify the `links` array has the entry `{ label: "Bridal Party", href: "/bridal-party" }` at index 4 (sixth array element counting the commented-out Our Story entry as index 1; or fifth counting only active entries).
    8. (A8) Navigate to `/bridal-party`. The Bridal Party nav link turns warm-gold (`text-primary`, #d4a373). Navigate away to `/registry` — Bridal Party returns to muted; Registry now shows the active gold treatment. Confirm the active-state switch is automatic.

    **B. Hero (UI-SPEC B1-B6):**
    9. (B1) Hero is full-width and exactly 614px tall on desktop. The background image fills it with a parallax-on-scroll effect. The 2-axis scrim recipe darkens both the bottom edge and the left edge so the eyebrow and headline are legible against any background image variant.
    10. (B2) Hero eyebrow reads `Our People` in uppercase warm-gold with wide tracking (Manrope `font-label text-xs uppercase tracking-[0.4em] text-primary`).
    11. (B3 + B4) Hero `<h1>` reads `The Ones Standing With Us` where the entire phrase `Standing With Us` is italic warm-gold (`italic font-light text-primary/80`) and the rest is cream (`text-on-surface`). Heading uses Noto Serif at a large display size that scales `text-5xl md:text-8xl` with `leading-[0.85] tracking-tighter`.
    12. (B5 + B6) Hero subtitle reads verbatim: `Eight on each side — the people we've leaned on, laughed with, and could not picture this weekend without.` Styled `text-on-surface-variant text-lg max-w-2xl font-light leading-relaxed`.

    **C. Sections (UI-SPEC C1-C7):**
    13. (C1) Bride's Side section renders ABOVE Groom's Side. Scrolling down: hero → Bride's Side section header → 8 Bride's Side rows → Groom's Side section header → 8 Groom's Side rows → footer.
    14. (C2 + C3) Each section block has vertical padding `py-24 md:py-32` against the `bg-background` page color. Inner content sits inside `max-w-[1440px] mx-auto px-6 md:px-12`.
    15. (C4) Bride's Side header reads eyebrow `THE BRIDE'S SIDE` (warm-gold uppercase wide-tracking) then `<h2>` `Bride's Side` with `Side` italicized in warm gold.
    16. (C5) Groom's Side header reads eyebrow `THE GROOM'S SIDE` then `<h2>` `Groom's Side` with `Side` italicized in warm gold.
    17. (C6) Both `<h2>` elements are `font-headline text-4xl md:text-6xl text-on-surface` (Noto Serif at the section-heading display size).
    18. (C7) Each section header wrapper carries `mb-16 md:mb-24 reveal-on-scroll` (the header fades up as the section enters the viewport).

    **D. Magazine Rows (UI-SPEC D1-D11):**
    19. (D1) Each row is a 12-column grid on desktop (`md:grid-cols-12`) with `gap-y-8 md:gap-x-12 lg:gap-x-16 items-center` and a `reveal-on-scroll` entrance.
    20. (D2 + D3) On desktop, Sarah Else (i=0) renders with text-left and photo-right. Emily Asinger (i=1) renders with text-right and photo-left. Lindsay Carr (i=2) reverts to text-left/photo-right. The alternation continues through all 8 rows. Dylan Straffon (i=0 of Groom's Side) also renders text-left/photo-right — anchoring both honor attendants in the same visual position at the top of each section.
    21. (D4) Shrink the viewport below 768px. Every row collapses to a single column. Text renders ABOVE the photo for EVERY row regardless of desktop alternation (verified by checking Sarah Else AND Emily Asinger — both should show text first, photo second, on mobile).
    22. (D5) Vertical spacing between rows is comfortable: ~64px on mobile, ~96px on `md`, ~128px on `lg`. Rows feel like editorial spreads, not crammed cards.
    23. (D6) Each photo container is a `aspect-[4/5]` portrait rectangle (taller than wide). The container background is `bg-surface-variant/50` so missing images don't show a transparent hole.
    24. (D7) Each row's role eyebrow above the name is uppercase warm-gold with wide tracking, identical treatment to the hero and section eyebrows (just smaller margin: `mb-4` instead of `mb-6`).
    25. (D8) Each row's `<h3>` name is Noto Serif at `text-2xl md:text-4xl` in cream — same tier as the Registry card titles.
    26. (D9) Each row's bio prose is `text-on-surface-variant text-lg font-light leading-relaxed` (Manrope, light weight, generous line-height).
    27. (D10) Hover over a row name or photo. NOTHING happens — no scale, no color change, no overlay shift. Rows are static editorial; this is a deliberate departure from the Registry card hover behavior.
    28. (D11) Each row's grid wrapper carries `reveal-on-scroll` (NOT `reveal-on-scroll-stagger`). Scrolling slowly through the page, each row fades up individually as it enters the viewport.

    **E. Monogram Fallback (UI-SPEC E1-E4):**
    29. (E1 + E2) Because no portrait JPGs exist in `/public/bridal-party/` yet, EVERY row should render the monogram fallback: a dark `bg-surface-container` block with two warm-gold initials centered in italic Noto Serif at `text-5xl md:text-7xl`. Verify by visually scanning the page — all 16 photo containers should show initials, not images.
    30. (E3) Verify initials are correct two-letter derivations from each member's name: Sarah Else → SE, Emily Asinger → EA, Lindsay Carr → LC, Sarah Horan → SH, Sam Jones → SJ, Shannon Robins → SR, Michelle Spencer → MS, Ryan Hindle → RH, Dylan Straffon → DS, Aaron Sorge → AS, Jack Cardello → JC, Ken Kinoshita → KK, Jon Metz → JM, Ian Adams → IA, Collin DeMatt → CD, Josh Tallman → JT.
    31. (E4) Open `app/(main)/bridal-party/page.tsx` source. Confirm `getInitials` is declared as an inline pure function at the top of the file, NOT a separate file. The implementation handles trim/split/empty-string guards.

    **F. Images (UI-SPEC F1-F4) — exercised when Tyler drops JPGs into /public/bridal-party/:**
    32. (F1 + F2) Inspect the source: the `<img>` tag uses `className="w-full h-full object-cover"` (no transitions, no scale) and is preceded by `{/* eslint-disable-next-line @next/next/no-img-element */}` on the line above. No `next/image` import in the file.
    33. (F3) Inspect the source: each `member.photo` value follows `/bridal-party/<lowercase-kebab>.jpg`. Verify by grep: `grep -oE '"/bridal-party/[^"]+\.jpg"' app/\(main\)/bridal-party/page.tsx | grep -E '[A-Z_]'` returns NO matches (no uppercase letters, no underscores in any of the 16 paths).
    34. (F4) Inspect the source: the `<img alt>` template literal interpolates `member.name` as `Portrait of ${member.name}`. If you drop a real JPG into `/public/bridal-party/sarah-else.jpg` and reload, the Sarah Else row swaps from the monogram to the photo and the `<img>` alt reads `Portrait of Sarah Else`.

    **G. Copy (UI-SPEC G1-G4):**
    35. (G1) Search the rendered page DOM for `!` characters. ZERO exclamation marks anywhere on the page (hero, section headers, role labels, bios). The page tone is reverent/intimate, not celebratory.
    36. (G2) Hero copy exactly matches the Copywriting Contract: eyebrow `Our People`, headline `The Ones Standing With Us`, subtitle `Eight on each side — the people we've leaned on, laughed with, and could not picture this weekend without.`
    37. (G3) All 16 bios are literally identical to the placeholder string. The uniformity is intentional — different placeholder variations would mask "still placeholder" status at code review.
    38. (G4) Only Sarah Else displays `MAID OF HONOR`. Only Dylan Straffon displays `BEST MAN`. The other 7 Bride's Side members all display `BRIDESMAID`. The other 7 Groom's Side members all display `GROOMSMAN`. Count via DOM inspection.

    **H. Motion (UI-SPEC H1-H5):**
    39. (H1) On initial paint at the top of the page, hero eyebrow fades up first (~200ms delay), then title (~400ms delay), then subtitle (~600ms delay). All three fade-ups are smooth, ~800ms duration.
    40. (H2) Scroll up and down at the top of the page — the hero background image scales subtly (1.08 → 1.0) and stays smooth.
    41. (H3) Each section header fades up as it enters the viewport.
    42. (H4) Each row fades up as it enters the viewport — individually, NOT as a stagger of children.
    43. (H5) Enable OS-level reduce-motion (macOS: System Settings → Accessibility → Display → Reduce motion). Reload `/bridal-party`. Hero reveals are instant or absent; row reveals are instant or absent; the hero background does not parallax. Disable reduce-motion afterward.

    **I. Accessibility (UI-SPEC I1-I7):**
    44. (I1) Page has exactly one `<h1>` (the hero title). Verify with DevTools Accessibility tree or `document.querySelectorAll('h1').length` in the console returns 1.
    45. (I2) Page has exactly two `<h2>` elements (Bride's Side, Groom's Side). Console: `document.querySelectorAll('h2').length` returns 2.
    46. (I3) Page has exactly sixteen `<h3>` elements, in member source order. Console: `document.querySelectorAll('h3').length` returns 16; `[...document.querySelectorAll('h3')].map(h => h.textContent)` returns the 16 names in the verbatim order from C13 above.
    47. (I4) Inspect the hero: the parallax bg div, the scrim div, and (for any monogram rows) the monogram inner block all carry `aria-hidden="true"`. Initials spans inside the monogram blocks also carry `aria-hidden="true"`.
    48. (I5) Inspect any future `<img>` tag (after dropping a JPG in): `alt` reads `Portrait of <member name>`.
    49. (I6) Search the page source for `focus:ring-0`. Zero occurrences in `app/(main)/bridal-party/page.tsx`. Native browser focus rings are preserved on navbar links.
    50. (I7) Search the rendered page DOM for `<a target="_blank"`. Zero matches on `/bridal-party` (the page has no outbound links — no tabnabbing surface at all).

    **J. Build + Lint (UI-SPEC J1-J3):**
    51. (J1) Stop the dev server. Run `npm run build`. Exit code 0. Build output lists `/bridal-party` as a static (○) route.
    52. (J2) Run `npm run lint`. Exit code 0 with no new errors or warnings in `app/(main)/bridal-party/page.tsx` or `components/Navbar.tsx` beyond the sanctioned single `eslint-disable-next-line @next/next/no-img-element` inline directive in the page file.
    53. (J3) TypeScript compiles. The Member type with `photo: string | null` resolves correctly.

    **Regression sweep — visit each previously-shipped route:**
    - `/` (home) — renders normally, hero unchanged, navbar shows new Bridal Party link
    - `/travel` — renders normally
    - `/itinerary` — renders normally
    - `/things-to-do` — renders normally
    - `/faq` — renders normally
    - `/registry` — renders normally, Phase 2 nav highlight still works (Registry turns gold when on `/registry`)
    - `/rsvp` — renders normally, RSVP form still submits to Supabase (no need to actually submit; just confirm the form is visible and the page renders)
  </how-to-verify>
  <resume-signal>Type "approved — phase 03 ready to close" or describe failing items by letter+number (e.g., "D21 failed: Emily Asinger row shows photo above text on mobile").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | This plan modifies only `components/Navbar.tsx` (one inserted line) and runs a smoke checklist. No new outbound surface, no new input handling, no new dependencies. The previously-existing Phase 2 tabnabbing mitigation (T-02-01) on the Registry card `<a target="_blank">` elements is verified as part of the regression sweep at smoke checklist item Registry (under the Regression sweep block). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-NAV | Spoofing (open redirect via nav) | Navbar Bridal Party link | accept | The Bridal Party link targets `/bridal-party` — a same-origin route on the same Next.js deployment. `next/link` does not interpolate user input. No open-redirect surface. |
| T-03-03-DEP | Tampering | npm packages | accept | Plan adds zero new dependencies. No install steps. RESEARCH.md package legitimacy audit is not required (gate fires only on install tasks). |
| T-03-03-REG | Tampering (verify Phase 2 mitigation) | Card `<a target="_blank">` elements on `/registry` (Phase 2 output) | mitigate (verify, no new action) | Smoke checklist regression sweep visits `/registry` and confirms the page still renders; the Phase 2 `rel="noopener noreferrer"` attributes are preserved because this plan does not touch `app/(main)/registry/page.tsx`. |
| T-03-03-OUT | (no category) | Outbound links on `/bridal-party` | accept (none exist) | The new page (Plans 01 + 02 output) creates zero `<a target="_blank">` elements. Plan 03 does not add any. No tabnabbing surface on the new page. |
</threat_model>

<verification>
- `npm run lint` exits 0 after Task 1
- `npm run build` exits 0 after Task 1
- Smoke checklist (Task 2) every item passes — checker explicitly approves with the resume-signal string
- `git diff components/Navbar.tsx` shows ONLY the links-array insertion (active-state logic and all other code is untouched)
- `git diff app/(main)/registry/page.tsx` is empty (Registry page untouched in Phase 3 — confirms no regression to the Phase 2 tabnabbing mitigation)
- `git diff app/(main)/bridal-party/page.tsx` is empty in this plan (the file is content-complete after Plan 02)
- Final phase output: `/bridal-party` is discoverable from every page via the nav, renders the full hero + 2 sections + 16 magazine rows with monogram fallbacks, and ships all 53 UI-SPEC §Acceptance Criteria items green
</verification>

<success_criteria>
- `components/Navbar.tsx` `links` array contains the Bridal Party entry between Things To Do and FAQ
- Active-state branching at lines 43-48 and 84-89 is unchanged
- Manual smoke checklist (53 items across sections A-J plus a 7-route regression sweep) passes — checker approves
- Phase 03 ready to close: `/gsd:verify-work 3` will pass
- All 12 phase requirements (D-01 through D-12) are now satisfied across Plans 01-03
</success_criteria>

<output>
Create `.planning/phases/03-bridal-party/03-03-SUMMARY.md` when done. Include:
- D-IDs satisfied (D-12; full phase D-01 through D-12 closed across Plans 01-03)
- Files modified (just `components/Navbar.tsx` in this plan)
- Smoke checklist result (which items, if any, were flagged and how they were resolved before approval)
- Confirmation that `git diff` on `app/(main)/registry/page.tsx` is empty (no regression to Phase 2 tabnabbing mitigation)
- Final desktop nav order observed in the running app
- Tyler's handoff items (carried forward from Plan 02): 16 placeholder bios to replace, 16 portrait JPGs to drop into `/public/bridal-party/`, 1 hero image to swap (`/public/...` instead of Unsplash placeholder)
- Phase 03 close-out note: ready for `/gsd:verify-work 3` and `/gsd:close-phase 3`
</output>
</content>
