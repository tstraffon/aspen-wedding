---
phase: 03-bridal-party
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(main)/bridal-party/page.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
tags:
  - bridal-party
  - magazine-row
  - monogram-fallback
  - data-arrays

must_haves:
  truths:
    - "D-01: Bride's Side section renders ABOVE Groom's Side on the page, in that source order"
    - "D-02: Members within each section render in the verbatim source order from CONTEXT D-03 (honor attendant first, then attendants in the user-given order — no alphabetization, no shuffling)"
    - "D-03: Exactly 16 member rows render — 8 Bride's Side + 8 Groom's Side — with the names locked in CONTEXT D-03"
    - "D-04: Only Sarah Else carries the `MAID OF HONOR` role label; only Dylan Straffon carries `BEST MAN`; the other 7 Bride's Side members all read `BRIDESMAID`; the other 7 Groom's Side members all read `GROOMSMAN`. Role label renders as the warm-gold uppercase eyebrow above the name."
    - "D-05: Each member is a full-width magazine row (not a card grid). On desktop, rows alternate text-left/photo-right and text-right/photo-left per row index. On mobile, every row collapses to text-above-photo single-column."
    - "D-06: Every photo container uses `aspect-[4/5]`"
    - "D-07: Each member's `photo` field carries the path `/bridal-party/<slug>.jpg` where `<slug>` is `lowercase-kebab(name)`; the missing-photo fallback renders a `bg-surface-container` block with the member's two-letter initials in `font-headline italic text-primary`"
    - "D-08: Every portrait `<img>` is plain `<img>` (NOT `next/image`), preceded by `{/* eslint-disable-next-line @next/next/no-img-element */}`. This supersedes the ROADMAP's `next/image` wording per the codebase-wins convention."
    - "D-09: All bios are first-person from the couple voice (`we`, `us`) — placeholder line in this plan satisfies the voice, real bios at Tyler's handoff"
    - "D-10: All 16 bios are 1-2 sentences max — placeholder line `A dear friend to both of us — we're so glad they're standing with us.` is one sentence and sets the upper bound"
    - "D-11: Each of the 16 bio fields is preceded immediately by `// TODO: replace with real bio` and contains the verbatim placeholder line `A dear friend to both of us — we're so glad they're standing with us.`"
    - "Each row (and each section header) carries `reveal-on-scroll` for entrance motion — NOT `reveal-on-scroll-stagger`"
    - "Page has the exact heading hierarchy: 1 × h1 (hero, from 03-01) → 2 × h2 (Bride's Side, Groom's Side) → 16 × h3 (one per member)"
  artifacts:
    - path: "app/(main)/bridal-party/page.tsx"
      provides: "Bridal Party page with all 16 magazine rows + section headers + monogram fallback + data arrays + getInitials helper"
      contains: "const brideSide"
      min_lines: 220
  key_links:
    - from: "brideSide[] and groomSide[] arrays"
      to: ".map((member, i) => row JSX)"
      via: "Array.prototype.map in JSX"
      pattern: "brideSide\\.map|groomSide\\.map"
    - from: "Row JSX"
      to: "isTextRight alternation rule"
      via: "const isTextRight = i % 2 === 1"
      pattern: "i % 2 === 1|isTextRight"
    - from: "Photo column"
      to: "getInitials(name) helper"
      via: "Conditional render when member.photo === null"
      pattern: "member.photo \\?|getInitials\\(member\\.name\\)"
    - from: "Section <section> elements"
      to: "TODO(03-02) markers from Plan 01"
      via: "Replacing the TODO comments with header + row stack"
      pattern: "id=\"bride-side\"|id=\"groom-side\""
---

<objective>
Replace the two empty `<section>` placeholders that Plan 01 left in `app/(main)/bridal-party/page.tsx` with the full content body: a top-of-file `Member` type, a `getInitials` helper, two locked data arrays (`brideSide` and `groomSide` per CONTEXT D-03), and inside each section a section header + an 8-row magazine layout with the alternating text/photo columns and monogram fallback.

Purpose: This is the novel content surface for Phase 3. Every locked decision D-01 through D-11 lands here (D-12 is Plan 03). The magazine row layout, monogram fallback, and `getInitials` helper have no codebase precedent — they are implemented directly from UI-SPEC §Magazine Row Spec and §Monogram Fallback Spec. The Phase 2 carry-forward decomposition isolates this surface so it gets its own context window for quality.

Output: The single file `app/(main)/bridal-party/page.tsx` grows from the Plan 01 scaffold (~80-120 lines) to the full page (~220-280 lines). No other files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bridal-party/03-CONTEXT.md
@.planning/phases/03-bridal-party/03-UI-SPEC.md
@.planning/phases/03-bridal-party/03-PATTERNS.md
@.planning/phases/03-bridal-party/03-01-SUMMARY.md
@AGENTS.md
@app/(main)/bridal-party/page.tsx
@app/(main)/registry/page.tsx

<interfaces>
<!-- The Member type Plan 02 introduces, the helper signature, and the two section header markup blocks. -->

`Member` type (PATTERNS Pattern 2):
```typescript
type Member = {
  name: string;
  role: string;          // ALL CAPS already — `MAID OF HONOR`, `BRIDESMAID`, `BEST MAN`, `GROOMSMAN`
  photo: string | null;  // `/bridal-party/<slug>.jpg` OR null to force monogram fallback
  bio: string;
};
```

`getInitials` (PATTERNS Pattern 3):
```typescript
function getInitials(name: string): string {
  const parts = name.trim().split(/\s+/);
  const first = parts[0]?.[0] ?? "";
  const last = parts.length > 1 ? parts[parts.length - 1][0] : "";
  return (first + last).toUpperCase();
}
```

Locked roster (CONTEXT D-03) — preserve verbatim names AND source order:
- **Bride's Side (8):** Sarah Else (MAID OF HONOR), Emily Asinger, Lindsay Carr, Sarah Horan, Sam Jones, Shannon Robins, Michelle Spencer, Ryan Hindle — all 7 non-honor entries get role `BRIDESMAID`.
- **Groom's Side (8):** Dylan Straffon (BEST MAN), Aaron Sorge, Jack Cardello, Ken Kinoshita, Jon Metz, Ian Adams, Collin DeMatt, Josh Tallman — all 7 non-honor entries get role `GROOMSMAN`.

Slug derivation (D-07): `lowercase-kebab(name)` — replace spaces with hyphens, lowercase. Examples: `sarah-else.jpg`, `dylan-straffon.jpg`, `jack-cardello.jpg`, `ken-kinoshita.jpg`, `michelle-spencer.jpg`. Period.

Placeholder bio (D-11 + UI-SPEC §Copywriting Contract): use the SAME verbatim line for every one of the 16 entries — `A dear friend to both of us — we're so glad they're standing with us.` Each bio field is preceded by `// TODO: replace with real bio` on its own line.

Section header markup (PATTERNS Pattern 6 + UI-SPEC §Section Header Spec):
```jsx
<div className="mb-16 md:mb-24 reveal-on-scroll">
  <span className="font-label text-xs uppercase tracking-[0.4em] text-primary mb-6 block">
    {sectionEyebrow}
  </span>
  <h2 className="font-headline text-4xl md:text-6xl text-on-surface">
    {sectionLeadingWord}{" "}
    <span className="italic font-light text-primary/80">{sectionAccentWord}</span>
  </h2>
</div>
```

Section content map:
| Section | Eyebrow | Leading word | Italic accent word |
|---------|---------|--------------|--------------------|
| Bride's Side | `THE BRIDE'S SIDE` | `Bride's` | `Side` |
| Groom's Side | `THE GROOM'S SIDE` | `Groom's` | `Side` |

Row markup (PATTERNS Pattern 8 + UI-SPEC §Magazine Row Spec):
```jsx
<div className="grid grid-cols-1 md:grid-cols-12 gap-y-8 md:gap-x-12 lg:gap-x-16 items-center reveal-on-scroll">
  <div className={`md:col-span-7 ${isTextRight ? "md:col-start-6" : ""}`}>
    <span className="font-label text-xs uppercase tracking-[0.4em] text-primary mb-4 block">{member.role}</span>
    <h3 className="font-headline text-2xl md:text-4xl text-on-surface mb-4">{member.name}</h3>
    <p className="text-on-surface-variant text-lg font-light leading-relaxed">{member.bio}</p>
  </div>
  <div className={`md:col-span-5 ${isTextRight ? "md:col-start-1 md:row-start-1" : ""}`}>
    <div className="aspect-[4/5] bg-surface-variant/50 overflow-hidden relative">
      {/* member.photo ? <img/> : monogram block (see Monogram Fallback Spec) */}
    </div>
  </div>
</div>
```

Photo-or-fallback markup (PATTERNS Pattern 7):
```jsx
{member.photo ? (
  <>
    {/* eslint-disable-next-line @next/next/no-img-element */}
    <img
      alt={`Portrait of ${member.name}`}
      className="w-full h-full object-cover"
      src={member.photo}
    />
  </>
) : (
  <div className="absolute inset-0 flex items-center justify-center bg-surface-container" aria-hidden="true">
    <span className="font-headline italic text-5xl md:text-7xl text-primary">
      {getInitials(member.name)}
    </span>
  </div>
)}
```

Row stack wrapper (so the 8 rows are vertically spaced):
```jsx
<div className="flex flex-col gap-y-16 md:gap-y-24 lg:gap-y-32">
  {brideSide.map((member, i) => { /* row JSX above */ })}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add type, helper, and two locked data arrays at top of file</name>
  <files>app/(main)/bridal-party/page.tsx</files>
  <read_first>
    - .planning/phases/03-bridal-party/03-CONTEXT.md D-03 (full 16-name roster, verbatim) + D-04 (role label rules) + D-07 (photo path convention) + D-09, D-10, D-11 (bio voice, length, placeholder rule)
    - .planning/phases/03-bridal-party/03-UI-SPEC.md §"Copywriting Contract" → "Per-Member Role Labels" (lines 356-374) — every locked role label string, member-by-member
    - .planning/phases/03-bridal-party/03-UI-SPEC.md §"Copywriting Contract" → "Placeholder Bios" (lines 377-394) — verbatim placeholder line + uniformity requirement
    - .planning/phases/03-bridal-party/03-PATTERNS.md §"Pattern 2 — Inline data arrays" (lines 39-90) + §"Pattern 3 — getInitials() helper" (lines 92-104)
    - app/(main)/bridal-party/page.tsx (current state from Plan 01 — verify the file still has zero data arrays before editing)
    - app/(main)/registry/page.tsx (lines 8-39 — the analog inline data array pattern; note the `// TODO: replace with real <field>` comment is placed on the line IMMEDIATELY ABOVE the placeholder field, not on the same line)
  </read_first>
  <action>
    Edit `app/(main)/bridal-party/page.tsx` to insert four blocks between the `export const metadata` block and the `export default function BridalPartyPage()` declaration, in this top-to-bottom order:

    1. **The `Member` type alias** (4 lines):
       ```ts
       type Member = {
         name: string;
         role: string;
         photo: string | null;
         bio: string;
       };
       ```

    2. **The `getInitials` pure helper function** — 6 lines, takes a `name: string`, returns the first letter of the first word and the first letter of the last word concatenated and uppercased. Per UI-SPEC §Monogram Fallback Spec, this lives inline at the top of `page.tsx`, is NOT exported, and is NOT extracted to a separate file. Use the implementation in PATTERNS Pattern 3 verbatim. Empty-string guards (`?.[0] ?? ""`) handle the edge case where `name` is empty or single-word (not used by the current roster but defensive against future single-name additions like a stage name).

    3. **The `brideSide: Member[]` array** — exactly 8 entries in this verbatim source order (per D-03):
       - Sarah Else → role `MAID OF HONOR`, photo `/bridal-party/sarah-else.jpg`, bio = the placeholder line below
       - Emily Asinger → role `BRIDESMAID`, photo `/bridal-party/emily-asinger.jpg`, bio = placeholder
       - Lindsay Carr → role `BRIDESMAID`, photo `/bridal-party/lindsay-carr.jpg`, bio = placeholder
       - Sarah Horan → role `BRIDESMAID`, photo `/bridal-party/sarah-horan.jpg`, bio = placeholder
       - Sam Jones → role `BRIDESMAID`, photo `/bridal-party/sam-jones.jpg`, bio = placeholder
       - Shannon Robins → role `BRIDESMAID`, photo `/bridal-party/shannon-robins.jpg`, bio = placeholder
       - Michelle Spencer → role `BRIDESMAID`, photo `/bridal-party/michelle-spencer.jpg`, bio = placeholder
       - Ryan Hindle → role `BRIDESMAID`, photo `/bridal-party/ryan-hindle.jpg`, bio = placeholder

       Each `bio:` line is IMMEDIATELY PRECEDED by a `// TODO: replace with real bio` comment on its own line (matches the Registry analog where the `// TODO: replace with real registry URL` comment sits one line above the placeholder field). Bio value is the verbatim string `A dear friend to both of us — we're so glad they're standing with us.` (the em-dash is a real `—` character, not `--`; the apostrophe is a real `'` smart apostrophe is NOT required — match the UI-SPEC source which uses straight `'`).

    4. **The `groomSide: Member[]` array** — exactly 8 entries in this verbatim source order (per D-03):
       - Dylan Straffon → role `BEST MAN`, photo `/bridal-party/dylan-straffon.jpg`, bio = placeholder
       - Aaron Sorge → role `GROOMSMAN`, photo `/bridal-party/aaron-sorge.jpg`, bio = placeholder
       - Jack Cardello → role `GROOMSMAN`, photo `/bridal-party/jack-cardello.jpg`, bio = placeholder
       - Ken Kinoshita → role `GROOMSMAN`, photo `/bridal-party/ken-kinoshita.jpg`, bio = placeholder
       - Jon Metz → role `GROOMSMAN`, photo `/bridal-party/jon-metz.jpg`, bio = placeholder
       - Ian Adams → role `GROOMSMAN`, photo `/bridal-party/ian-adams.jpg`, bio = placeholder
       - Collin DeMatt → role `GROOMSMAN`, photo `/bridal-party/collin-dematt.jpg`, bio = placeholder
       - Josh Tallman → role `GROOMSMAN`, photo `/bridal-party/josh-tallman.jpg`, bio = placeholder

       Same TODO-comment-above-bio rule as `brideSide`. Same verbatim placeholder string in every bio. The uniformity is the signal (per UI-SPEC §Copywriting Contract Placeholder Bios rationale).

    Photo path slug derivation (D-07): every `photo` value is `/bridal-party/<lowercase-kebab(name)>.jpg`. Verify slugs are computed manually here (not algorithmically) and match the lowercase-kebab convention exactly — capital letters in surnames like `DeMatt` and `Kinoshita` lowercase to `dematt` and `kinoshita`. NO smart-case preservation, NO underscores, NO ampersands.

    Do NOT in this task:
    - Replace the `<section>` placeholder TODO comments yet — Task 2 handles the section content body.
    - Touch the hero, the metadata, or the `<main>` wrapper from Plan 01.
    - Compute slugs at runtime (e.g., a `slug(name)` helper). The 16 paths are hardcoded strings — simpler, greppable, no surprises.
    - Set any `photo` field to `null`. The default state is "photo file expected"; the monogram fallback is exercised at runtime only if the actual `.jpg` file is missing from `/public/bridal-party/`. The fallback rendering logic is added in Task 2; the `null` case is for future use (e.g., if Tyler explicitly opts a member out of having a portrait).
    - Add real bios. D-11 explicitly defers real bios to Tyler at handoff; the 16 uniform placeholders are the spec.
    - Vary the placeholder string between entries. All 16 bios are literally the same string — that is the spec (UI-SPEC: "16 different placeholder variations would mask 'still placeholder' status during review").
    - Add a `slug` field to the `Member` type. UI-SPEC only requires `name`, `role`, `photo`, `bio`.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; grep -c "// TODO: replace with real bio" app/\(main\)/bridal-party/page.tsx | grep -qx "16" &amp;&amp; grep -c "BRIDESMAID" app/\(main\)/bridal-party/page.tsx | grep -qx "7" &amp;&amp; grep -c "GROOMSMAN" app/\(main\)/bridal-party/page.tsx | grep -qx "7" &amp;&amp; grep -q "MAID OF HONOR" app/\(main\)/bridal-party/page.tsx &amp;&amp; grep -q "BEST MAN" app/\(main\)/bridal-party/page.tsx &amp;&amp; grep -q "function getInitials" app/\(main\)/bridal-party/page.tsx &amp;&amp; grep -q "type Member" app/\(main\)/bridal-party/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly one `type Member = { ... }` block whose fields are `name: string`, `role: string`, `photo: string | null`, `bio: string` (no additional fields, no `slug`, no `id`)
    - File contains exactly one `function getInitials(name: string): string` declaration that returns a two-letter uppercase string; the helper is NOT exported
    - File contains exactly one `const brideSide` declaration AND exactly one `const groomSide` declaration; both are typed as `Member[]`
    - `brideSide` array length is exactly 8 (verify by `grep -c "name:" ` on the brideSide block, or by counting `{` opening braces between `const brideSide` and the closing `];`)
    - `groomSide` array length is exactly 8
    - The string `MAID OF HONOR` appears exactly once in the file
    - The string `BEST MAN` appears exactly once in the file
    - The string `BRIDESMAID` appears exactly 7 times (one for each non-honor Bride's Side member)
    - The string `GROOMSMAN` appears exactly 7 times (one for each non-honor Groom's Side member)
    - The comment `// TODO: replace with real bio` appears exactly 16 times
    - The placeholder bio string `A dear friend to both of us — we're so glad they're standing with us.` appears exactly 16 times verbatim
    - Verbatim names appear in source order — verify with `grep -n '"Sarah Else"\|"Emily Asinger"\|"Lindsay Carr"\|"Sarah Horan"\|"Sam Jones"\|"Shannon Robins"\|"Michelle Spencer"\|"Ryan Hindle"' app/\(main\)/bridal-party/page.tsx | head -8` returns lines in that exact order; same check for the Groom's Side roster starting with Dylan Straffon
    - All 16 `photo` paths follow the pattern `/bridal-party/<lowercase-kebab>.jpg` (verify no uppercase letters in any of the 16 paths via `grep -oE '"/bridal-party/[^"]+\.jpg"' app/\(main\)/bridal-party/page.tsx | grep -E '[A-Z]'` returning empty)
    - The slugs for `DeMatt` → `dematt` and `Kinoshita` → `kinoshita` are present (verify `grep -q 'collin-dematt.jpg' app/(main)/bridal-party/page.tsx && grep -q 'ken-kinoshita.jpg' app/(main)/bridal-party/page.tsx`)
    - File does NOT contain any other variation of the placeholder bio string (verify uniformity)
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - Page at `/bridal-party` still renders the hero unchanged from Plan 01 (the section bodies are still empty; the data arrays are declared but not yet consumed in JSX)
  </acceptance_criteria>
  <done>The file now contains the `Member` type, the `getInitials` helper, and the two locked-roster arrays totalling 16 members. Lint + build are green. The arrays are declared but not yet rendered — Task 2 wires them into the section bodies.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render Bride's Side and Groom's Side sections — headers + 8 magazine rows each + monogram fallback</name>
  <files>app/(main)/bridal-party/page.tsx</files>
  <read_first>
    - .planning/phases/03-bridal-party/03-UI-SPEC.md §"Section Header Spec" (lines 213-241) — wrapper class, eyebrow + h2 markup, content map
    - .planning/phases/03-bridal-party/03-UI-SPEC.md §"Magazine Row Spec" (lines 244-314) — row wrapper, text column, photo column, alternation rule, mobile collapse
    - .planning/phases/03-bridal-party/03-UI-SPEC.md §"Monogram Fallback Spec" (lines 318-335) — container, background, initials class, aria-hidden rule
    - .planning/phases/03-bridal-party/03-UI-SPEC.md §"Acceptance Criteria" sections C, D, E, F, G, H3, H4, I (the verifiable criteria this task must satisfy)
    - .planning/phases/03-bridal-party/03-PATTERNS.md §"Pattern 6 — Section header" (lines 175-194), §"Pattern 7 — Image-with-fallback" (lines 196-248), §"Pattern 8 — Magazine row layout" (lines 250-294), §"Pattern 9 — Row reveal motion" (lines 296-302)
    - .planning/phases/03-bridal-party/03-PATTERNS.md §"Patterns to NOT Copy from Analog" (lines 428-445) — explicit forbid list: no `group-hover:*`, no `editorial-underline`, no `target="_blank"`, no overlay div, no `transition-transform`, no `reveal-on-scroll-stagger`, no `bg-[radial-gradient...]`, no `mb-8` on photo container, no framing block
    - app/(main)/bridal-party/page.tsx (current state after Task 1 — the two `<section>` placeholders still have `{/* TODO(03-02): ... */}` markers)
    - app/(main)/registry/page.tsx lines 101-109 (section header analog) and 111, 121-132 (image container analog — note the parts to DROP per PATTERNS forbid list)
  </read_first>
  <action>
    Replace the two `{/* TODO(03-02): ... */}` comment markers inside the Bride's Side and Groom's Side `<section>` elements (placed by Plan 01) with the full content for each section.

    Each section's content body is structured as:
    1. A section header block.
    2. A row stack wrapper containing 8 magazine rows produced via `.map`.

    **Section header (apply to both sections, swapping only eyebrow + heading words per the content map):**

    Replace the TODO marker inside `<div className="max-w-[1440px] mx-auto px-6 md:px-12">` with two consecutive children: the header div, then the row stack div.

    Header div per UI-SPEC §Section Header Spec:
    - Outer wrapper: `<div className="mb-16 md:mb-24 reveal-on-scroll">`
    - Inside, an eyebrow `<span className="font-label text-xs uppercase tracking-[0.4em] text-primary mb-6 block">` whose text is the section eyebrow string.
    - Then an `<h2 className="font-headline text-4xl md:text-6xl text-on-surface">` whose content is the leading word, a `{" "}` space token, and an italic-accent `<span className="italic font-light text-primary/80">` wrapping the accent word.

    Content map (verbatim):
    - **Bride's Side header:** eyebrow `THE BRIDE'S SIDE`, leading word `Bride's`, accent word `Side`
    - **Groom's Side header:** eyebrow `THE GROOM'S SIDE`, leading word `Groom's`, accent word `Side`

    Note both headers use the same accent word `Side` — that is the spec, not a typo (UI-SPEC §Section Header Spec content table).

    **Row stack wrapper:**

    Immediately after the header div, render:
    ```jsx
    <div className="flex flex-col gap-y-16 md:gap-y-24 lg:gap-y-32">
      {brideSide.map((member, i) => { ...row... })}
    </div>
    ```

    (Use `groomSide.map` in the second section.) The `key` for each mapped row is `member.name` (names are unique within and across both arrays per the locked roster).

    **Per-row JSX (apply per UI-SPEC §Magazine Row Spec):**

    Inside the `.map` callback, compute `const isTextRight = i % 2 === 1;` then return a single `<div>` row.

    Row wrapper className: `grid grid-cols-1 md:grid-cols-12 gap-y-8 md:gap-x-12 lg:gap-x-16 items-center reveal-on-scroll`. Use `reveal-on-scroll` — NOT `reveal-on-scroll-stagger` (UI-SPEC explicitly forbids stagger on rows because the stagger CSS only defines `:nth-child(2)` and `:nth-child(3)` offsets, so rows 4-16 would silently lose the effect; also "staggering full-width rows feels chaotic").

    Inside the row wrapper, two children in this DOM source order (text first, photo second — this is what makes mobile collapse text-above-photo regardless of desktop alternation):

    Text column: `<div className={\`md:col-span-7 ${isTextRight ? "md:col-start-6" : ""}\`}>` containing:
    - `<span className="font-label text-xs uppercase tracking-[0.4em] text-primary mb-4 block">{member.role}</span>` — note `mb-4` (NOT `mb-6` — the per-row role eyebrow uses tighter spacing than the section header eyebrow per UI-SPEC §Typography Small tier and PATTERNS §Shared Patterns "Eyebrow label" note)
    - `<h3 className="font-headline text-2xl md:text-4xl text-on-surface mb-4">{member.name}</h3>`
    - `<p className="text-on-surface-variant text-lg font-light leading-relaxed">{member.bio}</p>`

    Photo column: `<div className={\`md:col-span-5 ${isTextRight ? "md:col-start-1 md:row-start-1" : ""}\`}>` containing a `<div className="aspect-[4/5] bg-surface-variant/50 overflow-hidden relative">` wrapper, inside which the conditional render decides photo vs. monogram fallback:

    `{member.photo ? (<img>...</img>) : (<div>monogram</div>)}`

    The `<img>` branch:
    - On the line IMMEDIATELY ABOVE the `<img>` tag, place the inline comment `{/* eslint-disable-next-line @next/next/no-img-element */}` — this matches the site-wide convention (every shipped page has this directive). D-08 supersedes the ROADMAP's `next/image` language.
    - The `<img>` itself has `alt={\`Portrait of ${member.name}\`}`, `className="w-full h-full object-cover"`, `src={member.photo}`. NO `transition-transform`, NO `duration-*`, NO `scale-*`, NO `group-hover:*` — those are dropped from the Registry analog because rows are static editorial per UI-SPEC §Magazine Row Spec ("No hover effects on rows.").
    - Wrap the `<img>` + eslint-disable comment in a React fragment `<>...</>` so the conditional ternary has a single child per branch (matches PATTERNS Pattern 7).

    The monogram fallback branch:
    - `<div className="absolute inset-0 flex items-center justify-center bg-surface-container" aria-hidden="true">` wrapping a `<span className="font-headline italic text-5xl md:text-7xl text-primary">{getInitials(member.name)}</span>`.
    - The wrapper div carries `aria-hidden="true"` because the visible `<h3>` name in the text column already announces the member's identity to assistive tech; the monogram is purely decorative.
    - The initials span does NOT need its own `aria-hidden="true"` if the wrapping div has it (aria-hidden cascades). UI-SPEC §Accessibility I4 lists the initials span as carrying `aria-hidden="true"` — adding it to the span as well is harmless and matches the spec exactly; do it for belt-and-braces explicitness.

    **Alternation invariant:** with `isTextRight = i % 2 === 1`:
    - i=0 (Sarah Else / Dylan Straffon): isTextRight=false → text in left 7 cols (default `md:col-span-7`), photo in right 5 cols (default `md:col-span-5`).
    - i=1: isTextRight=true → text uses `md:col-span-7 md:col-start-6` (right side), photo uses `md:col-span-5 md:col-start-1 md:row-start-1` (left side, same row).
    - i=2: isTextRight=false → text left, photo right.
    - …repeats through i=7.

    Both sections start at i=0 → both honor attendants (Sarah Else, Dylan Straffon) render text-left/photo-right, visually anchoring the top of each section in the same position (per UI-SPEC §Magazine Row Spec alternation rule).

    **Forbid list (anti-patterns from the analog — NONE of these may appear in the new section bodies; many are present in `registry/page.tsx` but must not be ported per PATTERNS §"Patterns to NOT Copy from Analog"):**
    - No `<a>` wrappers around rows. Rows are not clickable.
    - No `group cursor-pointer block` className. No `<a>` means no group + cursor.
    - No `target="_blank"`, no `rel="noopener noreferrer"`, no `aria-label="opens in new tab"`. There are zero outbound links on this page (verified in `<threat_model>` below).
    - No `editorial-underline` span. No "Visit X" CTA.
    - No `bg-background/20 group-hover:bg-transparent` overlay div inside the photo container.
    - No `transition-transform`, `scale-*`, `group-hover:*` on the `<img>`.
    - No `group-hover:text-primary` on the `<h3>`.
    - No `reveal-on-scroll-stagger` on the row stack or any wrapper. Each row's `reveal-on-scroll` is on its own grid wrapper, individually.
    - No `bg-[radial-gradient(...)]` overlay on either section.
    - No `mb-8` on the photo container (the row's `gap-y-8` on the grid wrapper handles mobile vertical spacing between text and photo).
    - No framing block above either section. The hero subtitle carries the framing role for Phase 3.
    - No section sub-nav (`id="..."` + sticky pill bar from `things-to-do/page.tsx`). The two `id` attributes Plan 01 placed on the sections are fine to keep for dev-tools navigation but no sticky nav consumes them.
    - No `next/image` import. Plain `<img>` only.

    Source-order discipline (D-02): do NOT alphabetize. Do NOT shuffle. The `.map` iterates the array in declaration order. Verify this by inspecting the rendered DOM: the first `<h3>` in the Bride's Side section reads `Sarah Else`, the second reads `Emily Asinger`, …; the first `<h3>` in the Groom's Side section reads `Dylan Straffon`, the second reads `Aaron Sorge`, ….
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; grep -c "<h2" app/\(main\)/bridal-party/page.tsx | grep -qx "2" &amp;&amp; grep -c "<h3" app/\(main\)/bridal-party/page.tsx | grep -qx "16" &amp;&amp; grep -c "aspect-\[4/5\]" app/\(main\)/bridal-party/page.tsx | grep -qx "1" &amp;&amp; grep -c "eslint-disable-next-line @next/next/no-img-element" app/\(main\)/bridal-party/page.tsx | grep -qx "1" &amp;&amp; grep -c "reveal-on-scroll" app/\(main\)/bridal-party/page.tsx | grep -qx "3" &amp;&amp; ! grep -q "reveal-on-scroll-stagger" app/\(main\)/bridal-party/page.tsx &amp;&amp; ! grep -q "target=\"_blank\"" app/\(main\)/bridal-party/page.tsx &amp;&amp; ! grep -q "group-hover" app/\(main\)/bridal-party/page.tsx &amp;&amp; ! grep -q "editorial-underline" app/\(main\)/bridal-party/page.tsx &amp;&amp; ! grep -q "next/image" app/\(main\)/bridal-party/page.tsx &amp;&amp; grep -q "i % 2 === 1" app/\(main\)/bridal-party/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly two `<h2` occurrences (Bride's Side and Groom's Side section headings)
    - File contains exactly 16 `<h3` occurrences (one per mapped member row — verified by the `<h3>` literal in the row JSX × 16 array entries across both sections)
    - File contains exactly one `<h1` occurrence (the hero from Plan 01 — unchanged)
    - File contains exactly one `aspect-[4/5]` className string — the photo container is templated and `.map`'d, so the literal class string appears once in source even though it renders 16 times. (Confirms only ONE photo container template; no accidental duplicated row markup.)
    - File contains exactly one `eslint-disable-next-line @next/next/no-img-element` directive — single `<img>` template inside the conditional renders 16 times via `.map`
    - File contains exactly one `<img` literal — confirms the row JSX is templated (NOT 16 hand-written rows)
    - File contains exactly one `.map(` followed by a row, applied twice (`brideSide.map(` and `groomSide.map(`) — verify with `grep -c '\\.map((member' app/(main)/bridal-party/page.tsx` returning 2
    - File contains the exact substring `i % 2 === 1` (the alternation rule)
    - The `reveal-on-scroll` className appears exactly 3 times (once on each of two section header wrappers + once on the row grid wrapper template that maps to 16 row instances) — and the file contains ZERO occurrences of `reveal-on-scroll-stagger`
    - The forbid-list patterns are ABSENT from the file: zero occurrences of `target="_blank"`, `rel="noopener"`, `group-hover`, `editorial-underline`, `next/image`, `transition-transform`, `scale-105`, `scale-110`, `cursor-pointer`, `bg-background/20`, `bg-[radial-gradient`, `mb-8 overflow-hidden`
    - File contains both `THE BRIDE'S SIDE` and `THE GROOM'S SIDE` as eyebrow text
    - Both `<h2>` elements contain a child `<span className="italic font-light text-primary/80">Side</span>` (verify: `grep -c 'italic font-light text-primary/80">Side</span>' app/(main)/bridal-party/page.tsx` returns 2)
    - Both section header wrappers carry `mb-16 md:mb-24 reveal-on-scroll`
    - The photo column template includes the conditional `member.photo ? (` ternary AND the fallback `<div className="absolute inset-0 flex items-center justify-center bg-surface-container"` block with `getInitials(member.name)` inside a `font-headline italic text-5xl md:text-7xl text-primary` span
    - The monogram fallback wrapper div carries `aria-hidden="true"`
    - Page heading order verified by curl + grep: `curl -s http://localhost:3000/bridal-party | grep -oE '<h[123][^>]*>[^<]+' | head -25` returns h1 first, then h2 Bride's Side, then 8 × h3 (Sarah Else, Emily Asinger, Lindsay Carr, Sarah Horan, Sam Jones, Shannon Robins, Michelle Spencer, Ryan Hindle), then h2 Groom's Side, then 8 × h3 (Dylan Straffon, Aaron Sorge, Jack Cardello, Ken Kinoshita, Jon Metz, Ian Adams, Collin DeMatt, Josh Tallman) — exactly that order
    - `npm run lint` exits 0; specifically no warnings about `@next/next/no-img-element` because the single inline `eslint-disable-next-line` directive covers the one templated `<img>`
    - `npm run build` exits 0; `/bridal-party` listed as static (○) route
    - Visiting `/bridal-party` on dev server renders both section headers and all 16 rows; in the absence of any `.jpg` files in `/public/bridal-party/`, every row's photo container renders the monogram fallback (warm-gold initials on dark surface) — confirms the fallback path works end-to-end
    - Resizing the browser at `<768px` collapses every row to single-column with text above photo (verify on at least Sarah Else and Aaron Sorge rows — Sarah is i=0 default-position, Aaron is i=1 alternated-position; both should collapse text-above-photo on mobile)
  </acceptance_criteria>
  <done>Both `<section>` placeholders are replaced with full content: section header + row stack of 8 magazine rows each. The page renders the heading hierarchy h1 → 2×h2 → 16×h3, alternates rows correctly on desktop, collapses to text-above-photo on mobile, and uses the monogram fallback for every row (since no portrait JPGs exist yet). All 12 of CONTEXT D-01 through D-11 are now enacted in code. D-12 (navbar) remains for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan adds hardcoded literal data arrays + templated JSX. No user input, no outbound links, no SDK imports, no data fetching, no `dangerouslySetInnerHTML`, no new dependencies. Every rendered string flows from compile-time literals through React's text-node escaping. The 16 `<img>` `src` values are hardcoded `/public/bridal-party/*.jpg` paths under same-origin control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-XSS | Tampering | Member name / role / bio rendered as JSX text content | accept | All 16 names, roles, and bios are hardcoded literals in the source file. React text-node escaping covers ASVS L1 §5.3 against any future content edit. No `dangerouslySetInnerHTML`, no untrusted template interpolation. |
| T-03-02-IMG | Tampering | `<img src={member.photo}>` and `alt={\`Portrait of ${member.name}\`}` interpolation | accept | The `member.photo` value is a hardcoded `/bridal-party/<slug>.jpg` path. The `alt` template literal interpolates `member.name`, which is also a hardcoded literal. No user-controlled input reaches either attribute. The missing-image fallback (monogram) is purely client-side CSS — no network failure surface. |
| T-03-02-PII | Information Disclosure | Placeholder bios | accept | The 16 uniform placeholder bios contain no PII, no contact info, no addresses. Real bios at Tyler's handoff are first-person from the couple, do not include third-party PII per CONTEXT §Deferred Ideas (contact info explicitly out of scope), and remain client-public content appropriate for a wedding RSVP site. |
| T-03-02-DEP | Tampering | npm packages | accept | Plan adds zero new dependencies. No install steps. RESEARCH.md package legitimacy audit is not required (gate fires only on install tasks). |
| T-03-02-OUT | (no category) | Outbound links | accept (none exist) | This plan creates zero `<a target="_blank">` elements and zero outbound `<a>` tags. No tabnabbing surface, no `rel="noopener noreferrer"` requirement, no T-02-01 equivalent (the Phase 2 tabnabbing threat) for this phase. |
| T-03-02-A11Y | (no category — quality) | Heading hierarchy + alt text | mitigate via spec | UI-SPEC §Accessibility I1-I7 enforces exactly 1×h1, 2×h2, 16×h3 with descriptive alt text on every `<img>`. Acceptance criteria gates this with explicit counts. Reduced-motion is handled globally in `app/globals.css:613-621` — no per-page override. |
</threat_model>

<verification>
- `npm run lint` exits 0 after both tasks
- `npm run build` exits 0 after both tasks; `/bridal-party` is a static (○) route
- `git diff app/(main)/bridal-party/page.tsx` shows additions only (no deletions to hero, metadata, or the `<main>` wrapper Plan 01 created); the two `{/* TODO(03-02): ... */}` markers ARE replaced (they no longer appear in the file)
- `git diff` shows NO changes outside `app/(main)/bridal-party/page.tsx`
- Heading hierarchy curl+grep verification passes (h1 → h2 BS → 8×h3 → h2 GS → 8×h3 in that exact order)
- All 12 of D-01 through D-11 are enacted (D-08 was foundationally enabled in Plan 01; D-12 remains for Plan 03)
- The full forbid list (`group-hover`, `target="_blank"`, `editorial-underline`, `next/image`, `transition-transform`, `scale-*`, `reveal-on-scroll-stagger`, `bg-[radial-gradient`, `mb-8 overflow-hidden`, `cursor-pointer`) is absent from the file
- 16 placeholder-bio TODOs are present (one per member) and remain present at end-of-plan as Tyler's handoff list
</verification>

<success_criteria>
- One file modified: `app/(main)/bridal-party/page.tsx` (additions: type, helper, two data arrays, two section bodies)
- 11 of 12 phase requirements satisfied (D-01 through D-11); D-12 lives in Plan 03
- All 16 rows render with correct alternation on desktop, correct stacking on mobile
- Monogram fallback works end-to-end (verifiable now because no portrait JPGs exist yet)
- Lint + build green
- UI-SPEC §Acceptance Criteria sections C (all), D (all), E (all), G (all), H3, H4, I1-I7 are visibly satisfied (H1, H2, B*, A1, A2 carried from Plan 01; F1-F4, A3-A6 verified here; A7, A8, B*, F*, H5 final verification lands in Plan 03's smoke checklist)
</success_criteria>

<output>
Create `.planning/phases/03-bridal-party/03-02-SUMMARY.md` when done. Include:
- D-IDs satisfied (D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11)
- Files modified (just `app/(main)/bridal-party/page.tsx`)
- Confirmation that lint, build, and TypeScript all exit 0
- Heading hierarchy verification result (the curl+grep output truncated to 25 lines, confirming the exact order)
- Confirmation that the forbid list is empty in the file (each forbidden grep returns no matches)
- The 16-item TODO handoff list (one entry per member: `member name + bio placeholder reminder`) — this is Tyler's checklist at handoff to replace each placeholder with a real bio
- Reminder that 16 portrait JPGs are also pending at handoff; the monogram fallback covers the gap until Tyler drops files into `/public/bridal-party/`
- Handoff note to Plan 03: page is content-complete except for the navbar wiring; smoke checklist in Plan 03 walks every UI-SPEC §Acceptance Criteria item
</output>
</content>
