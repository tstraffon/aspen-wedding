---
phase: 04-guest-list-and-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-guest-list-and-api/SCHEMA.sql
  - .planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md
autonomous: false
requirements:
  - GUEST-01
  - MEAL-03
user_setup:
  - service: supabase
    why: "Schema migration must be applied to the live Supabase project via Studio SQL editor (no Supabase CLI integration in this repo; matches v0.1 pattern)."
    env_vars: []
    dashboard_config:
      - task: "Apply SCHEMA.sql migration"
        location: "Supabase Studio → SQL Editor → New Query → paste SCHEMA.sql → Run"
      - task: "Verify table shape post-apply"
        location: "Supabase Studio → Table Editor → guests (new) + rsvps (altered)"
      - task: "Capture dev-seed guest UUIDs into DEV-SEED-IDS.md"
        location: "Supabase Studio → SQL Editor → SELECT id, household_id, full_name FROM public.guests ORDER BY household_id, full_name; → paste output into .planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md"

must_haves:
  truths:
    - "A `guests` table exists in public schema with columns id (uuid pk), household_id (uuid, indexed), full_name (text, indexed via lower(trim(...))), created_at"
    - "The `rsvps` table has new nullable columns household_id (uuid), guest_id (uuid), meal_choice (text)"
    - "The `rsvps` table no longer has a `guest_count` column"
    - "A partial unique index `rsvps_guest_id_uniq` exists on rsvps(guest_id) WHERE guest_id IS NOT NULL — enabling upsert by guest_id while leaving v0.1 NULL rows untouched"
    - "A SECURITY-DEFINER-free, STABLE SQL function `public.lookup_guest_by_name(p_name text)` exists, executes `SELECT id, household_id, full_name FROM public.guests WHERE lower(trim(full_name)) = lower(trim(p_name)) LIMIT 1`, and has EXECUTE granted to anon — used by Plan 04-02's lookup route (W-2 fix)"
    - "The anon role has SELECT on guests; the anon role still has INSERT,UPDATE on rsvps and still cannot SELECT rsvps (Phase 1 grant intact)"
    - "The SCHEMA.sql file is tracked in git as the canonical source of the v0.2 migration"
    - "At least one dev-seed guest row exists in the live `guests` table so Wave 2 routes have something to lookup"
    - "`gen_random_uuid()` actually fires as the default for `public.guests.id` on this Supabase project (no missing `pgcrypto` extension); proven by an INSERT...RETURNING check during the apply checkpoint"
    - "`.planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md` exists and contains a table of full_name | guest_id | household_id for the dev seed households, captured by the Studio checkpoint. This file is the source of truth for Wave 2's request payloads, decoupling Plan 04-03 from Plan 04-02 at runtime."
  artifacts:
    - path: ".planning/phases/04-guest-list-and-api/SCHEMA.sql"
      provides: "Canonical v0.2 schema migration (CREATE TABLE guests, ALTER rsvps, indexes, GRANTs, lookup_guest_by_name() function, dev seed, Tyler-helper SQL)"
      contains: "CREATE TABLE IF NOT EXISTS public.guests"
      min_lines: 70
    - path: ".planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md"
      provides: "Captured UUIDs of the dev seed guests + their household_ids — pasted from Studio after the migration applies. Wave 2 plans (04-02, 04-03) read this file to construct curl payloads without depending on each other at runtime."
      contains: "full_name | guest_id | household_id"
    - path: "public.guests (Supabase)"
      provides: "Guest list storage with household grouping; anon SELECT allowed for the lookup gate"
      contains: "id uuid pk, household_id uuid, full_name text, created_at timestamptz"
    - path: "public.rsvps (Supabase, altered)"
      provides: "RSVP storage extended with v0.2 columns; v0.1 backward-compat via nullable additions"
      contains: "household_id uuid NULL, guest_id uuid NULL, meal_choice text NULL; no guest_count column"
    - path: "public.lookup_guest_by_name (Supabase function)"
      provides: "STABLE SQL function used by Plan 04-02 for the case-insensitive trim match against guests_full_name_lower_idx (commits to W-2's single implementation path — no .ilike fallback)"
      contains: "SELECT id, household_id, full_name FROM public.guests WHERE lower(trim(full_name)) = lower(trim(p_name)) LIMIT 1"
  key_links:
    - from: ".planning/phases/04-guest-list-and-api/SCHEMA.sql"
      to: "public.guests + public.rsvps (Supabase)"
      via: "manual paste into Supabase Studio SQL Editor (D-17 / Phase 1 pattern)"
      pattern: "CREATE TABLE.*guests"
    - from: "anon role"
      to: "public.guests"
      via: "GRANT SELECT (D-07)"
      pattern: "GRANT SELECT ON public\\.guests TO anon"
    - from: "anon role"
      to: "public.lookup_guest_by_name(text)"
      via: "GRANT EXECUTE — Plan 04-02 calls supabase.rpc('lookup_guest_by_name', { p_name })"
      pattern: "GRANT EXECUTE ON FUNCTION public\\.lookup_guest_by_name"
    - from: "rsvps_guest_id_uniq partial unique index"
      to: "Wave 2 submit upsert via onConflict:guest_id (D-15)"
      via: "CREATE UNIQUE INDEX ... WHERE guest_id IS NOT NULL"
      pattern: "rsvps_guest_id_uniq"
    - from: "DEV-SEED-IDS.md"
      to: "Plans 04-02 + 04-03 curl smokes"
      via: "Wave 2 reads the file to construct request payloads — removes 04-03's runtime dependency on 04-02 (W-2 / B-2 decoupling)"
      pattern: "DEV-SEED-IDS\\.md"
---

<objective>
Produce the canonical v0.2 schema migration as a tracked SQL artifact (`SCHEMA.sql`), apply it to the live Supabase project via Studio, verify the resulting schema shape, and capture the dev-seed guest UUIDs into a tracked markdown file (`DEV-SEED-IDS.md`) so Wave 2 plans can construct curl payloads without depending on each other at runtime. The SCHEMA.sql artifact is the single source of truth for the migration — version-controlled, reviewable, re-runnable. Apply is gated by a blocking human-verify checkpoint because Supabase Studio has no CLI integration in this repo (matches the Phase 1 pattern).

Purpose: Every downstream Wave-2 task (lookup, submit) depends on the live database having the new `guests` table, altered `rsvps` columns, partial unique index for upsert, GRANT layer for anon SELECT, and the `lookup_guest_by_name()` SQL function that Plan 04-02 calls via `.rpc()` (W-2 fix — commits to a single implementation path, no `.ilike` fallback). Phase 5 (UI) calls those endpoints. Phase 7 (handoff) lifts the dev-seed SQL and Tyler-helper SQL into the runbook.

Output:
- `.planning/phases/04-guest-list-and-api/SCHEMA.sql` (new, tracked) — full migration including dev seed, Tyler-helper snippet, and `lookup_guest_by_name()` function
- `.planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md` (new, tracked) — captured UUIDs of the 4 dev seed guests + their household_ids
- Live Supabase: `public.guests` table created; `public.rsvps` altered (add household_id/guest_id/meal_choice, drop guest_count); partial unique index `rsvps_guest_id_uniq`; `public.lookup_guest_by_name(p_name text)` function created and GRANTed to anon; GRANT SELECT on guests to anon; REVOKE SELECT on rsvps from anon verified intact; `gen_random_uuid()` proven to fire as the id default

**GUEST-01 scope note:** Phase 4 satisfies GUEST-01 — the `guests` table exists, the column shape (`full_name`, `household_id`) matches the documented CSV import format, and Tyler can use Supabase Studio's built-in "Import CSV" button against this table today. Phase 7's runbook is operational polish (the exact Studio click-path, the `gen_random_uuid` helper integration, the cleanup steps) — not a missing requirement. The SUMMARY for this plan must call this out explicitly so the requirement traceability is unambiguous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase context (locked decisions — load before drafting SCHEMA.sql)
@.planning/phases/04-guest-list-and-api/04-CONTEXT.md
@.planning/phases/04-guest-list-and-api/04-PATTERNS.md

# Phase 1 — load-bearing for the GRANT-vs-RLS pivot and env contract
@.planning/phases/01-rsvp-enablement/01-01-SUMMARY.md

# Existing rsvps insert payload — defines the column shape that v0.2 extends
@app/(main)/api/rsvp/route.ts

# Project-specific Next.js constraint (not directly relevant to SQL but downstream tasks will need it)
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Phase 1 GRANT pattern (verbatim from 01-01-SUMMARY.md lines 90-98) -->
<!-- "Disable RLS on public.rsvps. REVOKE all default grants from anon, then GRANT only INSERT. -->
<!--  Same security intent (anon writes, never reads/updates/deletes), enforced by Postgres GRANT layer -->
<!--  instead of policy layer." -->
<!-- Phase 4 extension: guests adds GRANT SELECT TO anon (lookup must read). -->
<!-- rsvps keeps its Phase 1 grants intact; verify after ALTER (table-level grants cover ALTERed columns automatically). -->

<!-- Current rsvps column shape (from app/(main)/api/rsvp/route.ts lines 29-36) -->
<!-- The columns this migration must NOT touch (KEEP): full_name, email, attending, dietary_restrictions, note, created_at, id -->
<!-- The column this migration DROPs: guest_count (integer) -->
<!-- The columns this migration ADDs: household_id (uuid NULL), guest_id (uuid NULL), meal_choice (text NULL) -->

<!-- New `guests` table columns (per D-01) -->
<!-- id           uuid PRIMARY KEY DEFAULT gen_random_uuid() -->
<!-- household_id uuid NOT NULL                              -- indexed -->
<!-- full_name    text NOT NULL                              -- indexed via lower(trim(full_name)) for case-insensitive lookup -->
<!-- created_at   timestamptz DEFAULT now() -->

<!-- NEW (W-2 fix): SQL function for the lookup match, called by Plan 04-02 via .rpc() -->
<!-- CREATE OR REPLACE FUNCTION public.lookup_guest_by_name(p_name text) -->
<!--   RETURNS TABLE (id uuid, household_id uuid, full_name text) -->
<!--   LANGUAGE sql STABLE -->
<!--   AS $$ -->
<!--     SELECT id, household_id, full_name -->
<!--     FROM public.guests -->
<!--     WHERE lower(trim(full_name)) = lower(trim(p_name)) -->
<!--     LIMIT 1; -->
<!--   $$; -->
<!-- GRANT EXECUTE ON FUNCTION public.lookup_guest_by_name(text) TO anon; -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author SCHEMA.sql migration artifact</name>
  <files>
    .planning/phases/04-guest-list-and-api/SCHEMA.sql
  </files>
  <read_first>
    - .planning/phases/04-guest-list-and-api/04-CONTEXT.md §"Database schema" (D-01..D-05) and §"Access control" (D-06, D-07) and §"Tyler's data entry workflow" (D-17..D-19) — these decisions dictate every block of SCHEMA.sql
    - .planning/phases/04-guest-list-and-api/04-PATTERNS.md §"SCHEMA.sql (migration artifact)" — provides the verbatim structural template for the SQL file
    - .planning/phases/01-rsvp-enablement/01-01-SUMMARY.md §"Plan Deviations #1" — the GRANT-vs-RLS pivot rationale (lines 88-99). The Phase 4 schema must NOT enable RLS on either table per D-06.
    - app/(main)/api/rsvp/route.ts lines 29-36 — current insert payload defines the columns that are KEEPING (do not touch full_name, email, attending, dietary_restrictions, note)
  </read_first>
  <action>
    Create the file `.planning/phases/04-guest-list-and-api/SCHEMA.sql` with the full v0.2 migration. The structural template is in 04-PATTERNS.md lines 36-98 — follow that section ordering exactly. Required sections in this order:

    1. **Header comment block** — name the phase, instruct "Apply via Supabase Studio → SQL Editor → paste → Run", note the file is idempotent (IF NOT EXISTS / DROP IF EXISTS where supported), warn against running the dev seed in prod.

    2. **CREATE TABLE guests (per D-01)** — `CREATE TABLE IF NOT EXISTS public.guests` with the four columns exactly as specified in D-01 / 04-PATTERNS.md lines 46-51. Then two indexes: `guests_full_name_lower_idx` on `lower(trim(full_name))` and `guests_household_id_idx` on `household_id`, both with IF NOT EXISTS.

    3. **ALTER TABLE rsvps (per D-03)** — three `ADD COLUMN IF NOT EXISTS` statements (household_id uuid, guest_id uuid, meal_choice text — all nullable), one `DROP COLUMN IF EXISTS guest_count`, then `CREATE UNIQUE INDEX IF NOT EXISTS rsvps_guest_id_uniq ON public.rsvps (guest_id) WHERE guest_id IS NOT NULL` (partial unique — D-03's backward-compat carve-out for v0.1 NULL rows).

    4. **GRANT layer (per D-06, D-07)** — apply Phase 1's GRANT-only pattern (no RLS). On `guests`: `GRANT SELECT ON public.guests TO anon` and `GRANT ALL ON public.guests TO authenticated`. On `rsvps`: re-state `GRANT INSERT, UPDATE ON public.rsvps TO anon`, `REVOKE SELECT ON public.rsvps FROM anon`, `GRANT ALL ON public.rsvps TO authenticated` (these may be already in place from Phase 1; re-stating is idempotent and serves as documentation of the intended grant matrix post-migration). Do NOT enable RLS on either table — D-06 is explicit; Phase 1 SUMMARY §"Plan Deviations #1" documents why.

    5. **Lookup function (W-2 fix) — `public.lookup_guest_by_name(p_name text)`:** add a SQL function that Plan 04-02's lookup route calls via `.rpc()`. This commits to a single implementation path (no `.ilike` fallback in Plan 04-02 — W-2). Verbatim block:
       ```sql
       CREATE OR REPLACE FUNCTION public.lookup_guest_by_name(p_name text)
       RETURNS TABLE (id uuid, household_id uuid, full_name text)
       LANGUAGE sql STABLE
       AS $$
         SELECT id, household_id, full_name
         FROM public.guests
         WHERE lower(trim(full_name)) = lower(trim(p_name))
         LIMIT 1;
       $$;
       GRANT EXECUTE ON FUNCTION public.lookup_guest_by_name(text) TO anon;
       ```
       Rationale comment in SQL: "STABLE — no side effects, can use indexes. SQL (not plpgsql) so the planner can engage `guests_full_name_lower_idx`. Not SECURITY DEFINER — runs as caller (anon), which already has SELECT on guests via the GRANT above. Trim is applied symmetrically on both sides of `=` to match the index's `lower(trim(full_name))` expression exactly."

    6. **Helper SQL for Tyler's household_id generation (per D-19)** — a comment-only block showing `SELECT gen_random_uuid();` and a one-line note that Tyler runs this once per household and pastes the result into his CSV's `household_id` column. Phase 7 lifts this into the runbook.

    7. **DEV SEED block (per CONTEXT Claude's Discretion)** — every line prefixed with `--` so the block is comment-out by default. Use the exact 4-row sample from 04-PATTERNS.md lines 92-97 (Tyler Straffon + Emily in household `1111...`, Sarah Else in `2222...`, Sarah Horan in `3333...`). Include the header comment `-- DEV SEED — DO NOT RUN IN PROD` (per D-17 / Claude's Discretion). The two Sarahs intentionally exercise D-10's no-fuzzy-matching policy (strict trim/case-insensitive match must NOT collapse them).

    Do NOT add: RLS enable/policy statements (D-06 forbids), FK constraint on `rsvps.guest_id → guests.id` (D-04 explicitly skips), CHECK constraint on `rsvps.meal_choice` (D-14 puts the enum at app layer so Phase 6 can swap with one line), `email` column on guests (D-02 explicitly defers; revisit if EMAIL-01 promotes), `supabase/migrations/` folder or any Supabase CLI artifacts (out of scope; matches v0.1 manual Studio workflow per STATE 2026-05-30 decision), SECURITY DEFINER on the lookup function (caller-rights is correct — anon already has the GRANT).

    File header MUST cite the decision IDs it implements: D-01, D-02, D-03, D-04, D-06, D-07, D-09 (RPC function implements the case-insensitive trim semantics), D-17, D-19, and CONTEXT Claude's Discretion (dev seed, app-layer meal enum).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'CREATE TABLE IF NOT EXISTS public.guests' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'guests_full_name_lower_idx' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'guests_household_id_idx' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'ADD COLUMN IF NOT EXISTS household_id' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'ADD COLUMN IF NOT EXISTS guest_id' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'ADD COLUMN IF NOT EXISTS meal_choice' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'DROP COLUMN IF EXISTS guest_count' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'rsvps_guest_id_uniq' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'WHERE guest_id IS NOT NULL' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'GRANT SELECT ON public.guests TO anon' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'REVOKE SELECT' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'gen_random_uuid' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'DEV SEED' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'CREATE OR REPLACE FUNCTION public.lookup_guest_by_name' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q 'GRANT EXECUTE ON FUNCTION public.lookup_guest_by_name' .planning/phases/04-guest-list-and-api/SCHEMA.sql && grep -q "lower(trim(full_name)) = lower(trim(p_name))" .planning/phases/04-guest-list-and-api/SCHEMA.sql && ! grep -qE '^[^-]*ENABLE ROW LEVEL SECURITY' .planning/phases/04-guest-list-and-api/SCHEMA.sql && ! grep -qE '^[^-]*CREATE POLICY' .planning/phases/04-guest-list-and-api/SCHEMA.sql && ! grep -qE '^[^-]*REFERENCES public\.guests' .planning/phases/04-guest-list-and-api/SCHEMA.sql && ! grep -qE '^[^-]*CHECK \(meal_choice' .planning/phases/04-guest-list-and-api/SCHEMA.sql && ! grep -qE '^[^-]*SECURITY DEFINER' .planning/phases/04-guest-list-and-api/SCHEMA.sql && [ "$(grep -v '^[[:space:]]*--' .planning/phases/04-guest-list-and-api/SCHEMA.sql | grep -c '^[[:space:]]*INSERT INTO public\.guests')" -eq 0 ]</automated>
  </verify>
  <done>
    SCHEMA.sql exists with all seven required sections (header, CREATE TABLE guests + indexes, ALTER rsvps + partial unique, GRANT layer, lookup_guest_by_name() function + EXECUTE GRANT to anon, gen_random_uuid helper, gated dev seed). Forbidden constructs (RLS enable, CREATE POLICY, FK to guests, CHECK on meal_choice, SECURITY DEFINER, uncommented INSERT into guests) are absent. Decision IDs cited in the file header.
  </done>
  <acceptance_criteria>
    - File `.planning/phases/04-guest-list-and-api/SCHEMA.sql` exists
    - `grep -c 'CREATE TABLE IF NOT EXISTS public.guests' SCHEMA.sql` returns 1
    - `grep -c 'guests_full_name_lower_idx' SCHEMA.sql` returns at least 1
    - `grep -c 'guests_household_id_idx' SCHEMA.sql` returns at least 1
    - `grep -c 'ADD COLUMN IF NOT EXISTS household_id' SCHEMA.sql` returns 1
    - `grep -c 'ADD COLUMN IF NOT EXISTS guest_id' SCHEMA.sql` returns 1
    - `grep -c 'ADD COLUMN IF NOT EXISTS meal_choice' SCHEMA.sql` returns 1
    - `grep -c 'DROP COLUMN IF EXISTS guest_count' SCHEMA.sql` returns 1
    - `grep -c 'rsvps_guest_id_uniq' SCHEMA.sql` returns at least 1
    - `grep -c 'WHERE guest_id IS NOT NULL' SCHEMA.sql` returns at least 1
    - `grep -c 'GRANT SELECT ON public.guests TO anon' SCHEMA.sql` returns at least 1
    - `grep -c 'GRANT ALL ON public.guests TO authenticated' SCHEMA.sql` returns at least 1
    - `grep -c 'REVOKE SELECT' SCHEMA.sql` returns at least 1 (rsvps SELECT REVOKE from anon, carried forward from Phase 1)
    - `grep -c 'CREATE OR REPLACE FUNCTION public.lookup_guest_by_name' SCHEMA.sql` returns 1 (W-2 fix — RPC function for Plan 04-02)
    - `grep -c 'GRANT EXECUTE ON FUNCTION public.lookup_guest_by_name' SCHEMA.sql` returns at least 1
    - `grep -c 'lower(trim(full_name)) = lower(trim(p_name))' SCHEMA.sql` returns at least 1 (matches D-09's exact semantics)
    - `grep -c 'gen_random_uuid' SCHEMA.sql` returns at least 1 (helper for D-19)
    - `grep -c 'DEV SEED' SCHEMA.sql` returns at least 1 (header for the gated insert block per D-17/Claude's Discretion)
    - Forbidden patterns are absent (uncommented): `ENABLE ROW LEVEL SECURITY`, `CREATE POLICY`, `REFERENCES public.guests` (FK), `CHECK (meal_choice` (DB-layer enum), `SECURITY DEFINER` (caller-rights is correct), uncommented `INSERT INTO public.guests` (dev seed must be comment-out by default)
    - File header contains the decision IDs D-01, D-02, D-03, D-04, D-06, D-07, D-09, D-17, D-19
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply SCHEMA.sql via Supabase Studio, verify shape, capture dev-seed UUIDs</name>
  <what-built>
    Task 1 produced `.planning/phases/04-guest-list-and-api/SCHEMA.sql` — the canonical v0.2 migration. This checkpoint is the apply step: Tyler pastes the SQL into Supabase Studio's SQL Editor, runs it, then runs verification queries and pastes the output back so the executor can confirm the live schema matches the intended shape. The dev seed block must also be enabled (uncommented in Studio, not in the repo file) so Wave 2 has at least one guest row to lookup. **Additionally (B-2 fix)**, this checkpoint captures the dev-seed guest UUIDs into a tracked file (`DEV-SEED-IDS.md`) so Wave 2 plans 04-02 and 04-03 can run in parallel without 04-03 depending on 04-02's lookup endpoint at runtime.

    This is `checkpoint:human-verify` (not `auto`) because there is no Supabase CLI integration in this repo — same posture as Phase 1's schema/RLS tasks. The repo file stays version-controlled; Studio is where the SQL executes.
  </what-built>
  <how-to-verify>
    Follow these steps in order. Paste outputs back into chat after each verification query.

    **Step 1 — Apply the migration:**
    1. Open https://supabase.com/dashboard and select the Aspen Wedding project (the same project from Phase 1).
    2. Left sidebar → SQL Editor → click "+ New query" (top-right).
    3. Open `.planning/phases/04-guest-list-and-api/SCHEMA.sql` in your editor. Copy the entire file contents.
    4. Paste into the Studio SQL Editor.
    5. Click "Run" (bottom-right, or Cmd+Enter on Mac).
    6. Expected: "Success. No rows returned" (CREATE TABLE / ALTER / CREATE INDEX / CREATE FUNCTION / GRANT / REVOKE statements do not return rows). If any statement errors, paste the error back into chat and stop — do not proceed.

    **Step 2 — Apply the dev seed (Studio-only, not committed):**
    1. In the same SQL Editor window (or a new query), copy the DEV SEED block from SCHEMA.sql (the four `INSERT INTO public.guests ...` lines).
    2. **Remove the leading `--` from each INSERT line** so the SQL actually executes (the repo file keeps them commented; you uncomment in Studio only).
    3. Click "Run". Expected: "Success. 4 rows affected" (or similar — the wording varies by Studio version).

    **Step 3 — Verify `guests` table shape:**
    Run this query in Studio:
    ```sql
    SELECT column_name, data_type, is_nullable, column_default
    FROM information_schema.columns
    WHERE table_schema = 'public' AND table_name = 'guests'
    ORDER BY ordinal_position;
    ```
    Paste the output. Expected 4 rows: `id` (uuid, NOT NULL, default `gen_random_uuid()`), `household_id` (uuid, NOT NULL, no default), `full_name` (text, NOT NULL, no default), `created_at` (timestamp with time zone, NOT NULL or YES depending on Postgres reporting, default `now()`).

    **Step 4 — Verify `guests` indexes:**
    Run:
    ```sql
    SELECT indexname, indexdef FROM pg_indexes
    WHERE schemaname = 'public' AND tablename = 'guests';
    ```
    Paste the output. Expected: 3 entries — the primary key index (auto-named, on `id`), `guests_full_name_lower_idx` (definition includes `lower(btrim(full_name))` or `lower(trim(full_name))`), and `guests_household_id_idx` (on `household_id`).

    **Step 5 — Verify `rsvps` schema delta:**
    Run:
    ```sql
    SELECT column_name, data_type, is_nullable
    FROM information_schema.columns
    WHERE table_schema = 'public' AND table_name = 'rsvps'
    ORDER BY ordinal_position;
    ```
    Paste the output. Expected to contain: `id`, `full_name`, `email`, `attending`, `dietary_restrictions`, `note`, `created_at` (from v0.1, untouched), plus the three new columns `household_id` (uuid, YES nullable), `guest_id` (uuid, YES nullable), `meal_choice` (text, YES nullable). Expected to NOT contain: `guest_count` (dropped this phase).

    **Step 6 — Verify partial unique index on rsvps:**
    Run:
    ```sql
    SELECT indexname, indexdef FROM pg_indexes
    WHERE schemaname = 'public' AND tablename = 'rsvps' AND indexname = 'rsvps_guest_id_uniq';
    ```
    Paste the output. Expected: 1 row with `indexdef` containing `WHERE (guest_id IS NOT NULL)` (or equivalent Postgres formatting).

    **Step 7 — Verify GRANT matrix:**
    Run:
    ```sql
    SELECT grantee, table_name, privilege_type
    FROM information_schema.role_table_grants
    WHERE table_schema = 'public' AND table_name IN ('guests','rsvps') AND grantee IN ('anon','authenticated')
    ORDER BY table_name, grantee, privilege_type;
    ```
    Paste the output. Expected matrix:
    - `guests` / `anon` → at minimum SELECT present
    - `guests` / `authenticated` → SELECT, INSERT, UPDATE, DELETE present (GRANT ALL)
    - `rsvps` / `anon` → INSERT, UPDATE present; **SELECT must NOT appear** for anon on rsvps (Phase 1 REVOKE intact — this is the load-bearing security boundary)
    - `rsvps` / `authenticated` → SELECT, INSERT, UPDATE, DELETE present (GRANT ALL)

    **Step 8 — Verify dev seed landed AND capture UUIDs into DEV-SEED-IDS.md (B-2 fix):**
    Run:
    ```sql
    SELECT id AS guest_id, household_id, full_name
    FROM public.guests
    ORDER BY household_id, full_name;
    ```
    Paste the output back into chat. Expected: 4 rows — two with `household_id = '11111111-1111-1111-1111-111111111111'` (Tyler + Emily), one with `2222...` (Sarah Else), one with `3333...` (Sarah Horan). The two Sarahs in different households are intentional — they verify D-10's no-fuzzy-matching policy in Wave 2.

    Then **the executor writes the output to `.planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md`** as a markdown table with the exact format:
    ```
    # Dev Seed Guest IDs (Phase 4)

    Captured from Supabase Studio after applying SCHEMA.sql + the dev seed block. Wave 2 plans (04-02, 04-03) read this file to construct curl payloads. Phase 7's cleanup runbook truncates these rows before real invitations go out.

    | full_name      | guest_id                              | household_id                          |
    |----------------|---------------------------------------|---------------------------------------|
    | Emily ...      | <uuid from query>                     | 11111111-1111-1111-1111-111111111111  |
    | Tyler Straffon | <uuid from query>                     | 11111111-1111-1111-1111-111111111111  |
    | Sarah Else     | <uuid from query>                     | 22222222-2222-2222-2222-222222222222  |
    | Sarah Horan    | <uuid from query>                     | 33333333-3333-3333-3333-333333333333  |
    ```
    The executor pastes the actual UUIDs from the Studio query into the table cells. This file is tracked in git; it is the source of truth for Wave 2 fixture IDs.

    **Step 9 — Verify `gen_random_uuid()` default fires (W-4 fix):**
    Some older Supabase projects do not have `pgcrypto` enabled by default, in which case `gen_random_uuid()` silently returns NULL and the INSERT fails. Prove it works on this project:
    ```sql
    INSERT INTO public.guests (household_id, full_name)
    VALUES ('99999999-9999-9999-9999-999999999999', '__schema_verify_delete_me__')
    RETURNING id, household_id, full_name;
    ```
    Assert: returned `id` is a valid uuid (not NULL, not `'99999999-...'`). Then clean up:
    ```sql
    DELETE FROM public.guests WHERE full_name = '__schema_verify_delete_me__';
    ```
    Confirm 1 row deleted. This proves `gen_random_uuid()` actually fires on this Supabase project. **Failure mode:** if the INSERT errors with "function gen_random_uuid() does not exist" or returns a NULL id, the `pgcrypto` extension is missing — add `CREATE EXTENSION IF NOT EXISTS pgcrypto;` to the top of SCHEMA.sql, re-run Task 1's verify, then re-apply.

    **Step 10 — Verify the `lookup_guest_by_name()` RPC function works end-to-end (W-2 fix):**
    Run:
    ```sql
    SELECT * FROM public.lookup_guest_by_name('  TYLER STRAFFON  ');
    ```
    Paste the output. Expected: 1 row with `full_name = 'Tyler Straffon'` and the matching household_id `11111111-...`. The leading/trailing whitespace and uppercase input prove the function applies the `lower(trim(...))` normalization on both sides. If it returns 0 rows, the function logic is wrong — re-check SCHEMA.sql against the W-2 spec.

    **Reply with all 8 query outputs** (Steps 3-10), confirm `DEV-SEED-IDS.md` was created, and either "approved" if all expectations match, or describe the specific divergences.
  </how-to-verify>
  <resume-signal>Type "approved" with all 8 query outputs pasted (Steps 3-10) AND confirmation that `.planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md` was written with the captured UUIDs, or describe any divergence from the expected shape.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → Supabase | SCHEMA.sql is the canonical migration; applied manually via Studio SQL Editor (no CI/CD migration tooling, by design — matches v0.1 manual Studio workflow per STATE 2026-05-30 decision) |
| Public web → Supabase `public.guests` | After this migration, anon role can SELECT from guests (D-07) and EXECUTE `lookup_guest_by_name()` (W-2 fix). The data exposed is names already on Tyler's invite list; no addresses, emails, or phones (D-02 defers email column for exactly this reason). |
| Public web → Supabase `public.rsvps` | Phase 1's GRANT matrix is carried forward: anon has INSERT/UPDATE only, no SELECT. The migration must NOT regress this (Step 7 verification covers it). |
| Tyler → live database | The dev seed lands real rows in production-shape data. Phase 7 covers the pre-launch cleanup runbook (truncate test households, re-import real list). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-RLS-REGRESSION | Elevation of Privilege | `public.rsvps` GRANT matrix | mitigate | Step 7 verification explicitly asserts `anon` does NOT have SELECT on rsvps. If the ALTER TABLE inadvertently caused a grant change (it shouldn't — table-level grants cover ALTERed columns), the migration is halted and rolled back. |
| T-04-GUESTS-PII | Information Disclosure | `public.guests` SELECT to anon (D-07) | accept | Per CONTEXT D-12: ~100-150 person invite list; the data exposed is names only (no addresses, emails, phones per D-02); everything fronted by SITE_ACCESS_CODE proxy gate. Reversal trigger: if invite list >500 or if D-02 reverses and email is added to guests, re-evaluate. |
| T-04-DEV-SEED-IN-PROD | Tampering | DEV SEED INSERT block in SCHEMA.sql | mitigate | The block is comment-out by default in the repo file (every line prefixed `--`). Step 2 of the checkpoint instructs Tyler to uncomment in Studio only. Phase 7's runbook covers truncating the test households before sending real invitations. |
| T-04-NO-FK-ORPHAN | Tampering | `rsvps.guest_id` with no FK to `guests.id` (D-04) | accept | Per CONTEXT D-04: Tyler controls the guests table; he won't delete people post-RSVP. If a guest is removed pre-RSVP, no orphan rows exist yet. Reversal trigger: if guest-deletion-post-RSVP becomes a workflow, add `ON DELETE SET NULL` FK. |
| T-04-MIGRATION-NON-IDEMPOTENT | Tampering | Re-applying SCHEMA.sql | mitigate | All statements use IF NOT EXISTS / IF EXISTS / DROP IF EXISTS. CREATE INDEX IF NOT EXISTS is idempotent. ADD COLUMN IF NOT EXISTS is idempotent. CREATE OR REPLACE FUNCTION is idempotent. Re-running the file is safe; downstream phases can re-apply if they need to add columns later. |
| T-04-RPC-PRIVILEGE | Elevation of Privilege | `lookup_guest_by_name()` function exposed to anon | accept | Function is STABLE, not SECURITY DEFINER — runs as caller (anon), which already has SELECT on guests via the same GRANT layer. Adding the function does not increase anon's reachable data; it merely provides a parameterized query path that engages `guests_full_name_lower_idx`. T-04-GUESTS-PII covers the data-exposure side. |
| T-04-PGCRYPTO-MISSING | Denial of Service | Missing `pgcrypto` extension → `gen_random_uuid()` returns NULL → INSERTs fail | mitigate | Step 9 of the checkpoint explicitly tests this with an INSERT...RETURNING + DELETE pair. Failure mode is documented inline (add `CREATE EXTENSION IF NOT EXISTS pgcrypto;` to SCHEMA.sql top, re-apply). |
| T-04-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this plan. SCHEMA.sql is SQL only; no new dependencies. |
</threat_model>

<verification>
After both tasks complete:

1. **File exists in repo:** `test -f .planning/phases/04-guest-list-and-api/SCHEMA.sql && wc -l < .planning/phases/04-guest-list-and-api/SCHEMA.sql` returns a value ≥ 70.
2. **DEV-SEED-IDS.md exists (B-2):** `test -f .planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md && grep -cE '11111111-1111-1111-1111-111111111111' .planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md` returns at least 2 (Tyler row + Emily row, same household).
3. **Live schema matches:** All 8 query outputs from Task 2 Step 3-10 pasted in chat and reviewed.
4. **Anon SELECT on rsvps is blocked:** Step 7 confirms no row with `(grantee='anon', table_name='rsvps', privilege_type='SELECT')`.
5. **Anon SELECT on guests is permitted:** Step 7 confirms a row with `(grantee='anon', table_name='guests', privilege_type='SELECT')`.
6. **Dev seed populated:** Step 8 confirms 4 guest rows across 3 household_ids, including the two Sarahs in different households (D-10 fuzzy-matching test fixture).
7. **gen_random_uuid() fires (W-4):** Step 9's INSERT...RETURNING returns a non-NULL, non-supplied uuid; DELETE removes 1 row.
8. **lookup_guest_by_name() works (W-2):** Step 10 returns Tyler Straffon's row when called with `'  TYLER STRAFFON  '`.
9. **No RLS enabled:** Step 7 implicitly verifies (if RLS were on with no policy, anon SELECT on guests would fail; if Step 7 returns the expected anon-SELECT row for guests, RLS is correctly disabled).
10. **Phase 1 grant matrix intact:** Step 7 confirms `anon` has INSERT and UPDATE on rsvps.
</verification>

<success_criteria>
- `SCHEMA.sql` committed to git with all seven required sections, decision IDs cited in header, dev seed comment-gated, lookup_guest_by_name() function present with EXECUTE GRANT to anon, forbidden constructs (RLS, CREATE POLICY, FK, CHECK, SECURITY DEFINER, uncommented INSERT) absent.
- `DEV-SEED-IDS.md` committed to git with the captured UUID table (4 rows across 3 households).
- Live Supabase has `public.guests` with id/household_id/full_name/created_at columns, both indexes, 4 dev-seed rows across 3 households.
- Live Supabase has `public.rsvps` with new household_id/guest_id/meal_choice columns added, `guest_count` dropped, `rsvps_guest_id_uniq` partial unique index present.
- Live Supabase has `public.lookup_guest_by_name(text)` function with EXECUTE granted to anon; Step 10 query proves it works.
- `gen_random_uuid()` proven to fire (Step 9 INSERT...RETURNING returns a real uuid).
- Live Supabase GRANT matrix: anon has SELECT on guests, INSERT+UPDATE (no SELECT) on rsvps; authenticated has ALL on both.
- All 8 verification query outputs pasted in chat for the SUMMARY to reference.
- SUMMARY explicitly notes the GUEST-01 traceability per the framing in the `<output>` block below.
</success_criteria>

<output>
Create `.planning/phases/04-guest-list-and-api/04-01-SUMMARY.md` when done. The plan also produces `.planning/phases/04-guest-list-and-api/DEV-SEED-IDS.md` during Task 2 (per the B-2 fix); both files must be committed.

SUMMARY MUST include:

- **Frontmatter** following the project's SUMMARY schema (phase, plan, subsystem, tags, requires, provides, affects, tech-stack, patterns, key-files, key-decisions, patterns-established, requirements-completed, duration, completed).
- **`provides:` field** must include: "`public.guests` table created (id uuid pk, household_id uuid indexed, full_name text indexed via lower(trim)), 4 dev-seed rows across 3 households", "`public.rsvps` extended (added household_id/guest_id/meal_choice nullable; dropped guest_count)", "Partial unique index `rsvps_guest_id_uniq` enabling upsert by guest_id (Wave 2 dependency)", "`public.lookup_guest_by_name(p_name text)` STABLE SQL function with EXECUTE GRANT to anon — used by Plan 04-02's lookup route via `.rpc()` (W-2 fix)", "GRANT SELECT on guests to anon; Phase 1 grant matrix on rsvps intact (verified post-migration)", "DEV-SEED-IDS.md tracked artifact — captured guest UUIDs that Wave 2 plans read at runtime (B-2 fix decouples 04-03 from 04-02)".
- **`affects:` field** must include 04-02 (lookup needs guests SELECT + `lookup_guest_by_name()` RPC + dev seed), 04-03 (submit needs rsvps_guest_id_uniq + altered columns + DEV-SEED-IDS.md for payload IDs), and Phase 7 (CSV import workflow lifts SCHEMA.sql helper SQL and dev seed cleanup).
- **`requirements-completed:` field** must list: GUEST-01 (table exists with the documented CSV import column shape; Studio's "Import CSV" button works against it today; Phase 7 is operational polish), MEAL-03 (meal_choice column added to rsvps).
- **GUEST-01 traceability note** in the body — explicit paragraph stating: "Phase 4 satisfies GUEST-01 — the `guests` table exists, the column shape (`full_name`, `household_id`) matches the documented CSV import format, and Tyler can use Supabase Studio's built-in 'Import CSV' button against this table today. Phase 7's runbook is operational polish (the exact Studio click-path, the `gen_random_uuid` helper integration, the cleanup steps) — not a missing requirement." (W-1 fix — drops the "enabled by" minimizing language.)
- **Verification query outputs** from Task 2 Steps 3-10 embedded in the SUMMARY as a "Test Results" table so they're not lost to the chat scroll. Include the Step 9 gen_random_uuid() proof and the Step 10 lookup_guest_by_name() proof.
- **DEV-SEED-IDS.md cross-reference**: include a copy of (or link to) the captured UUID table in the SUMMARY so Wave 2 plans have a single reference.
- **Cleanup note** — flag the 4 dev-seed rows for Phase 7 cleanup before real invitations go out.
</output>
</output>
